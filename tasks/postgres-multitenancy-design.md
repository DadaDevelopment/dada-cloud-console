# Postgres multi-tenancy: кластер + LB + шарды

Дизайн v2. Триггер — запрос клиента `artemmendeleev@gmail.com` поднять
`shared_buffers` до 2-4GB на своей БД. Разбор показал, что тюнить нечего:
у клиента нет своего постгреса, а общий инстанс не выдержит и текущей
нагрузки. v1 предлагал «пулы» (N StatefulSet из одного чарта) — отвергнуто
владельцем: это не масштабируется в кластер и не даёт единой точки входа.

## Целевая архитектура

```
apps ──► pg-router (LB: пулер-роутер, 2+ реплик, единый DSN навсегда)
              │  роутинг по dbname → шард (реестр в control plane)
     ┌────────┼─────────────┐
  shard-0   shard-1   ...  shard-N     (шард = primary + replica, свои ресурсы)
  ПЛАТФОРМА  клиенты        клиенты
```

Три компонента:

1. **Шарды** — независимые HA-инстансы Postgres. Каждая логическая БД целиком
   живёт на одном шарде. `shard-0` — только платформенное (`cloud-console`,
   `keycloak`, внутренние сервисы), клиентский трафик туда не роутится.
2. **pg-router (LB)** — фарм пулеров-роутеров перед всеми шардами. Приложение
   всегда коннектится на один и тот же хост; роутер по имени базы отправляет
   коннект на нужный шард и пулит нативные соединения.
3. **Реестр** tenant→shard — таблица в control plane, отражается в
   `ServiceDatabaseV2.spec.shard`, композиция выбирает `providerConfigRef`
   шарда. Плейсмент только автоматический.

## Как это делают снаружи (исследование 2026-08-06)

- **SPQR (Яндекс Cloud)** — роутер+координатор+etcd, шардирует ОДНУ схему по
  ключу шардирования: роутер парсит первый запрос транзакции, ребаланс
  ε-split диапазонов ключей, мультишард best-effort, 2pc без глобального
  snapshot isolation. Подходит для «одна таблица orders на терабайты».
  **Не наш случай**: у нас tenant = целая логическая БД, ключа шардирования
  нет, и его ограничения (парсинг SQL, распределённые транзакции) нам не
  нужны — БД и так атомарно лежит на одном шарде.
- **Supavisor (Supabase)** — наш случай один в один: multi-tenant
  пулер-кластер, реестр тенантов в отдельном Postgres, клиент коннектится в
  любую ноду, пул тенанта живёт на ноде-владельце, миллионы клиентских
  коннектов → пул нативных. Так сервятся сотни тысяч тенантов.
- **Yandex Managed PG** — каждый кластер за Odyssey (их пулер: 1M+ RPS на
  сотнях хостов, multithreaded, session/transaction pooling, авто-Cancel и
  Rollback брошенных транзакций при обрыве клиента).
- **Яндекс Такси (процессинг)** — virt-шарды: `hash(key) % N_virt` → статический
  маппинг в конфиге, без центрального координатора; решардинг фазами
  read-only → переключение записи → фоновая переливка → валидация.

Вывод: для DB-per-tenant индустрия строит **router + registry + шарды целыми
БД**, а не key-based шардирование. Key-based (SPQR/Citus) — если когда-нибудь
одна клиентская БД перерастёт шард; тогда она получает выделенный шард, и
только внутри него встаёт SPQR. Не сейчас.

## Компоненты подробно

### Шарды

- Шард = Postgres primary + streaming replica, anti-affinity по нодам,
  ресурсы от 4Gi RAM (не сегодняшний 1Gi), свой том, свой superuser-секрет,
  свой `ProviderConfig` (`postgresql-shard-N`).
- `shared_buffers` на шарде ставится под его RAM (25%), `pg_stat_statements`
  и `log_min_duration_statement = 1s` включены с рождения.
- Оператор HA (CloudNativePG либо продолжаем bitnami+ручной failover) —
  решить в шаге 1; CNPG даёт failover, backup-интеграцию и `-rw`/`-ro`
  сервисы из коробки, bitnami — то, что уже умеем.
- Существующий `databases/postgresql-0` становится `shard-1` (клиенты
  остаются на месте, ноль переездов в момент включения роутера).

### pg-router (LB)

- Кандидаты: **Odyssey** (Яндекс, multithreaded, cloud-scale) или
  **PgBouncer-фарм** (проще, per-db маппинг `dbname → host` в конфиге,
  масштабируется репликами за Service). Оба умеют роутить по имени базы на
  разные хосты. Выбор в шаге 2 по результату стенда; дефолт — PgBouncer как
  наименее рискованный, Odyssey — если упрёмся в CPU одного треда.
- Deployment ≥2 реплик за Service `pg-router.databases.svc` — этот хост
  зашивается во все connection-секреты вместо адреса инстанса. **DSN клиента
  больше никогда не меняется**, что бы мы ни делали с шардами.
- Конфиг (маппинг dbname→shard + auth) рендерится gitops-агентом из реестра
  тем же механизмом, что values приложений; reload онлайн, без разрыва.
- Auth: креды генерит наша композиция → SCRAM-хэши кладутся в auth-файл
  роутера из тех же секретов. Мы владеем всеми кредами — passthrough не нужен.
- Платформенное (`cloud-console`, `keycloak`) ходит на `shard-0` — либо через
  тот же роутер (отдельный listener/pool class), либо напрямую в
  `shard-0-rw`: решить в шаге 5; напрямую = SSO не зависит от роутера,
  через роутер = единая труба и единые метрики. Дефолт — напрямую:
  зависимость «вход в консоль требует живого роутера» опаснее, чем два пути.

### Реестр и плейсмент

- Таблица `db_shards` (shard, dsn-secret, capacity, state) +
  `spec.shard` на XR (аналогично уже сделанному `spec.tier`).
- Плейсмент при создании: платформенное → `shard-0`; клиентское → шард с
  наименьшей суммой `pg_database_size_bytes` среди открытых. Руками — никогда.
- Рост = добавить строку шарда. Реестр мал (десятки строк), etcd/координатор
  как в SPQR не нужен — хватает Postgres control plane, как у Supavisor.

### Переезд БД между шардами

Порядок как у Такси, но на уровне целой БД:
1. logical replication базы на целевой шард (или dump/restore для мелких);
2. quota-воркер ставит `read-only` (механизм уже есть — `spec.enforcement`);
3. догон реплики, валидация счётчиков;
4. флип строки реестра → rerender конфига роутера → reload;
5. снятие read-only, старая копия удаляется после срока хранения.
Клиент видит паузу записи в минуты, DSN не меняется.

## Тиры и квоты (сделано, ортогонально шардам)

Значения тиров в values чарта `crossplane-platform-api`, применяются через
`Role.connectionLimit` + `configurationParameters` — ноль ручных ALTER.

| тир | коннекты | размер | statement_timeout | idle_in_tx | temp_file_limit | work_mem |
|---|---|---|---|---|---|---|
| free | 15 | 1 GB | 15s | 30s | 512MB | default |
| starter | 40 | 5 GB | 30s | 60s | 2GB | 8MB |
| business | 100 | 25 GB | 120s | 300s | 8GB | 16MB |
| dedicated shard | по договору | по договору | настраиваемо | настраиваемо | настраиваемо | настраиваемо |

Размер — жёсткая квота: воркер `db_quota_state`, ступени warn 0.8 /
read-only 1.0 / frozen 1.25 / снятие 0.9, принуждение через
`spec.enforcement` → операция `SetDatabaseEnforcement`. CPU/IO квотируем
косвенно (timeout'ы + temp_file_limit), шумного соседа детектим по доле
`pg_stat_database_active_time_seconds_total` шарда.

## Наблюдаемость

- Дэшборд `Postgres shards`: на шард — коннекты/`max_connections`, том,
  cache hit ratio, deadlocks, `temp_bytes`, топ-5 баз по размеру и
  `active_time`; на роутер — клиентские vs серверные коннекты, время
  ожидания пула, ошибки роутинга.
- Карточка БД в консоли: размер/квота, коннекты/квота, шард, состояние
  (ok / warn / read-only), кнопка апгрейда тира.
- PrometheusRule (в dada-cloud, с `keep_firing_for`): коннекты шарда > 80%,
  том шарда > 80%, cache hit < 0.9 5м, база в read-only, лаг реплики,
  насыщение пула роутера.

## Порядок работ

- [x] a. XRD + композиция: `spec.tier` (`a6b28000`), живой tier в снапшоте
      (`969e90d1`), рычаг `spec.enforcement` (`6cd84dc9`, `627f4c69`).
- [x] b. Воркер квоты размера: `db_quota_state`, лестница с гистерезисом,
      аудит, письма — `885d2464`. Живьём не прогонялось (kubectl недоступен).
- [x] 1. Шард-чарт: `shard-0` рядом с существующим инстансом (= будущий
      `shard-1`) — argo-infra `94016d17`, app
      `platform/prod/apps/postgres-shard-0`, bitnami/postgresql 16.7.27,
      сервис `pg-shard-0-postgresql.databases.svc:5432`, лимит 4Gi,
      `shared_buffers=1GB`, `pg_stat_statements` +
      `log_min_duration_statement=1s` с рождения, ServiceMonitor включён.
      Оператор HA: **bitnami**, не CNPG (оператор + новые CRD + новый режим
      отказа на пути SSO ради failover, которого сегодня нет; контракт роутера
      `dbname → host` от оператора не зависит, отдельный шард переведём позже).
      **Реплика отложена**: `architecture: standalone`, второй под и второй том
      Longhorn вслепую не добавляем после потери ноды по диску 2026-08-06.
      Включение = `architecture: replication` + `readReplicas.replicaCount: 1`,
      данные и имя сервиса не меняются.
      Живьём не проверено: с этой сети k8s API недоступен вообще (и через
      bypass-прокси, и напрямую с en0 — 502/timeout), проверено `helm template`
      на values 16.7.27 и наличием bitnamilegacy-тегов в реестре.
- [x] 1b. Проверено живьём 07-08: под `pg-shard-0-postgresql-0` 2/2 Ready
      13 часов, `shared_buffers` = 1GB, `shared_preload_libraries` =
      `pgaudit,pg_stat_statements`, `max_connections` = 200, 3 базы; в
      Prometheus отвечают ОБА селектора `pg_database_size_bytes` — shard-1
      19.4 GB, shard-0 22.9 MB.
      Реплика НЕ включена, и это не забывчивость: `longhorn-prod` держит
      `numberOfReplicas: 3`, свободно по нодам 16.8/17.8/15.9/11.7 GiB против
      уже расписанных 58/32/85/64 GiB, файловые системы нод 76.5/75.1/83.3/
      88.9%. Второй том на 10Gi не размещается ни на одной ноде — реплику
      возвращать только после расширения ёмкости, иначе получим degraded том
      вместо запаса прочности.
- [x] 2. pg-router — argo-infra `4563077c`, app
      `platform/prod/apps/pg-router`, сервис `pg-router.databases.svc:5432`,
      PgBouncer 1.25.2, 2 реплики + anti-affinity + PDB minAvailable=1,
      pool_mode=transaction. Маршруты: wildcard `*` → дефолтный шард (то есть
      туда, где все базы лежат сегодня — включение роутера не двигает ничего),
      отдельная строка только у базы, размещённой на другом инстансе.
      Аутентификация — SCRAM pass-through: роутер спрашивает у шарда
      сохранённый секрет (`auth_query`) и переигрывает SCRAM-доказательство
      клиента, паролей арендаторов у него нет. Условия документированы: клиент
      по SCRAM, одинаковые секреты, в записи базы НЕТ `user=` — поэтому
      добавлять `user=` в маршрут нельзя никогда. PreSync-джоба на каждый шард
      создаёт роль поиска и SECURITY DEFINER `pgbouncer.get_auth` (pg_authid
      читает только суперпользователь); `auth_dbname=postgres`, поэтому функция
      нужна одна на шард, а не одна на каждую из 33 баз. Reload маршрутов без
      разрыва коннектов — sidecar в общем process namespace шлёт SIGHUP.
      **Odyssey отвергнут**: маршрутизация по имени базы есть у обоих, но у
      PgBouncer per-database host, онлайн RELOAD `[databases]` и SCRAM
      pass-through документированы, а стенда для замера нет; компонент, через
      который идёт весь трафик арендаторов, должен быть скучным. Вернуть, если
      один поток упрётся в ядро.
      Стенд НЕ проводился и живьём НЕ проверено: k8s API с этой сети
      недоступен, docker локально висит на pull. Проверено `helm template`
      (включая ветку per-db override) и наличие тегов образов в registry.
      Открытые риски до первого живого прогона: поведение entrypoint
      `edoburu/pgbouncer` с примонтированным read-only `pgbouncer.ini` и
      результат bootstrap-SQL на общем инстансе.
- [x] 3. Реестр и плейсмент. Инфра-половина — argo-infra `056e754a`:
      `spec.shard` в XRD (enum по `serviceDatabase.shards`, дефолт `shard-1`),
      композиция подставляет `providerConfigRef` шарда во все четыре объекта,
      `crossplane-platform-system` рендерит Secret+ProviderConfig на каждый
      шард из `providers.sql.shards`, бэкап K10 целится в StatefulSet шарда
      (иначе точка восстановления не содержала бы базу). Дефолт `shard-1`
      отображается ровно в `postgresql-prod` — менять `providerConfigRef` под
      живой базой нельзя, это её сервер.
      Бэкенд-половина — миграция `105_db_shards.sql` (реестр: state
      open/closed/draining, `is_platform`, `capacity_bytes`,
      `metrics_selector`; строки `shard-1` открытая клиентская и `shard-0`
      платформенная закрытая), плейсмент `placeTenantDatabaseShard` —
      наименьшая сумма `pg_database_size_bytes` среди открытых
      непплатформенных, шард сверх `capacity_bytes` пропускается,
      неизмеренный считается пустым, ничьи разруливаются по имени. Любая
      ошибка реестра/Prometheus → пустой шард → дефолт XRD, то есть сегодняшнее
      размещение; создать базу это не мешает. `shard` едет в
      `resources.values.yaml`, переживает переезд аппа между проектами
      (иначе provider целился бы в инстанс без этих данных) и попадает в
      снапшот из ЖИВОГО CR (`crDatabaseShard`, дефолт `shard-1`).
      Не проверено живьём: `metrics_selector` обеих строк —
      `service="postgresql-metrics"` / `service="pg-shard-0-postgresql-metrics"`
      — выведен из соглашения ServiceMonitor bitnami, не с работающего
      Prometheus (kubectl недоступен). Ошибка в селекторе = шард выглядит
      пустым, а не сломанный плейсмент.
- [x] 4. Секреты приложений → DSN роутера. Не композицией: provider-sql
      публикует connection details ТОЛЬКО при создании роли и при смене пароля
      (`pkg/controller/cluster/postgresql/role/reconciler.go` v0.14.0,
      `GetConnectionDetails` берёт `endpoint` из секрета ProviderConfig), значит
      подмена адреса в ProviderConfig не переписала бы ни один живой секрет, а
      направить туда сам provider-sql нельзя: админ ходит в базу `postgres`,
      которая есть на каждом шарде, — роутер по имени базы её не различит.
      Поэтому пишем секрет сами: status-reconciler gitops-агента правит
      `endpoint`/`port` в `<appRef>-db-credentials`, оставляя username/password
      нетронутыми (патч по ключам `data`, не `stringData`). Прямой адрес
      сохраняется в аннотации `platform.dada-tuda.ru/db-direct-endpoint` — это
      единственный путь назад, если роутер ляжет. Идемпотентно и
      самовосстанавливается: после ротации пароля Crossplane снова впишет адрес
      шарда, следующий тик снова поставит роутер.
      Рычаги: `DB_ROUTER_HOST` (пусто = выключено, дефолт),
      `DB_ROUTER_DIRECT_SHARDS=shard-0` — платформенные базы остаются на прямом
      адресе, чтобы вход в консоль не зависел от роутера (решение из шага 5
      зафиксировано здесь). Аннотация `db-router-opt-out=true` на секрете —
      побег для приложения, которому transaction pooling не подходит.
      Проверено `go build`/`go vet`/тесты (8 новых, fake clientset) + `helm
      template`/`helm lint`. Живьём НЕ включено: флаг пустой, а сам pg-router
      всё ещё без стенда (шаг 1b/2).
      ГЕЙТ перед включением ЗАКРЫТ: маршрутная таблица больше не ведётся
      руками. Реестр `db_shards` получил адрес шарда (миграция 107, backfill с
      адресов ProviderConfig'ов), консоль отдаёт готовую секцию `[databases]`
      на `GET /internal/db/routes.ini`, а pg-router тянет её init-контейнером и
      сайдкаром в emptyDir; `pgbouncer.ini` теперь только `%include`. Правила
      рендера: строка появляется ТОЛЬКО у базы вне дефолтного шарда (wildcard
      покрывает остальное, так что включение ничего не двигает), имя на двух
      шардах выбрасывается в комментарий (маршрутизация по имени не имеет
      верного ответа — лучше wildcard, чем чужие данные), имя вне
      `[A-Za-z0-9_-]` не попадает в файл вовсе (иначе tenant переписывал бы
      таблицу через имя базы), а рендер без адреса дефолтного шарда — ошибка,
      потому что файл без wildcard отказал бы всем неперечисленным. Промах
      фетча не ломает ничего: на старте — запечённый в чарт fallback
      (до-шардовое размещение), в работе — остаётся текущий файл. Релоадер
      теперь хеширует и `routes.ini`, так что смена таблицы = SIGHUP, не
      рестарт.
      Ещё: приложение читает секрет на старте, адрес меняется на следующем
      рестарте пода, не мгновенно.
      Роутер проверен живьём 07-08 (закрывает «стенд НЕ проводился» из
      шагов 2 и 4): консоль отдаёт таблицу, сайдкар логирует `routing table
      updated`, `/routes/routes.ini` в поде совпадает с ответом консоли,
      релоадер шлёт SIGHUP (`reloading pgbouncer pid=16`), pgbouncer пишет
      `got SIGHUP, re-reading config`, а реальный клиент через
      `pg-router.databases.svc:5432` логинится по SCRAM и приезжает на
      `10.244.2.162` = `postgresql-0` (shard-1), как и задумано wildcard'ом.
      Отказной путь тоже наблюдался: 404 от консоли → fallback-таблица,
      pgbouncer жив.
      Две ловушки, стоившие времени: (1) в ns `databases` висит default-deny
      NetworkPolicy от K10 (`podSelector: {}`, `policyTypes: [Ingress]`), у
      шардов свои bitnami-политики, а у pg-router политики не было — под
      отвечал на 127.0.0.1 и молча ронял пакеты через Service (таймаут, НЕ
      refused); лечится собственной NetworkPolicy на порт 5432, добавлена в
      чарт. (2) `SHOW DATABASES` не показывает строку `*` до первого
      клиентского коннекта — PgBouncer создаёт запись из wildcard лениво, это
      не признак непрочитанного `%include`.
- [~] 5. Переезд платформенных БД на `shard-0`. Окна обслуживания не будет:
      благодаря роутеру переключение обязано выглядеть секундным флапом, и
      механика для этого уже лежит. Сделано: `routerAdmin.Cutover`
      (`1549f3fa`) — PAUSE на каждой реплике роутера, RELOAD с опросом, пока
      каждая не отчитается новым хостом, RESUME на любом пути выхода. Адреса
      реплик берутся из headless Service `pg-router-admin` (argo-infra
      `f7487b6e`): PAUSE через балансируемый Service попал бы в один под, а
      второй продолжал бы лить записи в покидаемый шард. Там же
      `routeSync.intervalSeconds` 60 → 5 и проверка файла reloader'ом 10 → 2
      секунды: это ровно то время, что арендатор стоит в очереди.
      Осталось: сам переезд данных — предсинк логической репликацией по
      `DB_SHARD_ADMIN_DSNS` (роль с её SCRAM-верификатором копируется на
      целевой шард отдельно, роли кластерные), наблюдение за лагом, флип
      реестра + `Cutover`, состояние в `db_moves` и админ-ручка. Плюс
      `DB_ROUTER_ADMIN_*` в values консоли — не выставлял, пока их некому
      читать.
- [ ] 6. `odds-research` (15 GB) → выделенный шард; закрывает исходный тикет
      fonbet. Ждёт воркер переезда из шага 5, окно владельцу не нужно.
- [~] 7. Биллинг: БД = тир + гигабайты (+ dedicated shard по договору),
      а не штука. Сделано: гигабайты стали ВИДНЫ. `buildUsage` теперь считает
      `storage_gb` по факту — тома приложений (`resource_snapshots ->
      volume.size`) плюс размер управляемых баз из `db_quota_state`, то есть
      из тех же строк, что воркер квоты пишет из `pg_database_size`; вторая
      мерка разошлась бы с той, что переводит базу в read-only. Фронт эту
      строку уже рисовал — ключа в ответе просто не было, и карточка молча
      исчезала: клиент с базой на 15 ГБ не видел этого числа нигде.
      Число показывается, но НЕ гейтит: `overQuotaLines` перечисляет ресурсы
      поимённо, а плановый `storage_gb` сегодня ограничивает один том
      приложения, а не сумму по организации, поэтому предупреждения «почти у
      лимита» на фронте сузили до `ENFORCED_USAGE_KEYS` — обещать стену,
      которой нет, хуже, чем не обещать ничего.
      Осталось: сам тариф (цена за гигабайт/тир, dedicated shard по
      договору) и включение гейта — это решение владельца и оно видно
      клиенту, не автономный шаг.
- [x] 8. Наблюдаемость. Сделано: карточка шардов в консоли уже живёт на
      `/admin/db-shards` (размеры, рост за 7 дней, коннекты, топ баз,
      сироты), метрики маршрутизации — `dada_db_routes`,
      `dada_db_routes_dropped{reason}`, `dada_db_databases_by_shard{shard}`
      публикуются ИЗ ТОГО ЖЕ рендера, что отдаёт файл роутеру (второй расчёт
      разошёлся бы с реальностью), плюс реестровые
      `dada_db_shard_capacity_bytes` / `dada_db_shard_state` из коллектора.
      Алерты: `DadaDBRouteUnaddressable` (>0 баз, которые роутер не умеет
      адресовать по имени — до переезда безвредно, после переезда клиент
      приезжает на инстанс без своих данных) и `DadaDBShardClosed`.
      Причины дропа публикуются нулями всегда, иначе алерт «гаснет» на
      отсутствии данных, а не на исправлении.
      Дэшборд: argo-infra `77338fde` (ветка `console-migration`),
      `infra/dada-db-shards.json`, uid `dada-db-shards` — маршруты и дропы
      рядом (обе величины из одного рендера, разойтись не могут), базы и
      ёмкость по шардам, топ-15 баз по размеру. Положен в провайдер `infra`:
      его configMap уже смонтирован (697 КБ из 1 МиБ), новый провайдер стоил
      бы правки values kube-prometheus-stack ради одной панели.
      Осталось: пул самого роутера (клиенты, ожидание) — у pg-router нет
      экспортера, нужен sidecar pgbouncer_exporter.

Шаги 1+5 снимают главный риск (арендатор роняет SSO). Шаг 6 закрывает
исходную боль клиента. Шаги 2-4 делают дальнейший рост (shard-N, переезды)
рутиной без участия клиентов.

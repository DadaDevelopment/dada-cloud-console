# Гипотезы DADA Cloud

Живой реестр. v2 — merge двух backfill-проходов (critic-1: сессия 2026-08-12; critic-2: исторический backfill май–август).
Правила: гипотеза без теста и kill-критерия — мнение. Стратегические решения фаундера маркируются отдельно и не притворяются customer-гипотезами. Internal dogfooding ≠ customer validation.
Статусы: `supported` / `strongly-supported` / `plausible` / `untested` / `blocked` / `killed` / `superseded` / `strategic-call` / `bet`.

---

## 1. Факты (не гипотезы)

### Продукт
- F1 (май): рабочий GitOps control-plane loop: UI → Backend → DB → Worker → Git desired state → ArgoCD → K8s CRD. Реальный ServiceDatabase в проде.
- F2 (июнь): customer-facing позиционирование — «backend из GitHub в прод: сборка, БД, HTTPS/домен, без DevOps, из РФ за рубли». ICP: solo founders / малые команды / агентства.
- F3: продукт шире фронтенда: долгоживущие workloads, managed Postgres, Redis, домены/HTTPS, VM, git-driven deploy, logs/metrics, позже AI-примитивы.
- F4 (июль): Bring Your VPS построен на ~60–70%: manual VM connect, VM runtime, Compose deploy from Git, compose/.env editor, live container state, logs, rollback-примитивы, operation queue/RBAC. Не хватало discovery/import UX.
- F5: сознательное решение НЕ строить Coolify/Portainer feature parity (см. killed-список в backlog.md).

### Трекшн (июльский срез + август)
- ~10 projects; 3–4 реальных внешних юзера; 1 production tenant с 6 Ready apps.
- ≥3 регистрации с нулём деплоев — **главная улика activation cliff**.
- Первые платежи были; recurring нет.
- Органика при открытой двери: ~5 сайнапов/нед (боты, стартапы, сайты). Источники не размечены.
- Регистрация закрыта: нет оплаты (ждём регулятора) + абуз free VM фармерами.
- Churned-юзер (2 бота, 2 недели, запрос на удаление): автофикс открыл PR — не принят; 1 касание (2026-08-11) без ответа.
- Автофикс: внутренние мержи есть, внешних 0. Box: 3 774 мин, 100% internal → **untested, не falsified**.

### Из automator state (2026-08-12, авторитетные psql/Метрика-замеры)
- Воронка инструментирована: 187 uniques/30д на лендинге → **−83% до клика «регистрация»**; verifyEmail съедает **27%** сайнапов; терминальное действие умирающих — «TriggerBuild → тишина» и «ViewApps».
- Активация ~45–54%; retention-раскол без исключений: **все вернувшиеся деплоили СВОЙ код, все template-юзеры ушли за день**.
- Платёжный тракт построен e2e (тест-магазин, 30-дневные планы), BILLING_ENABLED=false, боевой магазин ЮKassa ждёт ключей. Внешних платежей — 0 за всю историю.
- Живые юзеры поимённо: artem (3 аппа+2 БД), ggrk52 (2+1 БД), artempro2021; bruzas (4 аппа, 15 билдов/сутки) самоудалился 08-10 после 4 дней CrashLoop.
- Owner-решения (institutional memory, отсутствовавшая в обоих backfill): 07-21 — bot-identity отвергнута («лобовая Amvera»), fake-doors убиты, TG мёртв, MCP признан слабым, позиционирование = **«облако, которое запускает и само чинит твой апп»**, три потока: upload-без-git / скорость+надёжность / auto-fix; 07-30 — письма юзерам запрещены (канал = сам продукт); SEO «аналог X» near-dead, из pSEO выжил только `/hosting-telegram-bot`.

### Superseded assumptions (хранить как уроки, не как факты)
- ~~«Onreza = frontend-only»~~ — SUPERSEDED 2026-08-12: у них serverless (128MB/30s), KV, Postgres с ветвлением, SSR/Bun, PR previews, интеграции GitHub/GitVerse/SourceCraft. По их маркетингу; capability не проверена руками (→ NOW-3 в backlog).
- ~~«Dada выигрывает, потому что у Onreza нет backend»~~ — falsified как формулировка; вопрос сузился до H05.
- «Пользователи Onreza — потенциальные backend-клиенты Dada» — не факт, гипотеза при H05.
- Onreza pull-сигналы: TG пуст, форум — один энтузиаст с 0 ответов. Их «500+ деплоев» не верифицированы. Pull не виден ни у кого на RU-рынке.

---

## 2. Positioning tree (историческое, не схлопывать)

- **P1 — RU Backend PaaS**: «у меня backend, не хочу становиться DevOps». Ближе всех к реальным пользователям.
- **P2 — Full application cloud**: backend+DB+VM+storage в одном control plane. Риск: вырождается в feature checklist.
- **P3 — Bring Your VPS / migration**: «не мигрируй — подключи сервер, мы поймём workload и станем control plane над ним». Другой GTM-клин; структурно совместим с P4/P5.
- **P4 — Agent-operated cloud**: CLI, MCP, machine-readable operations, автофикс, Box.
- **P5 — Agent-native infrastructure**: примитивы, спроектированные для агентов. Company-level гипотеза, недоказана.

### Архитектуры бизнеса (открытый стратегический вопрос — НЕ решать эссе)
- A: RU-PaaS core, AI-native — фича.
- B: RU-PaaS — входной клин, agent-native — destination (PaaS = distribution для будущего продукта).
- C: control plane — ядро; managed apps / imported VPS / VM / agent / Box — адаптеры одного плана управления (→ H13).

Главный вопрос: **какой job даёт первый repeatable pull, и является ли единый control plane экономически полезным мостом от human-operated к agent-operated?** Решается данными из воронки, не документом.

---

## 3. Гипотезы

### H01 — Deployment pain exists (Problem)
Solo founders / малые backend-команды в РФ испытывают достаточную боль от ручного VPS-деплоя, чтобы выбрать managed-платформу.
За: внешние юзеры были, прод-workloads были, первые платежи были. Против: абсолютные числа малы; часть сайнапов не дошла до деплоя.
**Тест:** воронка после открытия дверей. **Статус:** `supported`, not validated at scale.

### H02 — First-deploy cliff (Activation) ★
Главное ограничение — между signup и успешным первым деплоем, а не отсутствие ещё одного примитива.
Улики: ≥3 zero-deploy аккаунта при работающей платформе.
**Статус:** `strongly-supported` + **воронка уже инструментирована automator'ом** (activation-funnel-v2.sql, Метрика+audit). Известные утечки в порядке величины: (1) лендинг→клик регистрации −83%; (2) verifyEmail −27% сайнапов (owner-политика, ждёт решения); (3) пост-деплойная тишина («TriggerBuild → тишина» как терминальное действие). Работа = чинить эти три + forensics zero-audit когорты (6 юзеров, стабильна 8 проходов). **Метрика цели:** median time-to-first-deploy ≤10 мин, first-deploy success ≥60%.
**Evidence 2026-08-14 (E141, sess-0814i):** один класс мёртвого сигнапа оказался неизмеримым by design — провал автосоздания дефолтного проекта не оставлял следа ни на бэкенде (три пути отказа `EnsureDefaultProject` отвечали 500/400 без аудита), ни на фронте (голый `catch{}` рисовал пустой обзор). За 30 дней ровно 1 сигнап без проекта (`f93095a3-...`, 2026-07-16) — причина потеряна навсегда. Отгружено `74f3b518`: аудит `outcome=failure` с машинным reason + экран с ретраем. Число появится не раньше 09-14; сама цифра «сколько мёртвых сигнапов = наш отказ, а не bounce» до сих пор НЕ измерена.
**Evidence 2026-08-13 (E97, sess-0813g/h):** за 12 чистых дней (ферма 08-08/09/10 вырезана) 57 человек видели лендинг → 3 открыли `/register` → **1 нажал CTA**. Утечка №1 внутри H02 — не форма входа Keycloak (39 юзеров, вторая по объёму) и не пост-деплойная тишина, а **лендинг→клик**: 1.8% против прежней оценки −83%. Замер сделан при намеренно закрытой регистрации, то есть верхняя часть воронки мерилась при нулевом знаменателе; с 2026-08-13 `SIGNUP_ENABLED=true` [live kubectl configmap `dada-cloud-console-config`], знаменатель вернулся — перемерить на живой реге.

**Evidence 2026-08-13 (E115, sess-0813i):** перемерили на открытой реге — знаменателя всё ещё нет. Путь незнакомца пройден живьём целиком (лендинг → `/register` → `passport.yandex.ru`, обрывов нет, мобильный CTA не спрятан). Metrika 110158915 за сутки 08-13: **9 уникальных посетителей на весь домен**, 4 на `/`, 1 на `/register` (проверяющий), 3 на `/login`, `/callback` после открытия двери — 0. Три независимых замера того же дня (атрибуция регистраций `72cfa3eb`, смертность перелёта на GitHub `4cab562b`, намерение deploy-CTA `86dc87f2`) — все с доставленным кодом и **нулевым знаменателем**. Гипотеза «хиро обещает GitHub → упор в `block-new-users`» ОПРОВЕРГНУТА: `github` IdP `enabled:false` целиком, экран недостижим [live kcadm]. H02 не двигается замерами, пока к двери не идут люди.

**Evidence 2026-08-14 (E133/E128, автоматор):** первая живая регистрация после реопена — `michaelharlam@yandex.ru`, 08-13 12:33Z. Атрибуция сработала (`signup_source='direct'`, 1/1=100%, `users` и `audit_events` совпали побайтово тем же стейтментом) — механизм H02-инструмента подтверждён на N=1, распределение каналов пока `unmeasured` (нужно ≥5-10). GitHub-перелёт за те же сутки — 0 внешних `StartGitAppInstall` (5 строк за 30д все внутренние: agent-sandbox зонд + владелец лично), значит стена GitHub как причина обрыва H02 остаётся `unmeasured`, не подтверждена и не опровергнута. Один внешний юзер дошёл до регистрации и — судя по нулю его в `StartGitAppInstall`/`git_repos` — до git-connect не дошёл вовсе; куда именно он делся между `/register` и деплоем, следующему циклу разбирать поимённо.

**Evidence 2026-08-15 (E155, sess-0815m):** снят подозреваемый №1 в терминальном обрыве H02. Версия «`TriggerBuild` пишет юзеру и нам `outcome=success`, а сборки не существует» — **KILL**. Причина была не в продукте, а в каскадном стирании истории сборок, и его уже закрыла миграция 116 (`ab7ec33c`, раскатана 08-13 14:16Z) до того, как подозрение сформулировали. Замер по всем действиям, несущим `build_id` (у `TriggerBuild` знаменатель после фикса всего 4 — на нём вердикт строить нельзя): доля стёртых до фикса → после — `BuildFinished` 22/35 (63%) → 1/28 (3.6%), `SendBuildNotification` 37/106 → 1/27, `ViewBuildLogs` 6/30 → 0/9. Все остаточные строки после фикса — один пограничный билд, летевший через 17 минут после раскатки; за двое суток после него стёртых нет. Поимённо: у `a.markov-buturskiy@fzlabs.ru` и `dkazakova1810@gmail.com` `CreateApp` реально приземлил образ (76с и 109с), аппы жили неделями и снесены самими владельцами — продукт им не врал, врал наш замер. Терминальная точка H02 после этого вычета и вычета фермы 08-08 (19 из 39 голых `SessionStart` — боты) смещается на **пустой экран**: `michaelharlam@yandex.ru` — 4 визита за двое суток без единого продуктового действия; `cryocrm@gmail.com` — дважды погасил промокод и оба раза ушёл из пустого списка приложений. Оба возвращались, то есть интерес был. Метод: «нет строки в таблице» не улика отсутствия события, пока не прочитан `confdeltype` — живая схема (`pg_constraint`), а не текст исходной миграции.

**Evidence 2026-08-15 (E145, sess-0815d/sess-0815j, platform-truth):** не про воронку юзера — про инфру под ней (`db-tier` реконсайлер). Живой замер: задвоение `SetDatabaseTier` у баз с дублирующимся именем в двух окружениях (`jira-app`/`nexus`, 2 строки вместо 1 в одном тике) ПОДТВЕРЖДЕНО. «Вечный почасовой перезапуск» для баз с замороженным снапшотом — НЕ подтверждён (6 из 7 чистых тиков дали `retiered=0`), и дальше неизмерим этой формулировкой: коммит `78e7021b` дедупит по `Committed` и глушит ровно тот сигнал. Закрыто статусом `partial`, не killed/measured. Не влияет на activation-цифры H02 напрямую, но фиксирует класс отказа в том же реконсайлере, что и E143.

**Evidence 2026-08-14 (E142, sess-0814j):** класс «канонический `ssl: true` ложится на проверке hostname» взведён на три четверти, но НЕ закрыт. Отгружены все три недостающих конца: строфа `hostAliases` в общем workload-чарте (`dada-argo@develop` `8b7043f3`), заполнение `PgRouterHostAliasIP` дефолтом из env в рендерере gitops-agent (`e9a126fa`, намеренно мимо `dbwatcher.go` с чужим WIP), проброс `PG_ROUTER_CLUSTER_IP=10.96.139.238` (`argo-infra@console-migration` `87f82c4d`, подтверждён мной в живом деплойменте). Заземление отмело два более дешёвых пути: CoreDNS живёт в `beget-coredns`, Application без `spec.source` и с меткой `toleration.beget.com/ignore: true` — правка эскалируется владельцу/Beget; зона `pv.dada-tuda.ru.` в PowerDNS `kind: Native`, публично делегирована с wildcard `*.pv → 155.212.223.198`, так что указать `db.pv` на ClusterIP значит сломать всех внешних клиентов. Флаг `MANAGED_DB_TLS_DSN_ENABLED` остаётся выключенным: прод-образ gitops-agent на момент записи `b84fa872`, кода в проде нет, hostAliases ни в одном поде нет. Статус класса — по-прежнему ОТКРЫТ для следующего юзера с каноничным сниппетом; вердикт по гипотезе не двигаю.
**Evidence 2026-08-15 (E144, sess-0815a, [live psql + live http]):** впервые измерена последняя миля по ВСЕЙ базе, а не по выборке. Знаменатель — 11 приложений с терминально успешным последним билдом и публичным hostname (из 18 пар вычтены 4 с committed `DeleteApp` и одно без hostname). Наши 4/4 живы; **у внешних юзеров 7 в знаменателе и 3 мертвы — 43%**: `fanvk` 502 (билд накануне, 08-14 07:41Z), `fonbet-value` 503, `megafactory` 404. Неизмеренных (`000`) ноль. Вывод: `builds.status='success'` — не сигнал здоровья, и до этого цикла доля живых адресов не измерялась ничем. Отгружено в тот же цикл: проба последней мили в gitops-agent (`2c340337`), позиция `live_urls` в `/api/v1/admin/overview` (`74f823d0`), карточка панели (`994939a0`); включение на проде (`LIVENESS_PROBE_URL` + rollout) — незакрытый долг E144.
**Evidence 2026-08-15 (E138, sess-0815a, [live psql/kubectl]):** `bad_connection_string` вердикт так и не сработал ни разу (0 строк) — не провал доставки (образ `c423cd4d`≤прод подтверждён), а структурный нулевой знаменатель: единственный кандидат-кластера (`megafactory`) прошёл стадию «нет схемы» до того, как классификатор его увидел, и уже стоит на `ssl_not_supported`; сейчас апп починен вручную (Running, 0 рестартов), но не кликом баннера. Класс остаётся непроверенным по существу, нужен новый живой кандидат.
**Evidence 2026-08-15 (E143, sess-0815a, [live psql/kubectl]):** доставка `23cbbe1b` подтверждена (backend+gitops-agent оба на этом теге), но раскатка свежая (~7-8 мин на момент замера) и все 4 baseline-опа (`SetDatabaseTier`, включая `40ca155c`/fonbet-db) всё ещё `Processing`, `metadata.noop='true'` = 0 строк — самолечение, предсказанное коммитом, физически не успело пройти ни одного цикла `ClaimPending` (~30 мин) под новым бинарём. Не fail, рано мерить — перемерить в течение суток.
**Evidence 2026-08-15 (E143 закрытие, sess-0815m, [live psql]):** финальный замер довёл интерим предыдущей строки до конца. Все 4 baseline-операции `SetDatabaseTier` получили терминальный статус САМИ, без единой ручной правки в БД, на первом же клейме нового бинаря `23cbbe1b`: `keycloak` 00:29:04Z, `mlflow-v2` 00:37:58Z, `fonbet-db` (внешний юзер `artemmendeleev@gmail.com`, висела ~10.5 ч) 00:41:07Z, `fin-core` 00:41:07Z. `count(operations WHERE status IN ('Processing','Reconciling') AND updated_at < now()-30min)` = 0 по всей платформе. У всех четырёх строка `audit_events` с `outcome=success`, `metadata->>'noop'='true'` и точной причиной; `payload.tier` совпал с CR — менять действительно было нечего, класс отказа был именно в незакрытии строки операции. `status: closed, result SUCCESS`. Порождает E145: noop-терминация обнажила отдельный класс отказа в том же реконсайлере.
**Evidence 2026-08-14, повторный цикл (E142 recheck, [live kubectl, ns `argocd-prod` не `platform`]):** гейт 1 закрылся сам — прод-образ `dada-cloud-console-gitops-agent`/`-backend` теперь `712bd424` (HEAD, выше `e9a126fa`), `PG_ROUTER_CLUSTER_IP=10.96.139.238` живой в env. Но гейт 2 не сдвинулся: `kubectl get pods -A -o json` — 0 из ВСЕХ подов кластера несут `spec.hostAliases`, включая `megafactory-deploy` (ServiceDatabaseV2, поды 20ч, передеплоены уже ПОСЛЕ того как код стал живым). Код не гейтит рендер hostAliases флагом — по коду ожидалось непустое поле у любого свежерендеренного аппа. Корень расхождения не изолирован (ArgoCD Application живёт в mgmt-кластере `e7b608-client-super-admin@e7b608-client`, не проверен в этом цикле). Класс остаётся ОТКРЫТ, но причина сменилась: раньше «кода нет в проде», теперь «код в проде, эффекта на живом редеплое нет» — конкретнее, но не решено.
**Evidence 2026-08-15 (E144 закрытие, sess-0815c, [live kubectl + live API + прямой curl]):** долг включения из E144 (08-15a) погашен и проверен независимо. `backend`+`gitops-agent` на `86044ec6`; `GET /api/v1/admin/overview` → `live_urls`={"checked":17,"ok":11,"dead":6,"stale":0} в 02:18:55Z, `dead_apps` называет megafactory (404), fonbet-value (503), fanvk (502); прямой `curl -w %{http_code}` по точным hostname из ответа дал те же три кода — проба и прямая проверка сошлись 3 из 3. Последняя миля теперь измеряется честно (закрывает и находку E146 про 308-редирект — тот баг больше не воспроизводится на этом образе), 6 из 17 внешних+внутренних аппов (35%) реально мертвы при формально успешном билде. `builds.status='success'` окончательно списан как индикатор здоровья; следующий шаг — снижать `dead`, не перепроверять инструмент.
**Evidence 2026-08-15 (E145 закрытие, sess-0815d, [live kubectl + live psql]):** ещё один класс тихой платформенной порчи под `builds.status='success'` — `db-tier` реконсайлер решает «надо менять» по замороженному `resource_snapshots.summary_json.tier`, а не по git; на живом чистом тике `02:36:31Z` (7 чистых тиков в окне, порог ≥3 выполнен) базы с именем, заявленным двумя окружениями (`jira-app`, `nexus`), задвоили `SetDatabaseTier` (по 2 операции за один тик) — механизм подтверждён живым событием, не только чтением кода; `status` ИСПРАВЛЕН на `partial` (sess-0815d, разбор той же таблицы): подтверждено ровно ЗАДВОЕНИЕ по `envs=2`, а не «вечный почасовой цикл» — всплеск `02:36:31Z` накрыл ВСЕ 11 баз (включая восемь с `envs=1`, однократный догон после расчистки очереди), а 6 чистых тиков из 7 дали кандидатам ноль, что говорит ПРОТИВ вечности. Пред-регистрированный критерий требовал «~1 в час И у остальных ноль» — обе половины провалены. Чинить: тир из git + развести дублирующиеся имена по окружениям.

**Evidence 2026-08-16 (sess-0816a, `8ef4b802`, [live kubectl secrets + MCP `getDatabaseCredentials`]):** на пути «база → рабочий апп» найден и закрыт разрыв между тем, что консоль ПОКАЗЫВАЕТ, и тем, чем апп РЕАЛЬНО подключается. После включения `MANAGED_DB_TLS_DSN_ENABLED` страница кредов отдавала TLS-строку, а в секретах обоих живых юзерских аппов с базой (2 из 2, `fanvk` и `megafactory`) лежала замороженная до-флаговая строка вообще без `sslmode`. Причина — сторож `seedDatabaseDSNIfAbsent` решал по ФАКТУ наличия `DATABASE_URL`, поэтому платформа никогда не обновляла собственную выданную строку. Форма дефекта важнее числа: это диагноз без рычага — канонический `ssl: true` бьётся о хост без TLS, а починить юзеру нечем, потому что значение писал не он. Знаменатель мал (2 аппа), поэтому это не замер конверсии, а снятый структурный блокер на участке, где H02 теряет людей молча.

**Evidence 2026-08-19 (E158, sess-automator, [live psql]):** инструмент для замера пустого экрана доставлен и работает — `data-ux` маркеры `empty-apps-cta-*` пишутся в `ux_events` на реальных кликах (10× `empty-apps-cta-upload`, 2× `empty-apps-cta-git`, 2 distinct юзера не из farm-волны). Знаменатель `ViewApps{empty:true}` за то же окно (08-15..08-19) = 70 просмотров. Продуктовый сигнал слабый (2 кликера на 70 просмотров), но ненулевой — терминальная точка «пустой экран» из E155-эвиденса больше не measurement-слепа, копить выборку до 8-10 кликеров прежде чем судить про конкретные CTA.

### H03 — RU sovereignty wedge (Value/moat)
Для части RU-разработчиков локальная оплата и независимость от foreign SaaS — причина выбора платформы.
**Тест:** attribution: источник + 1 вопрос при сайнапе («почему мы?») + упоминание в интервью первых платящих. **Kill:** из 20 разговоров РФ-фактор ни разу не решающий. **Статус:** `plausible`, needs attribution.

### H04 — Full-stack breadth wins
Ширина (runtime+DB+VM+storage) существенно повышает willingness to choose против узких платформ.
Раньше считалась фактом; после Onreza — нет.
**Тест:** win/loss упоминания ширины как решающего фактора. **Статус:** `untested`.

### H05 — Long-running workload gap
Существует значимый RU-сегмент, которому нужны arbitrary long-running processes / Docker / Compose, хуже обслуживаемые serverless-центричными платформами.
**Тест, шаг 1 (capability):** матрица по Onreza/Amvera/Timeweb руками, не по лендингам: Docker-образ, python/Go long-running, worker, cron, websocket, Compose, DB, cache, sleep/cold-start, квоты, цены, GitVerse/SourceCraft, previews. **Шаг 2 (demand):** конверсия/retention сегмента (боты/воркеры vs сайты).
**Owner-решение 07-21 (constraint):** bot-hosting как ИДЕНТИЧНОСТЬ отвергнута («загон в нишу, лобовая Amvera»). Боты — дверь (единственный выживший pSEO — `/hosting-telegram-bot`), не позиционирование. Бывшая H-боты сведена к сегмент-замеру внутри H05, без права переписывать hero под ботов.
**Evidence 2026-08-15 (шаг 1, первый замер за всё время):** `docs/product/h05-capability-matrix.md` — 12 критериев × 5 площадок (Amvera, Timeweb Cloud Apps, Onreza, Beget, deploy-f). Grounded 29 клеток (20 `yes`, 5 `no`, 4 частично), 31 `unknown` (52%). Вывод: ниша «долгоживущий процесс + Postgres рядом» уже занята Amvera и Timeweb — как одиночный клин она мертва; отсутствует она только у Onreza. Живой остаток гипотезы — КОМБИНАЦИЯ compose + managed Postgres + GitVerse/SourceCraft + previews, ни у кого не собранная целиком. Шаг 2 (demand) не тронут: это по-прежнему capability-замер, а не спрос.
**Исходы:** gap есть → клин «долгоживущие процессы любого стека + Postgres рядом, за рубли»; gap нет → дифференциация = VM/BYO + агентский слой. **Статус:** `partial` — шаг 1 сделан, шаг 2 (demand) не начат.

### H06 — Existing-infra migration wedge (BYVPS) — REOPENED
Для владельца существующего VPS/Compose барьер входа существенно ниже при «подключи сервер, ничего не мигрируй» (discover → import → review → commit → deploy), чем при «пересоздай app у нас».
Построено ~60–70% (F4). Заброшена без customer validation — единственная гипотеза с job + ICP + отличимым оффером + sunk cost одновременно.
**Тест (дешёвый, acquisition-эксперимент):** два оффера одному трафику — «задеплой из GitHub» vs «подключи свой VPS, импортируем Compose» — сравнить CTR/сайнап/активацию. Плюс вопрос в zero-deploy forensics: «у тебя уже был VPS?»
**Kill:** оффер BYVPS стабильно хуже greenfield на том же трафике. **Статус:** `untested, partially built`.

### H07 — GitHub dependency is strategic risk — STRATEGIC CALL (Alex, 2026-08-12)
Не customer-гипотеза. РФ-продукт не должен иметь GitHub единственной SCM-зависимостью (сценарий бана как в Китае). Следствие: честные OAuth GitVerse + SourceCraft — committed backlog, generic git — дополнение, не замена.
**Учебный чекпойнт (не гейт):** замерить usage интеграций через 60 дней после релиза — для калибровки будущих strategic calls, не для отмены этого. **Статус:** `strategic-call`.

### H08 — Agent becomes primary operator (Market/Behaviour)
Значимая доля workflows перейдёт от «человек деплоит через UI» к «coding agent деплоит, наблюдает, чинит».
Prerequisite всей agent-native ветки. Internal dogfooding не считается.
**Ранние индикаторы:** агентные вызовы CLI/MCP внешними юзерами; доля agent-led деплоев. **Статус:** `bet`, unvalidated customer demand.

**Evidence 2026-08-16 (E153, sess-0816c) — цепочка «крашлуп → причина → рычаг → живой апп» впервые наблюдалась целиком [live, `agent-sandbox`]:** архив с падающим контейнером → сборка `success` за 66с → под в `CrashLoopBackOff` через 3м32с после загрузки; платформа САМА назвала причину (`cause_kind=missing_env_var`, `cause_line=PROBE_API_TOKEN`) через ≈5 минут; нажатие рычага (`PUT .../env/{key}`, ставит операцию `DeployImageVersion`) подняло апп за 2м00с. Диагноз и рычаг существуют и смыкаются — это ответ на «диагноз без рычага оставляет юзера лежать».
**Тем же прогоном найдена дыра:** диагноз не липкий — у флапающего пода `reason` скачет `CrashLoopBackOff` ⇄ `Error`, тик читает лог только что перезапущенного контейнера, получает пусто и пишет NULL поверх уже найденной причины (`backend/internal/api/app_health_watcher.go:669,592-600`). Наблюдалось на пробнике: 10:46:42 `cause_kind=missing_env_var` → 10:52:50 `reason=Error, cause_kind=NULL` при том же самом падении. Строки живых юзеров в состоянии `reason=Error` с пустой причиной существуют (`bruzas-85-prod/sevarateambot`, `sevarabot-prod/sevarateambot`), но их `last_seen_at` = эпоха, то есть что у них причина именно СТЁРТА, а не не найдена ни разу, — не доказано.

**Evidence 2026-08-16 (E153 fanvk worker banner, sess-0816) — синхронизация закрытого замера [live psql `resource_snapshots`]:** воркер без HTTP (`fanvk`, `worker=true, http_status=502`) стабильно показывает предикату причину, а не тишину — живой пересчёт сегодня подтвердил тот же снимок (`http_checked_at=2026-08-16T11:09:56Z`), второй полюс без ложных срабатываний (второй `worker=true` апп, `sevarateambot`, не проходит пробу и потому не триггерит баннер). Один пункт в очереди «диагноз без рычага» закрыт как SUCCESS по данным.

**Evidence 2026-08-16 (E160, sess-0816e) — цепочка рвётся во ВРЕМЕНИ, а не в логике [live psql `app_health_alerts` + kubectl]:** у `bruzas.85` апп `sevarateambot` пролежал 18 часов (215 рестартов) при полностью исправной диагностике. Письмо ушло 08-15 21:08:40Z, когда `cause`/`cause_line`/`cause_kind` были пусты — юзер прочитал «контейнер упал, exit=1» без причины; сама причина (`missing_env_var` / `TELEGRAM_API_TOKEN`) легла в строку только 08-16 15:02Z, после раскатки фикса липкости. Кулдаун 24ч к этому моменту уже был потрачен. То есть «платформа знает причину» и «юзер знает причину» — две РАЗНЫЕ метрики, и вторая может отставать на сутки при зелёной первой. Отгружено `e4e471f9`: переход `cause_kind` пусто→непусто открывает ровно один догоняющий слот. Замер доставки письма — E160, `measure_after` 2026-08-17.

**Evidence 2026-08-16 (E161, sess-0816f) — платформа может не знать имени причины, которую сама же и создала [live psql `app_health_alerts` + `audit_events` + kubectl logs]:** внешний юзер `artemmendeleev@gmail.com`, апп `fonbet-value` — 22.5ч в `CrashLoopBackOff`, 37 рестартов, URL 503. На его стороне не менялось ничего (образ по digest, ReplicaSet 4d3h). Положило апп НАШЕ принуждение квоты: `SetDatabaseEnforcement` 08-15 20:41:36Z перевёл роль `svc-fonbet-db` в `default_transaction_read_only=on` (10.84 GiB против 1 GiB free), после чего каждая запись падает с `psycopg.errors.ReadOnlySqlTransaction`. Классификатор такого случая не знал: `cause_kind` пуст, письмо ушло через 18ч56м без причины, рычагов в баннере ноль — а поскольку отказ приходит внутри `Traceback (most recent call last)`, следующая по порядку таблица назвала бы виноватым КОД ЮЗЕРА про поломку, которую устроили мы. Отгружено `d4164c30`: `cause_kind=db_read_only`, проверяемый до языковых таблиц, нейтральная стилистика баннера и два рычага (базы, тариф). Замер — E161, `measure_after` 2026-08-17. Урок для H08 сверх E160: у цепочки «упал → диагноз → рычаг» есть третий разрыв — СЛОВАРЬ; молчание платформы и её ложь про чужой код растут из одного корня, а диагноз без права на действие (лимит места юзер себе не поднимет) апп всё равно не поднимает.

**Evidence 2026-08-19 (E164, sess-0819c) — платформа умела назвать причину и сама же её стирала неудачным окном лога [live psql `app_health_alerts` + kubectl logs, отгружено `e6a44a11`]:** сигнап того же дня `lifecoachrussia@yandex.ru`, апп `gulyaev-ai-core`, 25 рестартов на НЕИЗМЕННОМ `ModuleNotFoundError: No module named 'app'`; вердикт `app_code` был вычислен и письмо ушло, а живая колонка `cause_kind` периодически пуста → `crashCauseKey()` возвращает null и баннер теряет строку причины. Корень не в классификаторе: `causeRefreshOutcome` считал НЕПУСТОЙ лог без совпадения отрицательной уликой, хотя `tailLog` читает последние 20 строк одного инстанса контейнера и диагностическая строка гуляет в окно и из окна между рестартами. Теперь стереть вердикт может только другой непустой вердикт. Урок для H08 поверх E160/E161: четвёртый разрыв цепочки «упал → диагноз → рычаг» — УДЕРЖАНИЕ диагноза во времени; отсутствие совпадения это отсутствие улики, а не улика об отсутствии. Побочно замерено: у `fonbet-value` (artemmendeleev) причина уже была названа и стёрта — баннер немой прямо сейчас, восстановить нечем, `cause` для прошлого вердикта в базе не хранится.

**Evidence 2026-08-19 (E120, sess-0812d→замер 2026-08-19) — новый тул диагностики (`getAppHealth`) неделю простоял неиспользованным даже в целевом сценарии [live psql `agent_chat_messages`]:** 0 из 18 вызовов тулов за неделю после раската `29283cec` — `getAppHealth`; модель на живом кейсе (юзер про `megafactory` в `CrashLoop`, 08-13) 10 раз подряд позвала старый VM-only `getAppState` вместо нового k8s-тула. Framing спас диалог от dead-end (получила «not applicable to this environment», не повторила фрейминг юзеру, ушла в логи, нашла реальную причину) — но БЕЗ нового тула, старым путём. Третий тул подряд (после `getPlatformStatus`/E124) с рабочей логикой (юнит-тесты зелёные) и нулевой обнаружимостью моделью. Урок для H08: "дать тулу" ≠ "агент им воспользуется" — узкое место agent-native ветки не в наличии read-инструментов у бэка, а в том, читает ли модель их swagger-описание достаточно, чтобы выбрать нужный.

**Evidence 2026-08-19 (E121, sess-0812e→замер 2026-08-19) — заявленный сценарий (HTTP 200 + `live_error`) не встретился физически, знаменатель честный ноль [live psql `agent_chat_messages`]:** `content ilike '%live_error%'` = 0 за неделю — на текущем трафике никто не попал в путь compose-апп+апстрим-отказ. Побочно, на СОСЕДНЕМ (более старом) механизме `toolFailurePreamble` (non-2xx) — 0 нарушений на n=1 живой сессии: framed-отказ `getAppState` на k8s-аппе не был пересказан юзеру как диагноз его кода. Слабый позитивный сигнал, не подтверждает и не опровергает саму E121-ветку — измерение продлено, не наступило целевое событие.

**Evidence 2026-08-19 (E165, sess-automator) — вердикт узнавал аварию на одном языке и не узнавал на другом [live psql `app_health_alerts` + kubectl logs, отгружено `83af9559`]:** `fonbet-value` (`artemmendeleev@gmail.com`, самый ценный активный юзер) — 5 суток, 39 рестартов, `cause_kind` NULL, баннер пуст, панель читала аварию как код юзера. Живые логи пода: `OSError: [Errno 28] No space left on device: '/data/raw_data/...'`, том Longhorn 20Gi на 100%. Сигнатура ENOSPC в `platformCrashSignatures` БЫЛА, но лежала строчными буквами и матчилась case-sensitive: `strerror(ENOSPC)` и CPython печатают «No space left on device» с большой N, `os` в Go — с маленькой. Существовавший тест проходил, потому что кормил классификатор его же строчной строкой — страж, неспособный упасть на реальности. Пофикшено `containsFold` только для таблицы ОС-строк (языковые таблицы — имена типов исключений, регистр значим), применённым на ОБОИХ местах: вердикт и строка-улика теперь выбираются одним правилом. **Корректирует хвост E164:** у `fonbet-value` причина не «была названа и стёрта» — на 08-19 причина другая (ENOSPC, не `db_read_only`) и не называлась НИ РАЗУ. Урок для H08 поверх E160/E161/E164: пятый разрыв цепочки — СОВПАДЕНИЕ; словарь может знать имя причины и всё равно её не узнать, а собственный тест этого не покажет, если улику для теста пишет тот же код, что её матчит. Рычага для `platform_storage` по-прежнему нет (backlog 0371): юзер теперь узнает причину, но поднять том сам не может.

### H09 — Current clouds are bad agent substrates
Проблема agent-разработчиков не в отсутствии MCP, а в несоответствии interaction model облаков автономному lifecycle агента. (Замена слогану «designed for AI, not adapted» — слоган не является problem statement.)
**Тест:** problem-интервью с agent-heavy разработчиками: конкретные обломы их агентов об облако. **Статус:** `untested`.

### H10 — Box solves a distinct job
Разработчики будут использовать/платить за crystallizable execution environment, потому что агенту нужно собственное тело/состояние.
**Статус:** `untested` (не falsified). Externalization — по триггеру (см. backlog LATER). Известные блокеры: изоляция, abuse (урок фармеров), crystallize e2e не доказан (ADR-019).

### H11 — AI-generated apps create deployment demand

**Evidence 2026-08-13 (E116, sess-0813i):** тракт upload-без-git доказан живьём на проде, не сборкой: статика (Dockerfile) **222с** от загрузки до внешнего HTTP 200 на 6/6 узлах, Flask без Dockerfile (detect по `requirements.txt`) **205с**, 6/6. Билд 70-80с, деплой-фаза после билда 135-145с — самый дорогой отрезок. Детектор реальный (docker/next/vite/react/node/fastapi/flask/django/streamlit/python/maven/gradle/go), лимит 100MB. Разрыв обещания: «папка» и «одиночный файл» в коде отсутствуют — принимается только `.zip/.tar.gz/.tgz` (`frontend/components/deploy/upload-deploy.tsx:9-15,65-69`), то есть экспорт Lovable/Bolt/v0 распакованной папкой юзер обязан зазиповать сам.
Пользователи Lovable/Bolt/Lork после первой версии упираются в production backend/state/operations, которую закрывает Dada.
**Апгрейд из automator (07-21):** поток «upload-без-git» (папка/zip → auto-detect → deploy) строится ровно под этот job («экспорт Lovable/Bolt = zip, git не нужен») и является генерализацией единственного измеренно работающего активационного механизма (template-escape: 3/6 незнакомцев, git/OAuth-стена убивает ~100%). Плюс retention-раскол: возвращаются только деплоившие СВОЙ код — вайбкодер со своим сгенерённым кодом = правильный профиль.
**Тест:** конверсия/retention когорты upload-без-git после релиза потока 1. **Статус:** `plausible` (поднята с untested), первое косвенное evidence есть.
**Evidence 2026-08-13 (sess-0813f) — прямое, впервые:**
1. *Путь физически работает целиком* [live, прогон в `agent-sandbox`]: zip с `package.json` без Dockerfile → детект `vite:5173` → сборка 96с → апп `Ready` 2/2 → внешняя проверка с 6 нод отдала 200 с ожидаемым маркером. **zip → живой URL = ~3м30с, ни одного обрыва.**
2. *Путь используется и конвертит реальными людьми* [live psql `audit_events`]: `UploadSourceArchive` = 22 строки / 4 актора / 22 success / 0 failure. Из них ДВА внешних юзера (`artempro2021@bk.ru` → `fanvk`, `good.win2283@gmail.com` → `oxygen`) довели архив до живого `Ready`-аппа; `fanvk` — 14 успешных сборок 07-29→08-12. Медиана upload→первый успешный билд ≈ 2м43с.
3. *Контраст с git-дверью* [live psql]: конверсия `ViewApps → CreateApp/ConnectGitRepo` = 2.6% (4/156), четыре прохода подряд; `ViewApps` терминален у 33% юзеров.
Знаменатель мал (2 внешних юзера), поэтому статус остаётся `plausible`, а не `supported`. Но соотношение «2/2 доходят через upload против ~100% смертности на OAuth-стене» — первое прямое, а не косвенное, evidence. Продуктовое следствие уже отгружено: дефолт чузера деплоя для проекта без git-аппов переведён на upload (E93).
**Evidence 2026-08-19 (E117, sess-automator, [live end-to-end, `agent-sandbox`]):** реальный buildable Vite+React проект (настоящие зависимости, не заглушка) прошёл детект+билд до `success` живьём — расширяет п.1 выше с 4-файловой фикстуры на настоящий экспорт. Но live-URL не достигнут: апп остался `NotDeployed`, операция деплоя `Failed` с `error_code=PROCESSING_ERROR`, `"git push: fetch before commit: authentication required"` — новый, ранее неизвестный структурный баг gitops-agent (git-push auth в `agent-sandbox`), не специфичный для Vite и не relevant к качеству детекта H11. Detect/build-часть тезиса H11 усилена; end-to-end до живого URL на этом проекте — заблокирован отдельным инфра-багом, не потоком upload-без-git как таковым.

### H12 — AI Gateway as wedge
Архитектура развита (LiteLLM, BYOK, metering), но evidence, что это GTM-клин — нет. Не смешивать с основным позиционированием.
**Статус:** `bet`, demand evidence unknown. Отдельное решение о бюджете.

### H13 — Control-plane continuity (unifying candidate)
Ценность Dada — единый application control plane: сначала управляет Dada-managed workload или существующим VPS, затем становится естественным API для автономных агентов. Объясняет GitOps/VM/BYVPS/Compose/CLI/MCP/Box.
**Предупреждение критика: гипотеза, объясняющая все прошлые решения, неотличима от рационализации, пока не даёт различающих предсказаний.** Фальсифицируемые следствия:
1. imported-VPS юзеры со временем конвертятся в managed-ресурсы (funnel: import → attach managed DB/domain);
2. один control plane снижает churn против связки точечных инструментов;
3. агенты/CLI предпочитают единый API поверх обоих runtime (agent-led операции на imported и managed через одну поверхность).
Ни одно пока не наблюдалось. **Не hero. Не market truth.** **Статус:** `untested`, кандидат в ядро при сигнале из H06+H08.

---

## 4. Killed / superseded гипотезы

- **H-комьюнити-прослушка Onreza** — killed 2026-08-12: живого комьюнити нет. Джоба отменена.
- **Fake doors новые** — отклонено: ~20 дверей уже есть, проблема в разметке.
- **Паника «проиграть Onreza»** — снята: pull не виден ни у кого; проигрыш = оба не нашли спрос.
- **«RU-PaaS vs AI-native» как бинарный выбор** — superseded: преждевременная дихотомия; см. архитектуры A/B/C и H13.
- **Habr-корпблог как канал** — killed по цене (~268k₽/3мес); триггер возврата: MRR ≥ инфра-счёта.

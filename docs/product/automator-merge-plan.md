# План слияния: automator × продуктовый реестр гипотез

2026-08-12. Вердикт по итогам чтения `~/.claude/scheduled-tasks/auotmator` (SKILL.md + state, срез в `.automator-snapshot/`).

## Вердикт одной строкой

Automator — лучшая evidence-машина компании и худший стратег: безупречная дисциплина исполнения, ноль слоя «зачем». Слияние = не «добавить ему ума», а соединить два контура: `docs/product/hypotheses.md` становится стратегическим реестром, его `experiments.md` — тактическим, с обязательной привязкой каждого эксперимента к гипотезе.

## Что у него сильнее нас (перенять, не трогать)

- Grounding M0–M5, анти-як, «замыкание измерений — первый долг», разбор аудита как путей юзеров, запрет ложного завершения. Это лучше любых наших гейтов — наши «3 имени» рядом выглядят самодельно.
- Он уже инструментировал воронку (activation-funnel-v2.sql, Метрика+audit стык) — пункт «Instrument funnel» из backlog.md v2 в основном **сделан**; осталось чинить найденные утечки.
- Его три потока (upload-без-git / скорость+надёжность / auto-fix) — самый заземлённый ответ на «какой job даёт pull»: upload-без-git = генерализация единственного измеренно работающего активационного механизма (template-escape 3/6 незнакомцев); zip от Lovable/Bolt = живая версия H11.

## Что у него отсутствует (наш вклад)

1. **Слой гипотез.** E1–E122 — микро (CTA, панели, сироты). Ни один не отвечает на H01–H13. При нулевом знаменателе он оптимизирует измеримое → 55% последних циклов ушло в достоверность собственных панелей. Это рационально локально и бесплодно глобально.
2. **Критика собственной стратегии.** STRATEGY.md заморожен owner'ом 07-21 — правильно, но evidence с тех пор (bruzas самоудалился, боевой магазин на подходе, Onreza-разведка) никем не сопоставляется со STRATEGY. Его B3-само-аудит проверяет «як vs узкое место», но не «двигаем ли мы хоть одну гипотезу».
3. **Гигиена состояния** деградировала: см. одноразовую задачу ниже.

## Найденные противоречия (двусторонние — чинить в обе стороны)

**Наши файлы нарушают owner-решения, записанные у него:**
- H05/бывшая H-боты: owner 07-21 отверг bot-hosting как идентичность («загон в нишу, лобовая Amvera»). Боты = одна из дверей, не позиционирование. → hypotheses.md исправлен.
- Тактика «касание №2 churned-юзеру»: POLICY 07-30 «письма отменены» — для агентов. Owner сам писать может (и написал 08-11); automator'у не поручать никогда.
- «SEO-станок — доказанный канал» — завышено: класс «аналог X» измеренно near-dead, pSEO hosting-* убит **кроме `/hosting-telegram-bot`** (единственный выживший — снова боты как дверь). SEO = компаунд, не станок.
- Fake-door у owner'а убит 07-21 с формулировкой «тест словами без трафика = набалов» — мой ранний совет про fake-door был повторением уже похороненного (критик не читал institutional memory — та самая ошибка, против которой писался backfill).

**Его состояние противоречит само себе:**
- experiments.md открывается плашкой «PIVOT MODE ACTIVE… читать как справку» — дезавуирует активный файл, куда пишутся новые E1xx.
- cycle-log rolling-метрики врут втрое; digest от 08-06 описывает несуществующий план замеров; коллизии ID (E10, E36–E39, E84/E86, E103); три протухших лока.
- «MCP признан слабым» (07-21) vs шипнутый MCP на 41 тул и live-плагин — статус MCP как ставки нигде не решён явно. → вопрос owner'у: MCP = поддерживаем/замораживаем?

## Архитектура слияния (двухконтурная)

**Контур 1 — стратегический (мы + owner, по требованию):** `docs/product/hypotheses.md` + `docs/product/backlog.md`. Владелец статусов гипотез. Обновляется в сессиях с owner + автоматически evidence-строками из контура 2.

**Контур 2 — исполнительный (automator, hourly):** его state как есть. Изменения минимальны и точечны — он работает, крупная пересадка мозга сломает дисциплину.

**Интерфейс между контурами — три правила в его SKILL.md:**

### Патч SKILL.md (готовый текст, вставить после «Персистентное состояние»)

```
## Продуктовый реестр (контур гипотез)
- В начале цикла, вместе со state, читай `docs/product/hypotheses.md` (репо dada-cloud) —
  компактный реестр H01-H13. Он НЕ заменяет STRATEGY.md (при конфликте выигрывает STRATEGY
  + owner-actions), но каждый новый эксперимент в experiments.md ОБЯЗАН нести поле
  `hypothesis: H0X` либо тег `platform-truth` (достоверность панелей/инструментов).
- Закрыл эксперимент (scaled/killed/measured) с hypothesis-полем → допиши одну строку
  evidence в соответствующую гипотезу hypotheses.md: дата, эксперимент, число, вывод.
  Не меняй статус гипотезы сам — статусы двигает owner-контур.
- В B3-само-аудит добавь метрику: доля циклов за окно, двинувших хоть одну H-гипотезу
  (цель ≥50%). 6 циклов подряд чистого platform-truth → строка в owner-actions:
  «исполнение оторвалось от гипотез — нужен owner-контур».
- В B2-дайджест добавь секцию «Гипотезы»: какие H получили evidence за окно, какие
  статусы предлагаешь пересмотреть.
```

### Одноразовая гигиеническая задача для него (положить в его backlog как P1)

```
[ ] STATE-HYGIENE (одноразово): (1) все [x] из backlog.md → backlog-archive.md,
    цель <100КБ живого файла; (2) ротация cycle-log.md (старше 30д → архив),
    пересчитать rolling-метрики честно; (3) снять 3 протухших лока; (4) снять
    плашку PIVOT MODE с experiments.md, зафиксировать реестр ID (коллизии E10,
    E36-E39, E84/E86, E103 — переномеровать новыми ID, старые пометить collision);
    (5) выполнить просроченный B3-само-аудит (последний 07-25).
```

## Что слияние даёт немедленно (уже из его данных)

1. H02 (activation cliff) — `strongly-supported`, инструментирован; конкретные утечки: лендинг→клик регистрации −83%, verifyEmail −27%, «TriggerBuild → тишина» как терминал. Чинить эти три, «инструментировать воронку» из нашего NOW — закрыто.
2. H11 (AI-generated apps) — повышена: upload-без-git поток строится ровно под «экспорт Lovable/Bolt = zip»; retention-раскол (свой код = возврат) — первое настоящее evidence сегментации.
3. Гейт «ЗА ЧТО ПЛАТИТЬ» (его №3) = наш P0 «цена до деплоя»: квоты по живым данным + грандфазеринг (artem 3, ggrk52 2) + BILLING_ENABLED. Метрика — первый succeeded payment от чужого.
4. Открытые owner-блокеры из его owner-actions (свежее моего P0): ключи ЮKassa (срок сегодня), PROXY protocol на балансировщике, диски нод, verifyEmail-политика, решение по Директ ~25к₽.

## Не делать

- Не встраивать «стратегического критика» в hourly-цикл — часовой timebox и стратегия несовместимы; получим эссе по крону.
- Не переносить его state в docs/ и наоборот — контуры разные, интерфейс узкий (evidence-строки + дайджест).
- Не давать automator'у право менять статусы гипотез и STRATEGY.md.

## ⚠️ Безопасность снапшота

`.automator-snapshot/state/` содержит `.secrets`, токен-скрипты (`get-mcp-token.sh`, `dada-curl.sh`) и `vpn-bypass-proxy.py`. **Не коммитить**: добавить `.automator-snapshot/` в `.gitignore` немедленно, удалить после применения плана. Отдельно: `vpn-bypass-proxy.py` в state агента — проверить, зачем он там, на фоне нашей же AUP-позиции.

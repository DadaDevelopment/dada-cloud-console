# audit_events разбор — sess-0822b, 2026-08-21 22:xx UTC

Гейт: kubectl exec в pg-shard-0-postgresql-0 (ns `databases`), DSN из secret
`argocd-prod/dada-cloud-console-backend#DATABASE_URL`, все числа ниже измерены
живым SQL в момент запуска (`select now()` = 2026-08-21 22:09:59 UTC, max
`audit_events.created_at` = 22:06:05 — свежак, не протухший снапшот).

## 0. Что изменилось с прошлого цикла (sess-0822a)

Прошлый снапшот и этот совпадают почти 1:1 по users/audit — цикл сработал
почти сразу следом, реальных новых событий за это время 1 строка. Разбор ниже
переделан заново (не скопирован), и он **точнее прошлого** в одном месте:
`ViewApps` теперь имеет метаданные экрана (`apps`/`empty`/`healthy`/`unhealthy`/
`sources`) — это шипнуто между циклами (`backend/internal/api/apps.go:257`
`viewAppsAuditMetadata`, покрыто `apps_view_audit_test.go`). Это меняет вывод
№1 прошлого цикла: раньше "ViewApps — топ терминала" было безсодержательным
(просто имя экрана), теперь можно сказать **что юзер увидел в последний раз**.

## 1. Новые юзеры

7д: 3 — kkartov@yandex.ru (08-17 19:46), lifecoachrussia@yandex.ru (08-19
09:37), tarotreaderhimu@gmail.com (08-21 13:58).
30д: 30 новых users всего (без изменений).

### Полные цепочки (без изменений по сути, для справки)

**kkartov@yandex.ru** — 513 audit-строк. Создавал/удалял `instatic` ~5 раз.
172× `SeedDatabaseDSN failure` подряд каждые 10с (22:06:14–22:34:44 08-18) —
хвостовой `\n` в GITOPS_ENCRYPTION_KEY, пофикшено 08-19 (17db736d/d4af2faa),
юзер ушёл раньше фикса. Дальше ещё две failure-серии тем же актором в ту же
ночь: 31× `VerifyDomainAuthorization failure` (21:02–21:07) и 23×
`RevealEnvVar failure` (22:53–03:11) — то есть это была не одна, а как минимум
**три** отдельные failure-петли за одну сессию отчаяния, а не только уже
задокументированная. Терминал: `ViewApps` 08-19 19:26:37, **metadata =
`{"apps":0,"empty":true}`** — последний экран, который видел юзер, был пустым
списком приложений (он снёс `instatic` сам). Живого приложения нет.

**lifecoachrussia@yandex.ru** — репо gulyaev-ai-core, TriggerBuild x4 fail,
`TriggerAutofix` использован, затем `BuildFinished success` 08-19 11:26 и
`DeployImageVersion success` 08-20 07:16 (actor_type=system, юзер это
синхронно не видел). Терминал user-действия: `TriggerBuild success` 08-19
09:41 (последний `ViewApps` того же дня в 09:37 тоже `empty:true` — смотрел на
пустой список ДО того как билд взлетел, не после). **НЕ churn** — приложение
живо.

**tarotreaderhimu@gmail.com** — та же сигнатура: 2×TriggerBuild fail (npm
install), между ними CreateServiceDatabase+SeedDatabaseDSN (чинит не то), 3-й
TriggerBuild тоже fail. Тишина с 08-21 14:09:36, <12ч на момент замера — рано
считать финальным.

## 2. Кто не сделал ничего / провалы инструментирования

Zero-audit юзеров: 0 (SignUp всегда пишет строку, без изменений).

Мёртвые сигнапы (0 build, 0 repo, 0 chat) за 30д: 19 из 30, вся ботоволна
08-08 19:49–08-09 05:37 (`project_signup_farm_wave_pollutes_funnel.md`) +
внутренний a.meshkov@dada-tuda.ru. Без изменений.

Chat→audit разрыв (`AgentChatUserMessage`): с 08-07 (вайринг хука) — 50 chat
vs 37 audit = 26% теряется (то же число, что в прошлом цикле — разрыв не
растёт исторически). За последние 7д отдельно: 12 vs 12 = **0% потерь**, тоже
без изменений. `recordAuditAsync`-дыра (`project_audit_events_silently_drops_rows.md`)
молчит на текущей нагрузке третий цикл подряд — на грани того чтобы считать
это не регрессией, а фоновым уровнем шума эпохи ДО вайринга хука.

## 3. Граф пути (30д, actor_type=user, 39 акторов)

### Первое действие после SignUp
SessionStart 13 (бот-волна, кэшированный токен без реального действия),
UploadSourceArchive 3, DeployImageVersion 2, ViewProject 2, CreateProject 1,
AgentChatActionApproved 1, DeletePreviewEnv 1, BuildFinished 1, ConnectGitRepo
1, CreateApp 1. Без изменений по форме.

### Терминальное действие — уточнённый срез (реальные внешние, silence>48ч)
Исключены: бот-волна 08-08/09 (19), внутренние домены/тест-аккаунты
(`dada-tuda.ru`, `dada.local`, `keycloak.local` — 4 актора: a.meshkov,
michaelharlam@dada-tuda.ru, admin@dada.local, dada-e2e-test).

**6 внешних реальных, тишина >48ч:**
| актор | терминал | metadata последнего ViewApps/ViewProject | классификация |
|---|---|---|---|
| kkartov@yandex.ru | ViewApps | `empty:true, apps:0` | **churn** (снёс всё сам, не вернулся) |
| good.win2283@gmail.com | ViewApps | `healthy:1` (терминал 08-14 13:14) | **churn** (0 repo/0 build в проекте, ушёл при ЗДОРОВОМ экране — не "сломано", просто бросил) |
| cryocrm@gmail.com | ViewProject | `apps:0` (старый формат метадаты) | **churn** (0 repo/0 build) |
| sergeykozlov2006@gmail.com | ViewApps | `apps:1` (терминал ещё до шипа метадаты) | дормант-доволен (живое приложение с июня) |
| tech@xn--d1acaa3cs0b.xn--p1ai | CreateApp | — | дормант-доволен (живое с июня) |
| gunopice85@gmail.com | DeployImageVersion | — | дормант-доволен (живое с июня) |

Настоящий churn (0 repo, 0 build, реальный отвал): **kkartov, good.win2283,
cryocrm = 3 из 6**. Ровно как в прошлом цикле по составу, но теперь видно ЧТО
у каждого было на экране в последний раз — и это НЕ единообразно "сломано".

## 4. Новое: метадата ViewApps по ВСЕМ реальным (не-бот, не-внутренним) акторам, all-time

Взял последний `ViewApps` каждого внешнего актора (не только 30-дневное окно
новых юзеров, чтобы набрать выборку побольше — n=12, честно маленькая):

| финальный экран | акторов | доля |
|---|---|---|
| `empty:true` (пусто, сам всё снёс) | 5 (mytake, tarotreaderhimu, kkartov, lifecoachrussia, cryocrm) | 42% |
| `healthy:1, unhealthy:0` (всё ОК) | 4 (artempro2022, bruzas.85, artempro2021, good.win2283) | 33% |
| `unhealthy:1` (сломано) | 2 (artemmendeleev, michaelharlam@yandex.ru) | 17% |
| старый формат (до шипа метадаты) | 1 (sergeykozlov2006) | 8% |

**Из 2 явно "сломанных" экранов и 5 явно "пустых" — только 2 (kkartov,
lifecoachrussia) успели дожить до силентности >48ч и попасть в п.3; остальные
либо ещё свежие (<48ч), либо не входят в 30-дневное окно новых юзеров.**
Вывод предварительный на маленькой выборке, но направление ясное: причина
терминального `ViewApps` НЕ гомогенна. Гипотеза прошлого цикла ("юзер видит
провальный статус и не возвращается") объясняет максимум 17% случаев
(`unhealthy`). Больше народу (42%) в последний раз видел ПУСТОЙ список — то
есть сам снёс приложение как акт попытки починки/сдачи, а не молча ушёл от
ошибки. Ещё треть (33%) ушла со ЗДОРОВОГО экрана — это не продуктовый баг
вообще, это обычный отток/потеря интереса, не чинится UX-правкой апп-листа.

## 5. TriggerAutofix (счётчик успеха пункта 0457)

Всего 7 строк, 4 уникальных актора, за весь период 08-02→08-19:
`2dd1effb-…` 1× (08-02), `1dae45fe-…` 2× (08-11), `6758ab0b-…` 3× (08-17/18),
`d229ec6b-…`=lifecoachrussia 1× (08-19, единственный из окна 30д). **Ноль
строк с 08-19** — включая 08-21, дату приёмки 0457 по git-логу
(`fdd10290 Приёмка 0457: рычаг доказан тегом работающего пода`). Это ожидаемо
(<24ч с приёмки, у пользователей ещё не было повода нажать), НЕ тревога — но
следующий цикл должен увидеть хотя бы 1 новую строку, иначе рычаг
структурно недостижим (см. `project_shipped_lever_can_be_structurally_unreachable.md`
— уже был такой паттерн).

## 6. Повторяющиеся failure-петли (N одинаковых failure одним актором за окно)

Пересчитано с правильной колонкой (`outcome='failure'`, не текстовый ILIKE):

| актор | action | count | окно |
|---|---|---|---|
| kkartov | SeedDatabaseDSN | 172 | 28 мин (уже известно) |
| kkartov | VerifyDomainAuthorization | 31 | 5 мин (**новое** — не было в прошлом разборе) |
| kkartov | RevealEnvVar | 23 | 4ч18м (**новое**) |
| kkartov | SetEnvVar | 14 | 17 мин |
| **`00000000-…-000000000000` (system)** | **SetDatabaseTier** | **332** | 08-14→08-19, ещё 36 за последние 3 дня — **живой, не закрытый** |
| `00000000-…-000000000000` | AutoscaleApp | 58 | 08-01→08-21 (весь месяц, продолжается) |
| `00000000-…-000000000000` | SendBuildNotification | 38 | весь месяц |

**Новая находка, не user-facing, но реальная:** системный актор
`SetDatabaseTier` падает 332 раза с ошибкой `no ServiceDatabaseV2 "<app>"
anywhere under clusters/beget-prod/projects/<internal|platform|devtools>/…`
для внутренних приложений (`jira`, `nexus`, `dadagent`, `telemost-bot`,
`n8n`, `mlflow-v2`, `zerkalo` и др.) — это резолвер тира базы не находит
git-путь ресурса для ИНФРА-аппов (internal/platform/devtools проекты), и это
не разовый сбой, продолжается до сих пор (36 падений за последние 3 дня).
Это не влияет на внешних юзеров напрямую, но означает, что автоскейл тира
базы для собственной инфраструктуры Dada молчаливо не работает третью неделю
подряд. Кандидат на отдельный тикет (не 0457-related), не эскалирую как P0
т.к. внутренний, но фиксирую — раньше не было в разборе.

## 7. Сравнение с прошлым циклом

Прошлый: 4 из 7 живых отвалившихся кончают на `ViewApps` (57%).
Этот (по 30-дневному окну новых юзеров): 3 из 6 внешних реальных терминалов =
`ViewApps`/`ViewProject`-семейство (50%) — доля держится, состав акторов не
изменился (kkartov, good.win2283, cryocrm — те же три, что в прошлом цикле).
**Разница цикла — не в числах, а в разрешении:** метадата ViewApps теперь
доступна и показывает, что "ViewApps как терминал" — НЕ один сценарий, а три
разных (пусто-после-самоочистки / здоровый-но-бросил / реально-сломано), и их
нельзя чинить одной правкой.

## 8. UX-вывод / беклог — обновлённые кандидаты

**Кандидат №1 (уточнён, НЕ отменяет старый пункт 0412, но сужает его):**
Старая формулировка "юзер видит провальный статус в ViewApps и не
возвращается" подтверждается метадатой только для 17% выборки. Пункт 0412
стоит **разбить на два**:
  - 0412a — экран `unhealthy` apps (сломанное приложение) без явного call-to-
    action "что делать" — это по-прежнему кандидат на правку, но узкий.
  - 0412b (новый) — пустой экран (`empty:true`) уже имеет CTA
    (`frontend/app/(console)/projects/[projectId]/apps/page.tsx:568-593`,
    `EmptyState` + `TemplateDeployCards`), и юзеры ВСЁ РАВНО не возвращаются
    после того как сами снесли приложение. CTA есть, но не срабатывает —
    возможная причина: пользователь снёс приложение ПОСЛЕ провала (аналог
    "сброс и заново"), и ему нужен не общий CTA "создать первое приложение", а
    контекстный "твоя прошлая попытка упала на X, вот починка", которого нет.
    Нет данных чтобы утверждать точную причину — это гипотеза для следующего
    цикла (A/B или интервью, не кодовая правка вслепую).

**Кандидат №2 (без изменений с прошлого цикла, не закрывать):** kkartov —
живое доказательство что баг стоил юзера в реальном времени. Инцидент закрыт
кодом (d4af2faa/17db736d), но универсального детектора "N одинаковых failure
одним актором за окно" по-прежнему нет — и в этом цикле нашлись ЕЩЁ ДВЕ
такие петли у того же юзера (VerifyDomainAuthorization×31,
RevealEnvVar×23), которые тоже никто не заметил синхронно.

**Кандидат №3 (новый, sess-0822b):** системная failure-петля `SetDatabaseTier`
(332+36 падений, живая) на внутренних инфра-аппах — резолвер тира базы не
видит git-путь для non-project-owner (internal/platform/devtools) приложений.
Не user-facing, но означает автоскейл памяти БД для собственной инфры Dada
мёртв. Кандидат на отдельный тикет, искать вызывающий код по
`"set database tier: no ServiceDatabaseV2"` в gitops-agent.

**Кандидат №4 (наблюдение, не эскалировать):** chat→audit разрыв держится на
0% три недели подряд после вайринга хука — можно закрывать `project_audit_events_silently_drops_rows.md`
как исторический, если ещё один цикл подтвердит 0%.

## 9. Дыра инструментирования (без изменений, ещё не закрыта)

Нет действия, фиксирующего что юзер УВИДЕЛ причину фейла билда после
`ViewBuildLogs` — таблица знает про открытие лога, не про понимание. Метадата
ViewApps закрыла ОДНУ такую дыру (что юзер видел на списке приложений); эта
дыра (что юзер видел/понял в логе сборки) — открытый вопрос следующего цикла.

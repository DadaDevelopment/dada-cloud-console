# audit_events разбор — sess-0822 (повторный цикл), 2026-08-22

Гейт: kubectl exec в postgresql-0 живой, DSN получен, все числа ниже измерены.
DSN: argocd-prod/dada-cloud-console-backend#DATABASE_URL, exec через postgresql-0 в ns databases.

## 1. Новые юзеры

7д: 3 — kkartov@yandex.ru (08-17 19:46), lifecoachrussia@yandex.ru (08-19 09:37), tarotreaderhimu@gmail.com (08-21 13:58).
30д: 30 новых users всего.

### Полные цепочки

**kkartov@yandex.ru** — 513 строк audit, самый вовлечённый юзер цикла. Создавал/удалял приложение `instatic` минимум 5 раз подряд (ConnectGitRepo → TriggerBuild → часто BuildFinished failure → DeleteApp → заново). Внутри этого — 22:06:14–22:34:44 08-18: **172 подряд `SeedDatabaseDSN failure`** каждые ровно 10 секунд (28 минут), `metadata.error = "decoding encryption key: encoding/hex: invalid byte: U+000A"`. Это ТОТ ЖЕ инцидент, что в памяти `project_secret_trailing_newline_invisible_to_shell_checks.md` (хвостовой \n в GITOPS_ENCRYPTION_KEY, гейты 17db736d/d4af2faa) — уже пофикшено 2026-08-19, но kkartov ушёл ДО фикса и не вернулся. Терминал: `ViewApps success` 08-19 19:26:37, тишина с тех пор. **Живого приложения сейчас нет (git_repos=0, builds=0 — всё удалено самим юзером).**

**lifecoachrussia@yandex.ru** — репо gulyaev-ai-core, TriggerBuild x4 с failure, `TriggerAutofix` (юзер использовал автофикс), затем `BuildFinished success` 08-19 11:26 и повторный `DeployImageVersion success` 08-20 07:16 (оба — actor_type=system, юзер это не видел синхронно). Терминал user-действия: `TriggerBuild success` 08-19 09:41. **НЕ churn** — приложение живо и задеплоено, юзер просто не проверял результат руками (аналог findng прошлого цикла: асинхронный успех после ухода).

**tarotreaderhimu@gmail.com** — идентичная прошлому циклу сигнатура: 2 TriggerBuild подряд с BuildFinished failure (npm install), между ними CreateServiceDatabase+SeedDatabaseDSN (юзер чинит не то), 3-й TriggerBuild ушёл в BuildFinished failure тоже. Тишина с 08-21 14:09:36 (свежий кейс, <48ч на момент замера — рано считать финальным).

## 2. Кто не сделал ничего / провалы инструментирования

Zero-audit юзеров: 0 (как и в прошлом цикле — SignUp всегда пишет).

Мёртвые сигнапы (0 build, 0 repo, 0 chat) за 30д: **19 из 30**, все — ботоволна 08-08 19:49–08-09 05:37 (уже задокументирована `project_signup_farm_wave_pollutes_funnel.md`), плюс внутренний a.meshkov@dada-tuda.ru. Паттерн держится без изменений.

Chat→audit разрыв (`AgentChatUserMessage`): за окно с момента вайринга хука (08-07) по сейчас — 50 chat-сообщений vs 37 audit-строк = 26% теряется, **тот же процент, что в прошлом цикле — разрыв не растёт исторически**. НО за последние 7 дней отдельно: 12 vs 12 = **0% потерь**. Дыра `recordAuditAsync` (`project_audit_events_silently_drops_rows.md`) на свежих данных не проявляется — не значит закрыта, значит молчит на текущей нагрузке.

## 3. Граф пути (30д, actor_type=user, 39 акторов)

### Первое значимое действие после SignUp
CreateProject 8, CreateApp 4, BuildFinished 3, ViewProject 2, CreateServiceDatabase 1, AgentChatActionDeclined 1, RedeemPromo 1, SetEnvVar 1.

### Терминальное действие (последнее событие акторa, тишина >48ч на момент замера)
30 из 39 акторов молчат >48ч. Из них:
- **19 — голый `SessionStart` без единого follow-up действия** — все 19 это ботоволна 08-08 (1-4 audit-строки всего, вход по кэшированному токену без реальной активности). Не новый сигнал, объяснён волной.
- 11 реальных акторов: 4 внутренние/тестовые (dada-tuda.ru, dada.local, keycloak.local test-аккаунт).
- **6 внешних реальных**: ViewApps 3 (kkartov, good.win2283, sergeykozlov2006), CreateApp 1 (tech@xn--d1acaa3cs0b.xn--p1ai), ViewProject 1 (cryocrm), DeployImageVersion 1 (gunopice85).
- Из этих 6 — 3 (sergeykozlov2006, tech@xn--..., gunopice85) имеют **живые задеплоенные приложения с июня** — это дормант-доволен, не churn. Настоящий отвал с терминалом ViewApps: **kkartov (1)**, good.win2283 (0 build/repo — реальный отвал, чатился 4 раза но не билдил).

## 4. Сравнение с прошлым циклом (sess-0821h)

Прошлый: 4 из 7 живых отвалившихся кончают на `ViewApps` (57%).
Сейчас: 3 из 6 внешних реальных терминалов = `ViewApps` (50%) — **паттерн держится 2 цикла подряд**, хоть абсолютное число живых-отвалов ниже (часть прошлых "молчунов" оказалась дормант-доволен при перепроверке build/repo, не churn).

## 5. UX-вывод / беклог

**Кандидат №1 (держится, эскалировать не нужно, не закрывать):** `ViewApps` остаётся топ терминальной точкой реального (не бот, не внутренний) отвала — 2 цикла подряд, ~50-57%. Юзер смотрит на список приложений после непонятного/провального статуса и не возвращается. Пункт 0412 (заведён в прошлом цикле) остаётся актуальным. Файл для правки: рендер списка/пустого-стейта приложений в консоли (искать `ViewApps`/`AppList` в `frontend/` — точный file:line не найден в этом цикле, следующий шаг).

**Кандидат №2 (заведён sess-0822a):** churn kkartov — прямое доказательство, что живой конфиг-баг (гейт d4af2faa, хвостовой \n в GITOPS_ENCRYPTION_KEY) стоил реального юзера в реальном времени: 172 идентичных failure за 28 минут по одному актору, ни один страж не заметил. Инцидент-причина уже закрыта кодом (d4af2faa/17db736d) — но детектора для самого класса "N одинаковых failure по одному актору за окно" по-прежнему нет. Заведён пункт беклога (см. `bl` id ниже).

**Кандидат №3 (наблюдение):** chat→audit разрыв (26% исторически) не растёт на свежих 7 днях (0% потерь) — не подтверждает регрессию, но и не доказывает фикс. Смотреть ещё один цикл прежде чем закрывать.

## 6. Дыра инструментирования

Нет действия, фиксирующего, что юзер УВИДЕЛ причину фейла билда после `ViewBuildLogs` (открыл лог) — таблица знает про открытие, не про понимание. Недостающий action: что-то вроде `BuildFailureAcknowledged` или клиентское событие рендера ошибки. Это системная слепая зона графа: невозможно отличить "открыл лог, понял, ушёл чинить в другом месте" от "открыл лог, не понял, сдался".

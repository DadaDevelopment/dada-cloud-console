---
id: 0117
status: open
prio: P0
title: КТО УДАЛИЛ ПРОЕКТ — НЕВОССТАНОВИМО, ПОТОМУ ЧТО СНОС УБИВАЕТ СОБСТВЕННЫЙ СЛЕД НАМЕРЕНИЯ — расследование пропажи проектов chenlikun
created: 2026-08-13
sess: sess-0813a
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🔴 КТО УДАЛИЛ ПРОЕКТ — НЕВОССТАНОВИМО, ПОТОМУ ЧТО СНОС УБИВАЕТ СОБСТВЕННЫЙ СЛЕД НАМЕРЕНИЯ (sess-0813a, 2026-08-13, [code+live], origin/main@fac92de1) — расследование пропажи проектов `chenlikun.18@gmail.com` и `macmam@atomicmail.io` ЗАКРЫТО по механизму. H2 в буквальной формулировке ОПРОВЕРГНУТА: FK `audit_events.project_id` живьём = `ON DELETE SET NULL` (миграция 110), не CASCADE. H1 (демо-реапер) ОПРОВЕРГНУТА: `demo_apps.go:171` ставит только `DeleteApp`. Настоящая причина — прикладной `DELETE FROM audit_events WHERE project_id = $1` внутри `wipeProjectRows` (`gitops-agent/internal/worker/dbwatcher.go`), уже исправленный коммитом `6d5c4e93` (08-09 17:01Z) на detach+stash; оба инцидента (chenlikun 08-09 05:37Z, macmam 08-08 20:41Z) произошли ДО фикса. ОСТАЁТСЯ ОТКРЫТЫМ и это отдельная дыра: `wipeProjectRows` (`dbwatcher.go:1443-1533`, `DELETE FROM projects` на `:1532` — ЕДИНСТВЕННЫЙ такой call-site в репо) по-прежнему безусловно сносит строки `operations` проекта, а именно там живут `actor_id`/`action`, то есть КТО и ЗАЧЕМ снёс. Сегодня по обоим юзерам `operations` = 0 строк. Тот же класс, что [[project_proof_of_deletion_comes_from_intent_not_reflection]]. Фикс: писать одну переживающую снос строку намерения (actor, action, project id+slug) ДО транзакции сноса, либо `ON DELETE SET NULL` для `operations.project_id` вместо hard-delete. Внешние источники, пережившие снос, если понадобится добить «кто»: git-коммит `[DADA Console] Delete Project <slug>` от `mgr.RemoveAndPush` (`dbwatcher.go:1403-1408`) и лог `db-watcher: deleted project` (`:1440`).

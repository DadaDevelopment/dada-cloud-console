---
id: 0392
status: open
prio: P2
stream: 2
hypothesis: platform-truth
title: Наш собственный scratch-проект 15 суток держит красную строку в панели владельца
created: 2026-08-20
sess: sess-0820d
---
[live 2026-08-20, /api/v1/admin/overview + psql]

`domain_issues` содержит `m2-delwedge-6ccb0a-836ad9.dada-tuda.ru`,
`status=failed`, `cert_status=failed`, возраст 1340647 с (15.5 суток).
Проект `m2-delwedge-6ccb0a` (id `352f7adc-0fdb-47c4-ad2c-600c4e76a826`,
создан 2026-07-31 14:41Z, владелец `alexkekiy@dada-tuda.ru`) — НАШ собственный
scratch аварийного цикла, не юзерский.

Цена: владелец смотрит именно в эту панель, и половина её красных строк —
наш же мусор. Это ровно тот механизм, из-за которого красное перестают читать.
Вторая строка `a2a-hub.pro` (проект `DADA Internal`, 3.6 суток) — тоже наша.

Работа: убрать scratch-проект вместе с доменом (M5 — сначала проследить, кто
читает `352f7adc`, потом сносить), а для внутренних проектов решить: либо они
не попадают в `domain_issues` панели, либо панель отделяет «наше» от
«юзерского» отдельной секцией. Молча фильтровать нельзя — тогда мы ослепнем
к собственным поломкам.

M2: в `domain_issues` не остаётся строк старше суток, принадлежащих нам,
и при этом наши собственные поломки видны отдельным списком.

---
id: 0413
status: open
prio: P2
stream: 3
hypothesis: H08
title: Отказ в чат-подтверждении выглядит тупиком: бэк уже дозадаёт вопрос, фронт этого не показывает
created: 2026-08-20
sess: sess-0820e
---
Разбор аудита sess-0820e (state/audit-path-graph.md): из 12 неферм-аккаунтов один
ушёл в тишину именно на `AgentChatActionDeclined` (отказ на `connectGitRepo`).

Заземление [code]: бэкенд после отказа НЕ замолкает —
`backend/internal/api/agent_chat.go:32` кладёт модели явную инструкцию спросить,
что юзер хочет вместо этого, и `:1798` вызывает `ResumeTurn()`. То есть
follow-up реально генерируется.

Дефект во фронте: карточка подтверждения
`frontend/components/agent-chat-panel.tsx:796` рисует статичную плашку
«Отклонено» и ничем не намекает, что ниже сейчас появится продолжение диалога.
Юзер читает «отклонено» как конец разговора и уходит.

Фикс дешёвый и без бэкенда: авто-скролл к появившемуся follow-up ЛИБО чип
«агент отвечает ↓» на карточке отказа.

Мерить: доля цепочек, где после `AgentChatActionDeclined` есть следующее
действие в течение 10 минут (сейчас 0 из 1 наблюдаемого случая).

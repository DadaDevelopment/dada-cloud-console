---
id: 0235
status: open
prio: P2
title: P2-АЛЕРТ-УТОНУЛ-В-ШТОРМЕ · **Премисса «авария 08-03 не подняла ни одного алерта» ОПРОВЕРГНУТА живьём
created: 2026-08-04
sess: sess-0804d
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🟡 P2-АЛЕРТ-УТОНУЛ-В-ШТОРМЕ (sess-0804d, 2026-08-04) · **Премисса «авария 08-03 не подняла ни одного алерта» ОПРОВЕРГНУТА живьём — не воскрешать.** [origin argo-infra `d8698ee6`, 08-02 20:45] правила `ProductSurfaceDown` и `ProductSurfaceFlaky` были заведены за 20+ часов ДО аварии. [live Prometheus TSDB] серия `ALERTS{alertname="ProductSurfaceDown"}` перешла в `firing` в 17:09Z — ровно `for:5m` после начала простоя в 17:03Z — и держалась всё окно; `probe_success` дал 86 точек `==0` подряд. Ресивер тоже живой: Alertmanager роутит `severity=critical` в telegram-бот (чат `-1002477188567`, тред 451), маршрут стабилен с мая. Пустые логи пода alertmanager за окно — ротация из-за шквала, а не молчание; метрики TSDB ротации не подвержены и показывают отправку. **Настоящая дыра не в правиле, а в отношении сигнал/шум:** в момент аварии одновременно активных алертов стало 45 (было 8), скорость отправки 10-11 сообщений/5 мин — каскадные `ServiceIncident` по всем сервисам залили тред, и единственное сообщение, которое реально значило «продукт лежит для юзеров», утонуло. Поэтому инцидент нашли постфактум ручным запросом, хотя алерт честно сработал. Конфигом одного правила это не чинится. Кандидаты: (а) отдельный высокоприоритетный маршрут/тред для `ProductSurfaceDown` в обход общего потока, (б) дедуп/агрегация каскадных `ServiceIncident`, чтобы массовое падение давало одно сообщение, а не сорок пять. ⚠️ НЕ добавлять второе правило на ту же метрику: `ProductSurfaceFlaky` уже покрывает деградацию с более чувствительным окном (30м), дубль дал бы задвоенные нотификации ровно в шторм. **M0 для рутины:** это второй раз за цикл, когда задача выросла из чужой сводки, а не из проверенного источника (первый — `bb6e933` «не в проде»). Прежде чем строить по формулировке «X не работает», сначала прогнать авторитетный запрос, доказывающий, что X действительно не работает.

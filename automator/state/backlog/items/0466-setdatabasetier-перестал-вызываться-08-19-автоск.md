---
id: 0466
status: open
prio: P1
stream: 2
title: SetDatabaseTier перестал вызываться 08-19: автоскейл БД собственной инфры не падает, а молчит
created: 2026-08-21
sess: sess-0822c
---
Замер [live audit_events, sess-0822c]. Пункт 0463 описывал этот джоб как «живой и падающий»
(332 отказа, +36 за 3 дня). Сейчас картина другая и хуже: total 688 строк
(332 failed / 12 success / 344 pending), максимальный `created_at` = **2026-08-19 11:31:58**,
и за последующие трое суток — НОЛЬ строк любого исхода.

Контроль, чтобы это не было общим сбоем аудита: соседний системный джоб `AutoscaleApp`
пишет строки вплоть до 08-21 21:47. Значит перестал вызываться именно `SetDatabaseTier`.

Класс отказа тот, который мы уже ловили: «отсутствие улики кодирует два противоположных
факта» — пустой хвост читается и как «починилось», и как «умер планировщик». Отличать надо
по возрасту последней строки, а ни один страж этого не делает.

Что проверить: планировщик/backoff вокруг `SetDatabaseTier` в gitops-agent (грепать
`set database tier`), не заглушила ли петлю 344-строчная очередь pending. Диагностики по
коду ещё не было — только SQL.

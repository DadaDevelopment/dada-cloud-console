---
id: 0481
status: open
prio: P1
stream: 2
title: telemost-bot крашлупит на ModuleNotFoundError: журнальный модуль не доехал в образ
created: 2026-08-22
sess: sess-0822i
---
Пульс sess-0822i [live 2026-08-22 ~16:20].

`kubectl get pods -A`: `internal-prod/telemost-bot-deploy-9975cbc-mv8lt` = `0/1 CrashLoopBackOff`,
7 рестартов, возраст пода 12 минут. То есть образ ВЫКАЧЕН ТОЛЬКО ЧТО и сразу лёг.
Причина из панели `/api/v1/admin/overview` и логов: `ModuleNotFoundError: No module named 'journal'`.

Наше, не юзерское. Влияния на живых юзеров нет (внутренний бот), поэтому не взято в цикл
по анти-яку — но лежит по-настоящему, а не «выглядит мёртвым».

Важно перед работой: под молодой (12 мин). Возможно, прямо сейчас чинит владелец или
параллельная сессия. Сначала посмотреть, кто последний двигал образ, и только потом трогать.

Что сделать: найти, почему `journal` отсутствует в образе — модуль не положен в requirements
либо переименован, а импорт остался. Плюс отдельный вопрос: почему сборка образа прошла
зелёной, если импорт падает на старте — это наш повторяющийся класс «зелёный билд, мёртвый апп».

M2: под `telemost-bot-deploy` в `Running 1/1` без роста счётчика рестартов в течение 10 минут.

---
id: 0234
status: open
prio: P0
title: E3 (письмо возвращает уснувшего) — кандидат на kill, но N=1
sess: sess-0806a
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] E3 (письмо возвращает уснувшего) — кандидат на kill, но N=1. [live, sess-0806a] 7/7 отправок алертов прошли (`last_send_ok=true`), единственный реально уснувший получатель bruzas.85 получил 2 письма (08-04, 08-05) и не вернулся: 0 audit_events с 07-24. Одновременно E56 (health-блок внутри консоли) собрал 2/2 реальных внешних юзера с кликами. Вывод для распределения сил: в сценарии «приложение упало» вкладываться в внутрипродуктовые сигналы, а не в почту; почта = уведомление-для-протокола, не реактивация. Убивать E3 только после второго уснувшего получателя.









  - **Сделано (sess-0804d):** `bafa52d` — `resolveGitRepo` переведён на `LEFT JOIN` + сентинел `errRepoWithoutInstallation` (`cloud_tasks_store.go`); `autofix.go` отдаёт `code=repo_without_installation` и честный русский текст вместо «репозитория нет»; `diagnose.go` отдаёт наружу `autofix_unavailable_reason` (`no_repo` / `repo_without_installation`) и пишет его в аудит. Тест `backend/internal/api/resolve_git_repo_test.go` — три ветки (нет строки / строка без installation / живой installation), **реально выполнен, не скипнут**: локальный real-DB прогон (`TEST_DATABASE_URL` на throwaway PG 55432, все миграции) → `--- PASS` 3/3, `ok ... 1.648s`. gofmt на всех 4 файлах чист. **Не покрыто:** фронт (`agent-chat-panel.tsx` под WIP параллельной сессии) — кнопка всё ещё немая, текст причины пока виден только в API; и сам прод ещё на `bb6e9335`.

---
id: 0267
status: open
prio: P0
title: P0-CI-CACHE-IS-A-HARD-DEPENDENCY sess-0814k · **Залипший RWX-том кэша останавливает ВСЮ доставку платформы, а не деградирует её.**
created: 2026-08-14
sess: sess-0814k
section: 📈 СКВОЗНАЯ АНАЛИТИКА ПУТИ (owner 2026-07-31: разбирать аудит каждый цикл — прави
---
- [ ] 🔴 P0-CI-CACHE-IS-A-HARD-DEPENDENCY sess-0814k · **Залипший RWX-том кэша останавливает ВСЮ доставку платформы, а не деградирует её.** 2026-08-14 билд #1152 взял Jenkinsfile из `27219cbb` и не начал ни одной стадии: под сборщика висел `0/7 ContainerCreating` 10 минут, потому что PVC `jenkins-build-cache` (Longhorn RWX, `pvc-f419b2c6`) был в `attaching/unknown`, а движок `-e-0` на инстанс-менеджере ноды `zh58h` крутился в краш-цикле на залипшем iSCSI-таргете (`tgtadm: can't find the logical unit: exit status 22`). Разлочено вручную сносом застрявшего пода сборщика (том сразу `attached/healthy` на `rt7fr`; инстанс-менеджер НЕ трогали — на `zh58h` 5 работающих движков чужих томов). **Это рецидив:** прошлый цикл записал «три сборки подряд падали на Longhorn EIO в `jenkins-build-cache`». Правка по смыслу: смонтировать кэш так, чтобы недоступность тома давала сборку БЕЗ кэша (медленнее), а не отсутствие сборки. Живёт в шаблоне пода агента Jenkins (чужой конфиг) — заземлить, где именно объявлен volume, прежде чем предлагать патч.

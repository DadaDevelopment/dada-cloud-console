---
id: 0082
status: open
prio: P0
title: /telemetry/events ПРИНИМАЕТ path БЕЗ ПРОВЕРКИ СУЩЕСТВОВАНИЯ РЕСУРСА
created: 2026-08-14
sess: sess-0814e
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🔵 `/telemetry/events` ПРИНИМАЕТ `path` БЕЗ ПРОВЕРКИ СУЩЕСТВОВАНИЯ РЕСУРСА (sess-0814e, 2026-08-14, [code]) — `backend/internal/api/ux_events.go:184` (`RecordUXEvents`) сознательно неаутентифицирован (ловим долиновую часть пути), `path` = свободный текст клиента до 500 символов без валидации и без детекта бот-формы сверх per-IP токен-бакета. Это ПОТЕНЦИАЛ загрязнения; наблюдаемого загрязнения НЕ доказано — приоритет понижен до 🔵.
  ⚠️ САМОКОРРЕКЦИЯ ТОГО ЖЕ ЦИКЛА: первая редакция этого пункта утверждала, что волна-ферма реально записала выдуманную навигацию, опираясь на то, что 8 `project_id` из её путей отсутствуют в `projects`. Отсутствие объясняется СНОСОМ: бан-свип 08-08 удалил 17 проектов волны, а старый `wipeProjectRows` перед сносом делал явный `DELETE FROM audit_events` (FK из 001 без `ON DELETE`) — починено `6d5c4e93` + миграция `110_audit_events_project_fk_set_null.sql` **08-09, на день позже волны** [origin: тело коммита описывает ровно этот кейс — после свипа все 18 аккаунтов выглядели как «ничего не делали», выжил только `SessionStart`, потому что пишется без `project_id`]. Запрос существования ресурса не различает «не было никогда» и «удалено».
  Правило, которое остаётся в силе: читая `ux_events` как правду, сверять id из `path` с `projects`/`builds`/`git_repos` **и с историей удалений** — иначе снесённое читается как выдуманное.

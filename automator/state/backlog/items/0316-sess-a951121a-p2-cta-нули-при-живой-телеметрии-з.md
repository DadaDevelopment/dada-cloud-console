---
id: 0316
status: open
prio: P2
stream: 6
title: sess-a951121a 🟡 P2-CTA-НУЛИ-ПРИ-ЖИВОЙ-ТЕЛЕМЕТРИИ · Знаменатель ux_events здоровый: 1220 строк, max(occurred_at)=2026-08-02 21:33Z
created: 2026-08-02
sess: sess-a951121a
section: 📣 ПОТОК 6 — ПРОДАТЬ МЕХАНИКУ /box (owner-directive 2026-08-01 интерактив)
---
- [~] 🔒 sess-a951121a 🟡 P2-CTA-НУЛИ-ПРИ-ЖИВОЙ-ТЕЛЕМЕТРИИ (sess-0803b, из пульса) · Знаменатель `ux_events` здоровый: 1220 строк, `max(occurred_at)=2026-08-02 21:33Z` — то есть таблица пишется. При этом `git_platform_access_cta:%` = **0 за всё время**, `quota_upsell:%` = **0 за всё время**, `app_latest_build:%` = 4. Нули не артефакт мёртвого трекинга, но и не доказательство «юзеру не нужно»: сперва проверить условия рендера — показываются ли эти CTA вообще хоть кому-то (какой prop/флаг/квота их гейтит). Порядок: сначала рендер, потом интерпретация. Поправка к прошлым ТЗ: колонка называется `occurred_at`, не `created_at` — прошлые «пустые» замеры могли падать на этом. · **08-03 sess-a951121a: ПОРЯДОК СОБЛЮДЁН — сперва рендер.** (1) `quota_upsell` рендерится ТОЛЬКО при ответе `quota_exceeded` от API [code frontend apps/domains/databases page.tsx]; на проде таких отказов **0 за всё время** (`audit_events` c `metadata->>'reason' like '%quota%'` = 0) [live] — значит нуль честный, чинить нечего, никто в квоту не упирался. (2) `git_platform_access_cta` гейтится `platform_access='anonymous'` = github-репо без installation_id [code gitrepos.go:83 classifyPlatformAccess]; таких на проде 4 из 15 [live], но вкладку Git открывали ровно один раз — у аппа `fanvk`, а он `archive`, то есть CTA там и не должен был появиться. Нуль честный, но получен случайно. (3) ОТГРУЖЕНО `01bf79e`: открытие вкладки Git теперь всегда пишет `app_git_tab:<access>` (installation/anonymous/archive/none/load_failed), маркер CTA остался поверх — знаменатель и числитель по одной шкале. tsc/eslint чисто. live-M2 ОТКРЫТ: строку `ux_events` может создать только настоящий посетитель (синтетический клик = подделка телеметрии); после выката проверить наличие маркера в отданном чанке и ждать живой строки.

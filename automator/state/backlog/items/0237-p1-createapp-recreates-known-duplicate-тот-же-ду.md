---
id: 0237
status: open
prio: P1
title: P1-CREATEAPP-RECREATES-KNOWN-DUPLICATE · **Тот же дубль, который owner уже удалял руками 3 недели назад, консоль создала заново.**
created: 2026-08-04
sess: sess-0804b
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🟠 P1-CREATEAPP-RECREATES-KNOWN-DUPLICATE (sess-0804b, 2026-08-04) · **Тот же дубль, который owner уже удалял руками 3 недели назад, консоль создала заново.** [origin argo-infra] `3085a38d` (owner, 07-13): «Console CreateApp wrote a second app.yaml for reels-tracker under project `platform` while the app already existed» — тогда коллизия имён в ApplicationSet положила реконсиляцию ВСЕХ tenant-аппов. 08-02 17:02:32 `ef2ce674 [DADA Console] Create App reels-tracker` (актор `platform-bot@dada-tuda.ru`) создал этот же файл снова: `clusters/beget-prod/projects/platform/environments/prod/apps/reels-tracker/`. Имя теперь с хешем (`argoName: reels-tracker-prod-9dfda63a`), поэтому реконсиляцию не положило — но дубль живой и мёртвый одновременно. Настоящий апп: `internal-prod/reels-tracker-deploy` 1/1 Running, там env, ServiceDatabase, токен из секрета. Дубль: `platform-prod`, 2 реплики, ни разу не Ready с 08-02, падает на `pydantic ValidationError: telegram_bot_token Field required, input_value={}` (в его values.yaml env НЕТ вообще). PVC у дубля нет, домен `reels-tracker-fe2427.dada-tuda.ru` отдаёт 503 [live curl]. **Вторичный вред, который дороже рублей:** каждый цикл краша оба пода тянут образ заново — `Successfully pulled ... Image size: 295629382 bytes` каждые ~5 минут [live events]. Это ровно та нагрузка, от которой Nexus исторически уходил в локаут и ломал сборки ЖИВЫМ юзерам (см. память `project_nexus_npm_lockout`, `project_userservice_nexus_block`). M0: удаление руками не помогло — дубль вернулся, значит дыра в МЕХАНИЗМЕ `CreateApp` (нет гейта на platform-unique имя, хотя память `project_appset_name_collision` прямо это требует). НЕ брал в этом цикле по правилу одного яка.

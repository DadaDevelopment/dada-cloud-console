---
id: 0485
status: open
prio: P1
stream: 2
title: Свипер class-fix считает разморозкой техн. успех: возврат человека не измеряется
created: 2026-08-22
sess: sess-0822i
---
Приёмка свипера class-fix, sess-0822i [live psql + code + curl].

**Свипер РАБОТАЕТ, это подтверждено, пункт не про поломку.**
`backend/internal/api/build_classfix_sweeper.go:158-171`, запись реестра :66-74
(`static-npm-template-20260821`, FixedAt 2026-08-21T22:18:46Z).
Жертва env `9eca0ca6-…` (юзер `tarotreaderhimu@gmail.com`): 3 провала 08-21 13:58-14:09 UTC,
свипер на первом тике после раската создал сборку `3c8ebe82` (`trigger='class_fix'`, `attempt=2`,
`status='success'`), аудит `BuildAutoRetried` (actor `system`) с `class_fix_id` и
`previous_build_id`. Апп живой, `curl` по ингрессу = **200**.
Масштаб класса: **1 из 1** жертв на всей платформе разморожена.

**Дыра — в определении успеха, а не в механизме.** Свипер считает жертву разморожённой
по ТЕХНИЧЕСКОМУ исходу (сборка зелёная, под живой). Вернулся ли ЧЕЛОВЕК — не измеряется:
после третьего провала 14:09 UTC в `audit_events` по этому env НЕТ ни одной человеческой строки
(нет `ViewApps`, нет логина, нет ручного `TriggerBuild`) — только системные
`BuildAutoRetried`/`BuildFinished`/`CreateApp`/`SendBuildNotification`.
Единственный «сигнал возврата» — `SendBuildNotification: outcome=success`, а это претензия
бэкенда на попытку отправки, НЕ доказательство доставки, открытия или возврата.

Это наш повторяющийся класс: «отгруженный рычаг мерить счётчиком СВОЕГО действия».
Сейчас дашборд свипера может показать 100% разморозки при нуле вернувшихся людей.

Что сделать (минимум, без писем — они запрещены владельцем 07-30):
1. Развести в метрике свипера `unfrozen_technically` и `user_returned` — второе считать
   по ЧЕЛОВЕЧЕСКОМУ действию в `audit_events` после `BuildAutoRetried`.
2. Не называть в отчётах «разморожен» того, у кого нет ни одной человеческой строки после фикса.

M2: метрика свипера отдаёт два РАЗНЫХ числа, и для env `9eca0ca6-…` они равны 1 и 0.

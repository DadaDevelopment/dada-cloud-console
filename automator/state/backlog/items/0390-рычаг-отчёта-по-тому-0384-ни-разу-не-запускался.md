---
id: 0390
status: open
prio: P1
stream: 2
hypothesis: platform-truth
title: Рычаг отчёта по тому (0384) ни разу не запускался живьём — код доехал до прода только 2026-08-20
created: 2026-08-20
sess: sess-0820d
---
Заземление [live 2026-08-20]: код рычага доехал до прода только сейчас — под `dada-cloud-console-backend-5dbcd99f4c-gpckp` крутит `…backend:256ffeaa` (startTime 01:20:44Z), `git merge-base --is-ancestor 9e8ba9c7 256ffeaa` → true. До этого прод стоял на `72f80f95`, то есть с момента закрытия 0384 (коммит `9e8ba9c7`) обработчик физически отсутствовал в бинаре. RBAC-половина была применена раньше: `kubectl auth can-i create jobs --as=system:serviceaccount:argocd-prod:dada-cloud-console -n artemmendeleev-gmail-com-prod` → yes.

Работа: прогнать рычаг живьём и убедиться, что Job доходит до `Complete` и отдаёт реальные числа inode/диска, а не 202 в пустоту. Обработчик — `backend/internal/api/volume_maintenance_jobs.go` (613 строк, из `9e8ba9c7`), маршруты в `backend/internal/api/router.go`.

Честный предел, из-за которого это НЕ P0: отчёт по собственному признанию 0384 не освобождает ни одного inode, то есть `fonbet-value` он не поднимет. Гонять его в ЧУЖОМ неймспейсе ради числа, которое уже известно, — цена выше пользы. Прогон делать в `agent-sandbox` на специально забитом томе.

M2: в песочнице у аппа БЕЗ живого пода POST даёт 202, Job уходит в `Complete`, GET отдаёт непустые числа inode — рычаг работает ровно в том состоянии, ради которого делался.

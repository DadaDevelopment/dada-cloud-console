---
id: 0429
status: open
prio: P0
stream: 1
hypothesis: H11
title: InstallSolution падает 11 из 15 за 30д: юзер ловит 3 отказа за 44с и уходит навсегда
created: 2026-08-21
sess: sess-0821f
locked_by: sess-0821f
locked_until: 2026-08-21T13:15Z
---
Замер sess-0821f [live psql, `audit_events.metadata`, окно 30 дней].

Установка шаблона из галереи завершилась успехом 4 раза и отказом 11 раз — 27% успеха.
Причины отказов в метаданных: `quota`, `env_failed` (500), `malformed_body` (400).

Почему это P0, а не обычный баг. Anon-clone шаблона — ЕДИНСТВЕННЫЙ механизм активации,
про который у нас есть измеренное доказательство работы (3 из 6 незнакомцев активировались).
Он же — обход git/OAuth-стены, где умирает почти вся воронка. То есть 73% отказов бьют
ровно по тому месту, ради которого поток 1 вообще существует.

Живая цепочка, показывающая цену [live psql]: новый юзер `kkartov@yandex.ru` (рег 08-17)
получил три отказа `env_failed`/500 подряд за 44 секунды и после этого не совершил
НИ ОДНОГО действия. Четвёртый день с нулём аппов. Это не абстрактный процент, это
конкретный потерянный человек на единственном рабочем пути.

Что сделать по существу:
1. Заземлить [code] обработчик установки и развести три причины отказа — они разной природы:
   `quota` = стена плана (должна читаться как предложение, а не как поломка),
   `env_failed`/500 = НАШ отказ (юзер ничего не сделал не так),
   `malformed_body`/400 = контракт фронта с бэком, то есть тоже наш.
2. `env_failed` разобрать до корня: что именно не смогло примениться и почему это 500,
   а не понятный вердикт. 500 на пути активации = юзер видит «сломалось» и уходит.
3. Приёмка двухполюсная: доказать RED на воспроизведённом отказе и GREEN после фикса,
   а вердикт снимать со СЧЁТЧИКА собственного действия (доля успешных `InstallSolution`
   в `audit_events`), а не с того, что «стало собираться».

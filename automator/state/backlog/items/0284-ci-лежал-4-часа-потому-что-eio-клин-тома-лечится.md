---
id: 0284
status: open
prio: P0
title: CI ЛЕЖАЛ 4 ЧАСА, ПОТОМУ ЧТО EIO-КЛИН ТОМА ЛЕЧИТСЯ НЕ УДАЛЕНИЕМ ПОДА, А ПЕРЕЕЗДОМ НА ДРУГУЮ НОДУ
created: 2026-08-12
sess: sess-0812a
section: Открытые долги (не терять)
---
- [ ] 🔴 CI ЛЕЖАЛ 4 ЧАСА, ПОТОМУ ЧТО EIO-КЛИН ТОМА ЛЕЧИТСЯ НЕ УДАЛЕНИЕМ ПОДА, А ПЕРЕЕЗДОМ НА ДРУГУЮ НОДУ (sess-0812a, 2026-08-12, [live kubectl+longhorn]) — `jenkins_home` (`pvc-62cbc693`, RWO) отдавал `Input/output error` на любую запись И на `ls`, при этом Longhorn показывал `attached healthy`, обе реплики `running`, ext4 смонтирован `rw`. Симптом в продукте: триггеры уходили из очереди без executable, `nextBuildNumber` рос 1074→1075 «в воздух», один триггер вернул `IOException: Input/output error`. Клин был НОДО-локальным (d5dns — та самая нода, у которой в 01:26 UTC умер kubelet): том, переехавший на xnwvp, записался с первой попытки. Удаление пода (runbook postgres) НЕ помогло — новый под ушёл в CrashLoopBackOff (`Can not write to /var/jenkins_home/copy_reference_file.log`); помогло `scale 0` + удаление устаревшего `volumeattachment` + повторный подъём, который перепланировал под на другую ноду. Второй хвост: после подъёма Jenkins считал билд #1071 «в процессе» (`<completed>false</completed>` в `build.xml`, `result=FAILURE`), `DisableConcurrentBuilds` блокировал всю очередь, рестарт пода зомби НЕ разбирал — снял только `rm -rf builds/1071` + рестарт. Долг: этот класс не мониторится вообще — `attached healthy` в Longhorn не значит «пишется». Нужна проба записи как сигнал.

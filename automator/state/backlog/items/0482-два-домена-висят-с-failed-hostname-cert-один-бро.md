---
id: 0482
status: open
prio: P2
stream: 2
title: Два домена висят с failed hostname+cert, один — брошенный тест агента (17.9д)
created: 2026-08-22
sess: sess-0822i
---
Пульс sess-0822i [live 2026-08-22, `/api/v1/admin/overview` секция `domain_issues`].

Два домена, оба НАШИ, оба `hostname failed` + `cert failed`:
- `m2-delwedge-6ccb0a-836ad9.dada-tuda.ru` — возраст 17.9 суток. По имени (`delwedge`,
  хеш-суффикс) это брошенный тестовый ресурс агентской сессии, нарушение правила A5
  «свой тест-ресурс снимается в ТОМ ЖЕ цикле».
- `a2a-hub.pro` — возраст 5.96 суток, живой домен второго уровня, НЕ тестовый по виду.

Действия РАЗНЫЕ, не сваливать в одно:
1. `m2-delwedge-*` — кандидат на снос. НО перед сносом (M5) проследить, кто читает строку:
   есть ли под ней живой апп, не станет ли это ещё одним «осиротевшим доменом под живым аппом»
   (известный класс проекта). Пруф удаления брать из намерения, а не из отражения.
2. `a2a-hub.pro` — сначала выяснить, ЧЕЙ и нужен ли. Не сносить. Если нужен — чинить выпуск серта.

Плюс вопрос выше обоих: домен, у которого серт не выпускается 17.9 суток, не порождает
никакого сигнала — мы узнали о нём только из ручного разбора панели.

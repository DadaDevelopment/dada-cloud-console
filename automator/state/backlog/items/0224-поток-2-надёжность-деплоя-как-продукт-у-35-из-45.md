---
id: 0224
status: open
prio: P2
title: ПОТОК-2 (надёжность деплоя как продукт): у **35 из 45** аппов в проде НЕТ ни readinessProbe, ни livenessProbe
created: 2026-08-06
sess: sess-0806d
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🟡 ПОТОК-2 (надёжность деплоя как продукт): у **35 из 45** аппов в проде НЕТ ни `readinessProbe`, ни `livenessProbe` [live 2026-08-06, срез по всем `*-prod`]. Затронуты ВСЕ живые юзер-аппы: artemmendeleev, ggrk52 (оба), goleva, good-win2283, artempro2021, hzlol, tvkassistantbot, workassistantbot. Механизм вреда: без readiness-гейта k8s считает под Ready в момент старта процесса, Service сразу берёт его в Endpoints, а при `RollingUpdate` + `replicas:1` старый под гасится до того, как приложение реально забиндило порт → гарантированное окно 503 на КАЖДОМ деплое юзера, которого ни юзер, ни мы не видим. Это ровно «деплой долгий и ненадёжный» из STRATEGY.
      ПРИЧИНА НАЙДЕНА ТОЧНО [code]: пробы в чарте ЕСТЬ, но заперты флагом. `dada-argo/helm/common/templates/deployment.yaml:476` включает generic liveness/readiness/startup только при `.Values.healthcheck == "true"`, а дефолт `"false"` (`dada-argo/helm/common/values.yaml:88`) и ни `helm/app|python|javascript/values.yaml`, ни dada-cloud его не переопределяют — `rg -i healthcheck` по `backend/`, `gitops-agent/`, `frontend/lib/` даёт НОЛЬ попаданий. Spring-аппы идут отдельной веткой `:453` и пробы получают, поэтому 10/45 «здоровы». Отсюда ровно 35/45.
      ЗЛАЯ ИРОНИЯ: `deployment.yaml:274` уже просит `maxUnavailable: 0, maxSurge: 1`, то есть rollout ЗАДУМАН как surge-then-swap без простоя. Без readinessProbe эта гарантия пустая — k8s свапает на «готовый» под, который ещё не слушает.
      ПРАВКА (scoped): в `deployment.yaml:476-515` перевесить гейт с `healthcheck == "true"` на уже вычисляемый `$serviceEnabled`, цель — `port: http` (`$appPort`, `:50-52`), проба **tcpSocket**, а не `httpGet /health`: у юзер-аппов health-роут не гарантирован (у fonbet-value `/health` = 404), и именно поэтому флаг когда-то выключили. Плюс дефолт в `values.yaml:88`. Тестов на рендер Deployment в dada-argo НЕТ — завести `helm template`-проверку; при добавлении поля в values расширить `gitops-agent/internal/renderer/renderer_test.go:310`.
      ⚠️ БЛОКИРОВАНО ПОРЯДКОМ: параллельная сессия прямо сейчас правит ЭТУ строку `:476` (добавляет `service.enabled`, чтобы аппы без порта не получали ни Service, ни Ingress, ни проб) и `renderer.go:274-281`. Делать ПОСЛЕ того, как её работа сядет, иначе конфликт. Перед взятием — `git status` в обоих репозиториях.
      ПОБОЧНОЕ [code]: `renderer.go:264-266` уверяет в комментарии, что пустой AccessMode даёт ReadWriteMany, а на деле и `deployment.yaml:268`, и `optional-pvc.yaml:8` дефолтят в ReadWriteOnce → аппы с томом получают `Recreate` (это и есть случай fonbet-value). Комментарий врёт, поведение — RWO. Поправить вместе с пробами.
      🔴 ЛОВУШКА, ПРОВЕРЕНО [code+live 2026-08-06 sess-0806d]: «просто включить `healthcheck: "true"`» = СНОСИТ ВСЕ ЖИВЫЕ ЮЗЕР-АППЫ. Путь пробы по умолчанию — `/health` (`deployment.yaml:14-16`, `values.yaml:87 healthcheckPath`), а у вайбкодерского Next/статики такого эндпоинта нет: `startupProbe` не пройдёт → под убивается по failureThreshold → CrashLoop у КАЖДОГО аппа, плюс `maxUnavailable: 0` навсегда заклинит rollout. Проба обязана быть без допущений о пути: `tcpSocket` на тот же порт (в чарте `tcpSocket` НЕ поддержан ни разу — `grep -rn tcpSocket helm/common/templates/` пусто, это и есть недостающий кусок). Порядок правки: сперва в dada-argo научить чарт `tcpSocket`-варианту (readiness+startup, БЕЗ liveness — рестарты по HTTP-догадке опаснее простоя), потом renderer в dada-cloud выставляет флаг при `Port > 0`.
      ⚠️ ВСЁ ЕЩЁ БЛОКИРОВАНО ПОРЯДКОМ [live 2026-08-06]: рабочее дерево `/Users/alex/IdeaProjects/dada-argo` (ветка develop) держит незакоммиченный diff параллельной сессии ровно в `deployment.yaml` (гейт `:476` уже переписан на `$serviceEnabled`), `values.yaml`, `service.yaml`, `ingress.yaml`. Не трогал (M3). Брать после того, как их `service.enabled` уедет в develop.

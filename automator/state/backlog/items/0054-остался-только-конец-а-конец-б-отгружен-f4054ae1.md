---
id: 0054
status: open
prio: P1
hypothesis: H08
title: (остался только конец «а», конец «б» отгружен f4054ae1) fanvk
created: 2026-08-15
sess: sess-0815e
section: Backlog (execution-bet)
---
- [ ] 🟠 (остался только конец «а», конец «б» отгружен `f4054ae1`) `fanvk`: ЖИВОЙ ВОРКЕР НОСИТ ОСИРОТЕВШИЙ ВЕБ-ИНГРЕСС И ПОКАЗЫВАЕТ ЮЗЕРУ НЕОБЪЯСНЁННЫЙ 502 (sess-0815e, 2026-08-15, [live kubectl + снапшот + `app_url_alerts`], hypothesis: H08, origin/main@091d3a19) — под 1/1 Running, 0 рестартов, VK-бот работает по лонгполлу; 502 отдаёт nginx (`connect() failed (111: Connection refused)` на 8080), потому что апп теперь воркер (`{"worker": true, "port": 0}`), а дефолтный хостнейм `fanvk-f0e6dc.dada-tuda.ru` (`managed=t`, `active`, создан 2026-07-29) и Ingress остались от прежней ВЕБ-конфигурации. `backend/internal/api/apps.go:930` запрещает ВЫДАТЬ дефолтный домен воркеру, но не отбирает уже выданный при переключении в воркер. Алертов ноль нигде: `backend/internal/api/app_url_watcher.go:143` пропускает воркеров по посылке (комментарий на `:128-135`), что воркеру домен не выдаётся — для `fanvk` посылка ложна. Три конца: (а) при переводе аппа в воркер снимать дефолтный домен и Ingress; (б) в `app_url_watcher` проверять ПРОИСХОЖДЕНИЕ хостнейма, а не флаг воркера; (в) `AppLastMileBanner` для воркера с живым подом обязан говорить «это воркер, HTTP не обслуживается», а не показывать голое «502». Пруф = у `fanvk` в консоли нет красного 502 либо есть честная строка про воркер.
  **ПУНКТ РАСШИРЕН sess-0815f (2026-08-15, [live curl -D- + exec в под юзера + kubectl endpoints], прод == `091d3a19`): это не единичный `fanvk`, а ТРЕТИЙ полюс той же дыры в классификаторе, и второй внешний юзер уже в нём.** Панель отдаёт `dead=3` (`app_responded=3` — предикат прошлого цикла ЖИВ в проде, M2 закрыт). Разобрал все три поимённо:
  - `fonbet-value` (внешний, `artemmendeleev@gmail.com`) — под 1/1 Running, TG-бот и коллекторы работают, **порт 8000 СЛУШАЕТСЯ** (`/proc/net/tcp` → `00000000:1F40`), endpoints у сервиса есть, снапшот `worker=false, port=8000`. 503 отдаёт **САМ АПП**: `urllib` изнутри пода на `127.0.0.1:8000` → `HTTP Error 503`, снаружи `content-type: application/json`, тело `{"application_version":"0.8.2","blockers":["ESPORTSBATTLE_CURRENT_PROCESS_HAS_NOT_COLLECTED",…]}` — это его собственный readiness-ответ. Мы зовём мёртвым здоровый апп, сознательно отвечающий 503.
  - `fanvk` (внешний, `artempro2021@bk.ru`) — 502 отдаёт nginx: `content-type: text/html`, `content-length: 150`, тело ровно `<center><h1>502 Bad Gateway</h1></center><hr><center>nginx</center>`. Сервис и endpoints ЕСТЬ (`fanvk-service` → `10.244.2.209:8080`), но на 8080 никто не слушает — воркер.
  - `n8n` (наш) — 503 с хеш-домена, backend указывает на несуществующий `n8n-service`; реальный `n8n.dada-tuda.ru` отдаёт 200.
  **ВЫВОД, КОТОРЫЙ МЕНЯЕТ ФИКС: код статуса не несёт информации об ЭМИТЕНТЕ.** 404 бывает от здорового FastAPI (чинили прошлым циклом) и от сломанного маршрута; 503 бывает от здорового аппа (`fonbet-value`) и от ингресса без бэкенда (`n8n`) — ОДИН код, противоположные состояния. Дальше подкручивать список «мёртвых кодов» бессмысленно, это третий заход на те же грабли (E146 «все 17 отдают 308» → 08-15 «404 = смерть» → сейчас «503 = смерть»).
  **Дискриминатор найден живьём и он дешёвый:** ошибку генерирует ингресс ⇔ ответ несёт дефолтную страницу ingress-nginx (`content-type: text/html` + сигнатура `<center>nginx</center>`, длина ~150). Ответ аппа несёт свой content-type и своё тело. Это проверяемо прямо в пробе, без обращения к k8s.
  **Целевая классификация (4 живых кейса покрыты):** (1) `worker=true` в снапшоте → `worker_no_http`, никогда не dead, честная строка «это воркер, HTTP не обслуживается» (`fanvk`); (2) ответ = дефолтная страница nginx → `route_broken`, ЭТО настоящая поломка и она обязана остаться красной (`n8n`); (3) любой собственный ответ аппа → `app_responded` (`fonbet-value` 503, `telemost-bot`/`reels-tracker` 404); (4) не достучались (`0`) → `dead`.
  **ПРИЁМКА ОБЯЗАНА ТРЕБОВАТЬ ОБА ПОЛЮСА НА ЖИВЫХ ПРИМЕРАХ:** зелёное на `fonbet-value`/`fanvk` И красное на `n8n`. Тест, который умеет только «перестать краснеть», уже дважды прошёл и дважды соврал.
  **ОТГРУЖЕНО sess-0815f `a5681100` (2026-08-15, hypothesis: platform-truth): классификация по ЭМИТЕНТУ, оба полюса.** Проба читает до 4 КБ тела (`gitops-agent/internal/worker/livenessprobe.go`, `classifyLivenessResponse`/`isAppAuthoredBody`) и пишет автора в `http_reason`: `app_status_<code>` — тело написал апп, `status_<code>` — дефолтная страница ingress-nginx. Чтение: `backend/internal/api/admin_overview.go` (`isDeadProbeResult` + новая корзина `workers` в `live_urls`, воркер больше не dead и не ok) и `frontend/lib/last-mile-status.ts` (`isDeadLastMile`, `evaluateLastMile` молчит для `worker:true`). RED воспроизведён руками до фикса: бэкенд `admin_overview_emitter_test.go:67: after.Workers undefined` на реальной базе, фронт `not ok 1 - lib/last-mile-status.test.ts` (`isDeadLastMile` не существовал). После: 262/262 фронт, `go test ./internal/api ok` (real-DB), `go test ./internal/worker ok`, `tsc --noEmit` чист. Живые данные сверены запросом к `resource_snapshots`: `fanvk` несёт `worker=true`, у `n8n` ключа `worker` нет вовсе, `fonbet-value` — `worker=false` → после раскатки агента его 503 станет `app_status_503`. **M2 ЗАКРЫТ ЖИВЬЁМ sess-0815f 2026-08-15T06:11Z [live API + прямой SQL], оба полюса.** Прод во всех трёх деплоях `argocd-prod` == `a5681100`; панель отдаёт `workers: 1`, `dead: 1`, `dead_external: 0`, `app_responded: 4`, а в `dead_apps` остался РОВНО `n8n` (`503/status_503`). Снапшот `fonbet-value` несёт `app_status_503`, `fanvk` — `worker` вне корзины dead. **Нюанс, вскрытый приёмкой:** первый прогон новой пробы (06:05:00) честно записал `status_503` — в ту секунду апп выпал из Endpoints и 503 рисовал сам ingress; следующий прогон (06:10:20) записал `app_status_503`. То есть классификация по эмитенту ВРЕМЯЗАВИСИМА, а не липкая: `readiness: blocked` у `fonbet-value` флапает, и оба вердикта верны каждый в свою секунду. Проверено 12 последовательными in-cluster запросами через ingress с Host-заголовком — тело всегда JSON аппа, маркер `<center>nginx</center>` ноль раз.
  ОСТАТОК ПУНКТА: (б) `app_url_watcher` по происхождению хостнейма; (а) отзыв осиротевшего домена.
  **(в) ОТГРУЖЕНО sess-0815-automator2 `eb802912` (2026-08-15, hypothesis: H08).** `evaluateWorkerNoHTTP` (`frontend/lib/last-mile-status.ts`) + нейтральная (не красная) строка в `AppLastMileBanner`: «Адрес отвечает {code}: приложение работает как воркер и не обслуживает HTTP — проверено {ago}» + пояснение, что процесс может быть жив и адрес остался от прежней конфигурации. Условие тройное — `worker=true` И проба реально прошла И ответ не 2xx/3xx; воркер с живым кастомным доменом молчит, отсутствие пробы не рендерится как вердикт. RED снят выводом до фикса (`not ok 1 - lib/last-mile-status.test.ts`, `evaluateWorkerNoHTTP` не существовал), после — 264/264 `npm run test:unit` (было 262), `tsc --noEmit` чист. **M2 НЕ ЗАКРЫТ:** пруф на живом `fanvk` снимается после раскатки фронта, в консоли глазами не проверено.
  **Разрушительного конца (а) НЕ делать вслепую:** снос осиротевшего ингресса — односторонняя дверь, `reels-tracker` показал, что хеш-домен может быть load-bearing (`TICKTICK_REDIRECT_URI`). Сначала классификация и честный текст, снос домена — отдельным решением с проверкой ссылок на хост.

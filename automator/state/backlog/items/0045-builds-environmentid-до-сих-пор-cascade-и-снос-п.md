---
id: 0045
status: open
prio: P2
title: builds.environment_id ДО СИХ ПОР CASCADE, И СНОС ПРЕВЬЮ РЕАЛЬНО СТИРАЕТ ЕГО СБОРКИ
created: 2026-08-15
sess: sess-0815m
section: Backlog (execution-bet)
---
- [ ] 🟡 `builds.environment_id` ДО СИХ ПОР CASCADE, И СНОС ПРЕВЬЮ РЕАЛЬНО СТИРАЕТ ЕГО СБОРКИ (sess-0815m, 2026-08-15, [live `pg_constraint` + code]) — миграция 116 намеренно оставила `builds_environment_id_fkey` = `c`, аргумент в её комментарии: «когда окружение снесено, апп снесён, и сборке негде показываться». Аргумент верен для страницы аппа и неверен для замера: путь стирания живой, и он не гипотетический — `backend/internal/api/previews_test.go` и `preview_teardown_audit_fk_test.go:11,51` документируют, что тирдаун превью делает `DELETE FROM environments WHERE id=$1 AND is_ephemeral` последним шагом. Значит история сборок превью-окружений исчезает вместе с ними, и любая воронка по превью структурно недосчитывает. Масштаб `unmeasured` — за 30д этот путь в цифрах выше себя не проявил (остаток после фикса = 1 пограничный билд). НЕ чинить вслепую: сперва замерить, сколько сборок реально уносит тирдаун превью, и только потом решать SET NULL против осознанного сохранения CASCADE.

---
id: 0109
status: open
prio: P0
title: ЗАМЕРИТЬ АТРИБУЦИЮ РЕГИСТРАЦИЙ, КОГДА 72cfa3eb ДОЕДЕТ ДО ПРОДА
created: 2026-08-13
sess: sess-0813b
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🔴 ЗАМЕРИТЬ АТРИБУЦИЮ РЕГИСТРАЦИЙ, КОГДА `72cfa3eb` ДОЕДЕТ ДО ПРОДА (sess-0813b, 2026-08-13, задача владельца прямым текстом «займись UTM чтобы все реги были с источниками», origin/main@72cfa3eb) — ОТГРУЖЕНО: миграция 114 (`users.signup_source/medium/campaign/referrer`, бэкфилла нет — истории не существует); фронт ловит ПЕРВОЕ касание на любой странице приземления (`rememberAttribution` в `frontend/lib/metrika.ts`, вызов из `components/ux-telemetry-provider.tsx`), utm > хост внешнего реферера > литерал `direct`, same-origin реферер источником не становится; 4 куки на всю зону `.dada-tuda.ru` переживают перелёт в Keycloak; бэкенд читает их в `signupAttributionFromCookies` (`api/router.go`) и пишет ТЕМ ЖЕ стейтментом, что и INSERT юзера, плюс в метаданные аудит-строки `SignUp` (`auth/provision.go`). Ветка `ON CONFLICT` колонки не трогает — первое касание побеждает навсегда. Пустое поле = SQL NULL, не `''`. Гейты: `gofmt` пусто, `go build`/`go vet` чисто, 4 новых real-DB теста + весь пакет `internal/auth` 18/18 PASS, `tsc` чист, `test:unit` 190/190. ЗАМЕР — `state/signup-attribution.sql`: (1) убедиться, что прод-образ несёт `72cfa3eb` или новее; (2) запрос 2 — доля атрибутированных среди регистраций после выкатки, цель 100% (прямой заход обязан приехать как `direct`, NULL = дыра, разбирать поимённо); (3) запрос 4 — каналы по объёму. ДО выкатки запросы вернут ноль строк = «данных ещё нет», НЕ «атрибуция не работает». Осмысленно не раньше первых регистраций после реопена.
  **ЗАМЕР sess-0813i (2026-08-13), НЕ СОСТОЯЛСЯ — ЗНАМЕНАТЕЛЬ НОЛЬ.** Гейт доставки ПРОЙДЕН: прод-образ console-backend = `2a42767a`, `git merge-base --is-ancestor 72cfa3eb origin/main` → true [live kubectl+git]. Регистраций после выкатки (03:00Z 13.08) — **0**; последняя строка `users` вообще — 2026-08-11 13:30:05Z, то есть за 1.5 суток ДО выкатки; `SignUp` в `audit_events` после выкатки — 0 [live psql]. Контекст: 19 регистраций за 7д, значит источник не пересох в принципе, пересох именно после метки. Код не подтверждён и не опровергнут. ДЕЙСТВИЕ прежнее: первая же регистрация после 03:00Z 13.08 → сразу запросы 2/4; `signup_source IS NULL` у неё = дыра захвата первого касания, разбирать поимённо.


  **РЕЗУЛЬТАТ sess-0813d.** Перелёт разбит на пару строк аудита: `StartGitAppInstall` (pending, пишется ДО выдачи URL) и `FinishGitAppInstall` (вердикт при возврате), корреляция по `metadata->>'install_nonce'` — nonce подписанного state вытащен наружу из `signInstallState`. Callback анонимный, поэтому актор восстанавливается из строки намерения (`gitrepos.go recordInstallVerdict`); на user-authorization пути актор приезжает прямо из `github_oauth_states` тем же стейтментом (`git_oauth.go recordOAuthVerdict`). Вердикта без намерения не пишется — строка без актора портит пер-юзерные счётчики. Покрыты ОБА механизма коннекта (`metadata.flow` = `app_install` | `user_authorize`) — прошлая формулировка пункта знала только про App-install, а фронт `handleConnectProvider` ходит тремя ветками. Причины смерти в `metadata.reason`: `missing_installation_id`, `resolve_installation_failed`, `save_installation_failed`, `state_expired`, `exchange_code_failed`. Пруф: 7 тестов против реального рига + весь пакет `./internal/api` (5 прогонов зелёных). Запрос замера — `state/git-oauth-flight.sql`. **Сам обрыв НЕ починен**, до выкатки запросы вернут ноль строк (= «данных ещё нет», НЕ «утечки нет»).

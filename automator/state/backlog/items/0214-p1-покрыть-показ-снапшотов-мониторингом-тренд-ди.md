---
id: 0214
status: open
prio: P1
title: P1-ПОКРЫТЬ-ПОКАЗ-СНАПШОТОВ-МОНИТОРИНГОМ — тренд «диск ползёт вниз» разворачивается вручную раз в сутки агентом, а сигнала нет
sess: sess-0806k
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🟠 P1-ПОКРЫТЬ-ПОКАЗ-СНАПШОТОВ-МОНИТОРИНГОМ (sess-0806k, хвост закрытого P0 выше) — тренд «диск ползёт вниз» разворачивается вручную раз в сутки агентом, а сигнала нет: `DiskPressure` остаётся False (evictionHard = только `memory.available: 128Mi`, конфиг managed-Beget → owner-actions), `FreeDiskSpaceFailed` пишет «freed 0 bytes» и тонет. Нужен алерт на РАЗРЫВ `longhorn_volume_actual_size_bytes` против фактически используемых данных тома И на снапшоты со статусом `notready` старше ~1ч. Плюс проверить, кто ставит `markRemoved=true` без последующего purge (recurring job? наш код?) — это и есть корень, снапшоты копились «удалёнными».

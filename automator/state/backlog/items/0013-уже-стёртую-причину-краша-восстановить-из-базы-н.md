---
id: 0013
status: open
prio: P2
hypothesis: H08
title: УЖЕ СТЁРТУЮ ПРИЧИНУ КРАША ВОССТАНОВИТЬ ИЗ БАЗЫ НЕЧЕМ
created: 2026-08-19
sess: sess-0819c
section: Backlog (execution-bet)
---
- [ ] 🟡 УЖЕ СТЁРТУЮ ПРИЧИНУ КРАША ВОССТАНОВИТЬ ИЗ БАЗЫ НЕЧЕМ (sess-0819c, 2026-08-19, [live psql + code], hypothesis: H08, origin/main@e6a44a11). Правка `e6a44a11` останавливает стирание ВПЕРЁД, но строки, обнулённые до неё, остаются немыми до следующей положительной классификации. Живой пример на момент цикла: `artemmendeleev-gmail-com-prod/fonbet-value` — `last_sent_cause_kind=db_read_only` (причина была названа и по ней ушло письмо), а `cause_kind` и `cause` обнулены, значит баннер у самого заметного живого юзера прямо сейчас без вердикта. Дешёвого read-side фолбэка НЕТ: `app_health_alerts` хранит только `last_sent_cause_kind`, человеческого текста `cause` для него не сохраняется, а баннер (`app-alerts-banner.tsx:565-579`) требует И текст, И kind. Варианты: (а) хранить `last_sent_cause`/`last_sent_cause_line` рядом с kind и падать на них в `loadAppAlerts` (`app_alerts.go:106`); (б) разовый форс-реклассификации при пустом kind и непустом `last_sent_cause_kind`.

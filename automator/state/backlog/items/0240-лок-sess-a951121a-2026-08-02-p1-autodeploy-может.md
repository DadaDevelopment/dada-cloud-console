---
id: 0240
status: open
prio: P1
title: ЛОК sess-a951121a (2026-08-02) P1-AUTODEPLOY-МОЖЕТ-НЕ-ПИСАТЬ-AUDIT · **ОБЕ РАЗВИЛКИ СДЕЛАНЫ, ждём live-M2.** (а) читающая сторона
created: 2026-08-02
sess: sess-a951121a
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
---
- [ ] 🔒 ЛОК sess-a951121a (2026-08-02) P1-AUTODEPLOY-МОЖЕТ-НЕ-ПИСАТЬ-AUDIT · **ОБЕ РАЗВИЛКИ СДЕЛАНЫ, ждём live-M2.** (а) читающая сторона: §0 `audit-path-graph.md` больше не исключает `system@dada.local` слепо — для `DeployImageVersion`/`CreateApp` правило переписано, старые строки привязываются через `operations.environment_id` -> `git_repos.created_by`. (б) продукт, коммит `f7ba573` (в origin/main, едет): `handoffActor` теперь отдаёт `created_by` для ЛЮБОГО билда без `triggered_by`, а не только для первого; способ запуска переехал в отдельное поле `metadata->>'initiator'` (`manual`/`push`/`system`), поэтому ручной и push-деплой по-прежнему различимы, но уже не ценой потери человека. Атрибуция несла два смысла в одном `actor_id` — это и был корень. Гейты: `gofmt -l` пусто, `go build ./...` и `go vet ./...` чисто, real-DB `TestHandoffActor` 3/3 + `TestHandoffDeploy_PushRedeployKeepsTheOwner` PASS (плюс 4 старых теста хендоффа не сломаны). Проверил, что `operations.actor_id` нигде не читается как право доступа — только вставки [code rg backend/internal]. **live-M2 = строка `audit_events` action=`DeployImageVersion` с actor_id ≠ нулевого uuid И `metadata->>'initiator'='push'` на проде.** Ниже — исходная формулировка. **08-03, почему M2 всё ещё не закрыт [live psql]:** пуш-деплой ПРОШЁЛ (`DeployImageVersion` по `reels-tracker` 08-02 18:34Z), но лёг на нулевой актор с `initiator='system'` — `handoffActor` сваливается на системного юзера, когда `git_repos.created_by IS NULL`, а таких репо на проде 3 из 15. Соседняя сессия это заметила и выпустила миграцию 086, но её бэкфилл джойнит аудит по `(environment_id, resource_name)`, а у ВСЕХ исторических строк `ConnectGitRepo` `environment_id IS NULL` (поле стали писать позже) — миграция применилась на проде (`schema_migrations` содержит 086) и не изменила ни одной из этих строк; запасной путь `projects.owner_id` тоже пуст. Мой фикс `22fce07` — миграция 088, тот же бэкфилл по существующему ключу `(project_id, resource_name)`; на реальной БД доказано: сид прод-формы -> 086 оставляет NULL -> 088 проставляет актора -> повтор идемпотентен. Ожидаемый эффект на проде: 2 строки (`reels-tracker`, `dada-development-site` в проекте `640ed82d`) получают актора `4b62b2e7`; третья (`dada-development-site` в демо-проекте `10000000-…-0001`) остаётся NULL честно — её ConnectGitRepo записан в другом проекте. **M2 закроет ближайший push после выката 088.** · **08-03 [live psql] МИГРАЦИЯ 088 ПРИМЕНЕНА НА ПРОДЕ** (образ `22fce074` на всех подах консоли, `schema_migrations` содержит `088_git_repos_created_by_backfill_by_project`): `git_repos.created_by` у `DadaDevelopment/reels-tracker` и `dada-development-site` в проекте `640ed82d` = `4b62b2e7-b98c-4479-99d0-622e3757b81a`, NULL осталась ровно одна строка — `dada-development-site` в демо-проекте `10000000-...-0001`, чей connect-аудит записан под другим проектом, то есть честно без улик. Осталось для M2: дождаться ОРГАНИЧЕСКОГО пуша (ручной `triggerBuild` даст `initiator=manual` и ничего не докажет) и снять строку `DeployImageVersion` с `actor_id <> 00000000-...` и `metadata->>'initiator'='push'`. Последние деплой-строки на 08-03: обе системные (08-02 18:34Z reels-tracker, 16:17Z dada-development-site), новых билдов после выката ещё не было.

---
id: 0055
status: open
prio: P1
hypothesis: H08
title: ВЕРДИКТ «НАША ПОЛОМКА» ЕСТЬ В БАЗЕ, НО НЕ ВЕДЁТ НИ К КАКОМУ ДЕЙСТВИЮ
created: 2026-08-15
sess: sess-0815e
section: Backlog (execution-bet)
---
- [ ] 🟠 ВЕРДИКТ «НАША ПОЛОМКА» ЕСТЬ В БАЗЕ, НО НЕ ВЕДЁТ НИ К КАКОМУ ДЕЙСТВИЮ (sess-0815e, 2026-08-15, [live `app_health_alerts` + логи + UI-код], hypothesis: H08, origin/main@091d3a19) — на `fonbet-value` продуктовая часть сработала: `cause_kind=platform_network`, письмо ушло 08-14 09:58. Дальше пусто. (1) Дедуп `backend/internal/api/app_health_watcher.go:594-600` держит тишину, пока `reason` не менялся, — юзер просидел в молчании двое суток на НАШЕЙ поломке; при `cause_kind` платформенного класса и длительности > N часов нужно повторное уведомление, это не спам, это наш долг. (2) `ExtractCauseLine` (`backend/internal/notify/notify.go:629`) не срезает ANSI-escape, и они утекают в UI (`app-alerts-banner.tsx:428` рендерит `<pre>{alert.cause_line}` как есть). (3) Для `platform_network` в баннере только «Диагностика» и логи (`:445-455`) — рычага нет вообще, тогда как для `bad_connection_string`/`ssl_not_supported` есть inline-починка. Юзер видит «платформа виновата» и не может сделать НИЧЕГО. Минимум — статус инцидента с честным ETA вместо кнопки, которой не существует.
  **ЗАЗЕМЛЕНО ЖИВЬЁМ sess-0815e [live kubectl + прямой curl], И ЭТО ПЕРЕВОРАЧИВАЕТ ПУНКТ: ни один из трёх «наших трупов» не мёртв.**
  - `telemost-bot` — под Running, в логах пода рядом стоят `"GET / HTTP/1.1" 404` и `"GET /health HTTP/1.1" 200`, `"GET /docs HTTP/1.1" 200`; тело `{"detail":"Not Found"}`. Здоровый FastAPI без корневого роута. Работает long-polling (`POST api.telegram.org/…/getUpdates 200`) — входящий трафик ему не нужен вообще, лишний ingress `telemost-bot-f221d0-dada-tuda-ru` (ссылок на хост нет нигде: ноль в репо, ноль в ConfigMap/Secret/env).
  - `reels-tracker` — под Running, `/`=404, `/health`=200, сервис реально работает (`[sync] Syncing TickTick for 2 users` каждый час, последний 04:05Z). Публичный адрес НУЖЕН: `reels-tracker-config` несёт `TICKTICK_REDIRECT_URI=https://reels-tracker.dada-tuda.ru/callback`. **Ingress не сносить** — снос убьёт OAuth.
  - `n8n` — реальный хост `n8n.dada-tuda.ru` отдаёт **200**. 503 приходит ТОЛЬКО с хеш-домена: ingress `n8n-64b3d0-dada-tuda-ru` указывает в backend `service: n8n-service`, а сервис называется `n8n` → `services "n8n-service" not found`. Корень: генератор managed-domain ingress хардкодит соглашение `<app>-service`; для аппов, заведённых консолью, это совпадает, а для АДОПТИРОВАННОГО Helm-чарта — нет. Тот же класс, что [[project_infra_adoption_import]]: адопция не сверила реальное имя сервиса. Хост `n8n.dada-tuda.ru` load-bearing дважды (`WEBHOOK_URL`, `N8N_AI_ASSISTANT_BASE_URL` в env самого n8n + контакт-поинт алертов Grafana) — трогать только суррогатный хеш-ingress.
  **ГЛАВНЫЙ ДЕФЕКТ ОКАЗАЛСЯ НЕ В РАЗДЕЛЕНИИ СЧЁТЧИКА, А В КЛАССИФИКАТОРЕ.** `frontend/lib/last-mile-status.ts:27` (и потребитель в `backend/internal/api/admin_overview.go`) считают мёртвым всё с `http_status >= 400`. Из-за этого `{"detail":"Not Found"}` от ЗДОРОВОГО uvicorn и `503` от nginx-БЕЗ-БЭКЕНДА неразличимы, хотя это противоположные состояния. Плюс `gitops-agent/internal/worker/livenessprobe.go` дёргает только `/`, поэтому headless API/бот структурно неспособен когда-либо стать зелёным. Реальный расклад `dead=6` = 1 реально сломанный суррогатный ingress + 2 ложных срабатывания на 404-от-приложения + 3 юзерских.
  **Это второй заход того же грабля:** E146 ловил «все 17 отдают 308» (метрика слепа к смерти), теперь — «метрика видит смерть там, где её нет». Приёмка обязана требовать ОБА полюса: красное на заранее известном трупе И зелёное на заранее известном живом.
  **Пруф (обновлён):** `404` от приложения не попадает в `dead`; `503` суррогатного ingress n8n — попадает; счётчик разделён на внешних/наших.

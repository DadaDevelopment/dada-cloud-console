---
id: 0021
status: open
prio: P3
hypothesis: H08
title: ДОГОНЯЮЩИЙ СЛОТ ПИСЬМА НЕДОСТИЖИМ ДЛЯ КЛАССА reason=Error
created: 2026-08-16
sess: sess-0816f
section: Backlog (execution-bet)
---
- [ ] ❓ ДОГОНЯЮЩИЙ СЛОТ ПИСЬМА НЕДОСТИЖИМ ДЛЯ КЛАССА `reason=Error` (sess-0816f, 2026-08-16, [live+code], hypothesis: H08). Механизм E160 (переход `cause_kind` пусто→непусто открывает внеочередное письмо) физически не может сработать у аппов с `reason=Error`: `emailableReason` (`backend/internal/api/app_health_watcher.go:85`) пускает в почту только OOMKilled/CrashLoopBackOff/ImagePullBackOff/ErrImagePull, а `Error` исключён ОСОЗНАННО по просьбе владельца («хватит долбить письмами»). Наблюдение: `fonbet-value` получил верный `db_read_only` в 16:59Z, письмо не ушло и не уйдёт. Развилка владельца, а не задача: (а) оставить как есть — консоль знает, человек нет; (б) пускать в почту `Error` ТОЛЬКО когда причина названа (`cause_kind` непуст) — тогда шумных писем «упал, exit=1» по-прежнему нет, а письма с диагнозом есть. Молча расширять список НЕЛЬЗЯ.

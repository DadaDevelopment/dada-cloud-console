---
id: 0389
status: closed
prio: P0
stream: 2
hypothesis: H02
title: kkartov ушёл молча: коннект удался, билд упал, три установки шаблона подряд failure
created: 2026-08-20
sess: sess-0820d
closed_at: 2026-08-20
closed_commit: fc406299
closed_note: Обе половины названы. (1) Сборка kkartov упала за 14с на гонке нашего пути удаления: load repo 00000000-... no rows — починено dd419d4e (через ~9ч после инцидента), радиус за 30д = kkartov, 5 сборок. (2) Три InstallSolution с reason=env_failed/500 04:09-04:11Z 08-19 — платформенный отказ GITOPS_ENCRYPTION_KEY (хвостовой \n → hex.DecodeString invalid byte U+000A), тот же ключ в то же время ронял agent-chat в логах пода; починено 17db736d, живёт в проде. Оба уже закрыты кодом; ЖИВОЙ дефект из этой трассы — карточка сборки гнала юзера 'переподключить репозиторий' при platform_error/app_deleted (наша вина на его гит) — закрыт fc406299: needsRepoReconnect() + Rebuild становится primary, когда других кнопок нет. Тест-страж показан RED (not ok 92) до фикса.
---
Живой человек, потерянный на НАШИХ отказах, а не на своей ошибке. Вскрыто при разборе 0387
(sess-0820d, [live psql, audit_events]).

`kkartov@yandex.ru`, рега 2026-08-17. Цепочка терминального дня (2026-08-19, ~03:15-03:20Z):
1. `ConnectGitRepo` `GitRepo instatic` — **success**;
2. `TriggerBuild` сразу же — **`BuildFinished` failure через 14 секунд**;
3. `DeleteApp` — success (снёс за собой);
4. три `InstallSolution` (шаблоны) подряд — **все три failure**;
5. тишина. Больше в аудите его нет.

То есть человек дошёл до продукта, попробовал ЧЕТЫРЕ способа получить живой апп (свой репозиторий +
три шаблона) и все четыре раза получил от нас отказ. Шаблоны — это тот самый механизм, который у нас
числится ЕДИНСТВЕННЫМ измеренно-работающим путём активации (template anon-clone, 3/6 незнакомцев).
Здесь он дал 0/3.

Что НЕ выяснено и составляет работу пункта:
- почему упал билд `instatic` за 14 секунд (14с — это подозрительно рано для настоящей сборки: похоже
  на отказ ДО компиляции, а не на ошибку кода юзера). Поднять лог билда и назвать причину;
- почему три `InstallSolution` подряд отдали `failure` — один и тот же шаблон или разные, и общая ли
  причина с билдом. Если шаблоны ломаются системно, это бьёт по всем новичкам, а не по одному человеку;
- есть ли у нас вообще сигнал наружу, когда у юзера подряд падает всё: он ушёл молча, письма не было,
  `feedback` пуст.

Приоритет P0 по иерархии №1/№4: это не гипотеза о воронке, а конкретный человек, которому продукт
отказал четыре раза за пять минут, и класс отказа (шаблоны) общий для всех новых юзеров.

M2: названа причина отказа билда и причина трёх `InstallSolution failure`; если причина наша — починена
и проверена живым прогоном установки того же шаблона; если причина в коде юзера — это видно юзеру в
продукте в момент отказа, а не только нам в аудите.

---
id: 0059
status: closed
prio: P0
hypothesis: H02
title: 2026-08-15 sess-0815a **ПОЧИНЕНО db8c6299 origin/main** · 🔴 СОХРАНЕНИЕ ENV-ПЕРЕМЕННОЙ МОЛЧА ОТКАТЫВАЛО ЖИВОЙ ДЕПЛОЙ ЮЗЕРА НА ПРОТУ
created: 2026-08-15
sess: sess-0815a
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-15
closed_commit: db8c6299
---
- [x] 2026-08-15 sess-0815a **ПОЧИНЕНО `db8c6299` origin/main** · 🔴 СОХРАНЕНИЕ ENV-ПЕРЕМЕННОЙ МОЛЧА ОТКАТЫВАЛО ЖИВОЙ ДЕПЛОЙ ЮЗЕРА НА ПРОТУХШИЙ ОБРАЗ (sess-0815a, 2026-08-15, [live kubectl + psql + code], hypothesis: H02, origin/main@05da9c64) — `queueEnvApply` (`backend/internal/api/envvars.go:50-58`) при каждом сохранении env ставит редеплой и берёт образ из КЕША `resource_snapshots.summary_json->>'image'`. У `megafactory` (artempro2022@yandex.ru) это поле замерло на дайджесте `sha256:09af1723…` ещё ДО его успешного деплоя 08-13 23:20Z. Юзер дважды заходил чинить конфиг (08-14 07:25, 08-14 23:40) — и каждое сохранение env откатывало его рабочий деплой на старую сборку. Сейчас в `artempro2022-yandex-ru-prod` крутится именно старый дайджест: сервер поднимается («слушает порт 3000»), но у той сборки нет маршрута `/` → публичный адрес отдаёт 404 вторые сутки. Это НЕ тот инцидент с `DATABASE_URL` (тот закрыт `1623ab16`) — это отдельный класс: **редеплой из кеша вместо авторитетной записи**. Чинить: источником образа для env-редеплоя сделать последний успешный билд / фактически задеплоенный образ; при отсутствии авторитетной записи — патчить только env, не подменяя образ. Пруф = сохранение env на аппе не меняет дайджест в кластере. **СДЕЛАНО `db8c6299`:** `lastDeployedImage` читает последнюю строку `deployments` (её пишут только реальные деплои — build-agent `HandoffDeploy`, `RollbackDeployment`/`PromoteDeployment`; сам `queueEnvApply` туда не пишет, поэтому источник не самоотравляется, в отличие от `operations`), и только при отсутствии строки падает на старое поведение (снапшот) — не регрессия для аппов, задеплоенных лишь через `UpdateAppImage`/deploy-hook. Расхождение подтверждено живьём [live psql]: снапшот `sha256:09af1723…`, `deployments`/`builds` знают реальный успешный деплой 08-13 23:20Z `sha256:557541…`, а в `operations` два паразитных Committed-редеплоя старого образа ровно в моменты правок юзера (08-14 07:25, 08-14 23:40). Тесты `backend/internal/api/envvars_deploy_source_test.go` (риг с реальной БД): предпочтение журнала над протухшим снапшотом, фолбэк без строки, отсутствие редеплоя без образа где-либо; `go test ./internal/api/ -count=1` ok. **ОСТАТОК (M2):** апп юзера всё ещё крутит старый образ — фикс сам не редеплоил; пруф = после раскатки сохранение env на аппе не меняет дайджест в кластере.

---
id: 0037
status: closed
prio: P1
hypothesis: H02
title: ПЕРВЫЙ ДЕПЛОЙ ЖИВОГО ЮЗЕРА ИСЧЕЗ БЕЗ ЕДИНОГО СОБЫТИЯ
created: 2026-08-16
sess: sess-0816d
section: Backlog (execution-bet)
closed_at: 2026-08-16
closed_commit: 6d5c4e93
---
- [x] ЗАКРЫТ sess-0816d 2026-08-16 БЕЗ ПРАВКИ КОДА — ДЕФЕКТ ОКАЗАЛСЯ УЖЕ ПОЧИНЕННЫМ ДО ИНЦИДЕНТА-НАБЛЮДЕНИЯ · 🟠 ПЕРВЫЙ ДЕПЛОЙ ЖИВОГО ЮЗЕРА ИСЧЕЗ БЕЗ ЕДИНОГО СОБЫТИЯ (sess-0815r, 2026-08-15, [live psql], hypothesis: H02) — `macmam@atomicmail.io`, проект `9008a656-6373-4ab8-b9cf-6dbf87544f70`: по 34 сообщениям агент-чата видно, что апп `9router` дошёл до Ready с превью-ссылкой (20:43 UTC), ссылка не открылась, через 6 минут апп пропал из списка, юзер написал «протестирую позже» и не вернулся. Сегодня проекта нет в БД, и при этом в `audit_events` НЕТ ни `DeployImageVersion`, ни `DeleteApp`, ни `DeleteProject` — платформа съела первый деплой юзера, не оставив ни намерения, ни вердикта. Текст самой ошибки уже честный (`previewUpstreamUnavailableBody`, `backend/internal/api/preview_gate.go:71-79`), дыра в другом: пропажа не объяснена ничем. Пруф закрытия = сценарий «эфемерный/превью-апп исчез» оставляет строку в `audit_events` с причиной (grace/TTL/удаление юзером), и агент-чат её цитирует. Точка входа поиска: `grep -rn "AppServer attached" backend/internal/`.
  **ВЕРДИКТ sess-0816d [origin + live psql + live `pg_constraint`]:** пропажу объясняет НЕ дыра в аудите, а уничтожение уже записанных строк. До коммита `6d5c4e93` (2026-08-09T17:01Z) `wipeProjectRows` выполнял `DELETE FROM audit_events WHERE project_id = $1` в той же транзакции, что сносила `operations` и `projects` (`gitops-agent/internal/worker/dbwatcher.go`, старые FK были без `ON DELETE`, поэтому удаление было структурно обязательным). Инцидент `macmam@atomicmail.io` датирован 2026-08-08 ~20:43Z — то есть на ~20 часов РАНЬШЕ фикса. Сегодня оба FK живой схемы = `confdeltype='n'` (SET NULL), а код только отцепляет строки, пряча `deleted_project_id`/`deleted_project_name` в `metadata` (`dbwatcher.go:1465-1514`). Единственный путь, удаляющий строку `projects`, — `dbwatcher.go:1536` из `doDeleteProject`; жнец превью физически не может снести ПРОЕКТ (он шлёт только `DeleteApp` и всегда пишет `recordSystemAudit`, `demo_apps.go:205-214`); `deleteApp`/`deleteProject` в каталоге тулов агент-чата отсутствуют (`backend/internal/agentchat/toolset.go:95-116`). Значит строить «пруф закрытия» вокруг эфемерных аппов было бы починкой несуществующего бага. ОСТАТОК, который НЕ доказан и остаётся открытым вопросом: писался ли вообще аудит на СОЗДАНИИ этого аппа — отличить нельзя в принципе, старый код стирал и создание, и деплой, и удаление одним `DELETE`. Единственная уцелевшая улика — `ux_events` за окно 20:43-20:55Z; снять её дешевле, чем строить механизм.

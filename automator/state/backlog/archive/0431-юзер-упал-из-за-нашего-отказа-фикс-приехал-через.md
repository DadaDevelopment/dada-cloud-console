---
id: 0431
status: closed
prio: P1
stream: 2
hypothesis: H11
title: Юзер упал из-за нашего отказа, фикс приехал через 10.5ч, продукт не попытался его вернуть и не знает, кого задело
created: 2026-08-21
sess: sess-0821f
closed_at: 2026-08-21
closed_commit: 11a5c313
closed_note: Отгружено в main 11a5c313 [origin]. Бэкенд: platform_recovery.go -- реестр закрытых сигнатур ДЕЙСТВИЙ (сестра platform_selfheal.go, которая умеет только ресурсы), GET /api/v1/recovery-prompt, максимум одна карточка на юзера, гейты: падение строго < FixedAt, нет самовыздоровления после FixedAt, для install-вида ещё и ноль аппов (через projects.owner_id, не project_members). Две записи: solution_install_env_failed (17db736d, 2026-08-19 11:57Z, живой пострадавший kkartov) и payment_recurring_forbidden (b49fe2a8, 2026-08-16 10:58Z, живой пострадавший artempro2021). Фронт: карточка над TemplateDeployCards на пустом списке аппов, dismiss в localStorage по kind+failed_at, data-ux на показ и нажатие. Счётчик своего действия: dada_platform_recovery_prompts_served_total{kind} + аудит PlatformRecoveryPromptServed. RED доказан выводом, не словами: бэкенд -- точечная мутация гейта created_at < FixedAt валит РОВНО TestRecoveryPromptEligible_FailureAfterFixedAtYieldsNoPrompt (1 из 5), значит тесты различают, а не просто исполняются; фронт -- мутация shouldShowRecoveryPrompt валит 3 из 8 с настоящими assertion-диффами. GREEN после отката: бэкенд build+vet+go test ./... чисто на реальной базе (рига 127.0.0.1:55432), фронт 394/394 + tsc. Копия про оплату НЕ обещает автопродление: рекуррент на шопе 1396801 недоступен, owner-action от 08-16 открыт. Замер -- E188, по счётчику показов/нажатий, НЕ по «собралось».
---
Заземление sess-0821f [live psql + code + git].

kkartov@yandex.ru (рег 08-17) 08-19 04:10:54-04:11:38 получил три подряд `env_failed`/500
на установке шаблона homepage. Причина была ЦЕЛИКОМ наша: хвостовой перевод строки в секрете
`GITOPS_ENCRYPTION_KEY` ломал `hex.DecodeString` в `crypto.decodeKey`. Он ничего не
сделал не так. Фикс `17db736d` доехал 08-19 11:57 UTC -- через 10.5 часов. Юзер зашёл
ещё раз в 19:26, посмотрел список приложений, ушёл. Четвёртый день с нулём аппов.

Дыра НЕ в баге (он закрыт), а в том, что после починки:
1. Мы не знаем, кого задело. Нет ни списка, ни счётчика пострадавших от конкретного
   собственного отказа. Я восстановил kkartov'а вручную запросом по `audit_events`
   -- то есть данные ЕСТЬ, но механизма нет.
2. Мы не сделали ни одной попытки вернуть. Письма запрещены (владелец 07-30), значит
   рычаг обязан быть продуктовый, а не переписка.

Что делать по существу (не письмо):
- Механизм «кто пострадал от нашего отказа»: по `audit_events` с `outcome=failure` и
  причиной из списка платформенных (не юзерских) -- список акторов за окно инцидента.
  Это же даёт честный знаменатель для любого post-fix вопроса «а помогло ли».
- Рычаг возврата в продукте: если у юзера последнее действие -- НАШ отказ, и с тех пор
  он не создал ни одного аппа, консоль при следующем входе обязана показать
  «мы починили то, на чём ты упал -- повторить установку» с кнопкой, а не пустой список.
  kkartov заходил ПОСЛЕ фикса и увидел пустоту -- рычаг сработал бы.

Приёмка двухполюсная: RED -- на текущем проде юзер с таким следом видит пустой список
(воспроизвести на agent-sandbox); GREEN -- видит предложение повтора. Вердикт снимать
со счётчика собственного действия (показов/нажатий), не с «стало собираться».

Порог осторожности: не превращать в спам-баннер. Условие показа узкое -- последний
исход юзера = ПЛАТФОРМЕННЫЙ отказ И ноль живых аппов И фикс уже в проде.

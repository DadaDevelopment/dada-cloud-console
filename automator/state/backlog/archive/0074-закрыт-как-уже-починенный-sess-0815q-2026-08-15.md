---
id: 0074
status: closed
prio: P0
hypothesis: H02
title: ЗАКРЫТ КАК УЖЕ ПОЧИНЕННЫЙ sess-0815q (2026-08-15, , фикс 363163e3 того же дня)
created: 2026-08-15
sess: sess-0815q
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-15
closed_commit: 363163e3
---
- [x] ✅ ЗАКРЫТ КАК УЖЕ ПОЧИНЕННЫЙ sess-0815q (2026-08-15, [live прод-psql + kubectl + dig], фикс `363163e3` того же дня) — панель больше НЕ зовёт это поломкой: агрегат разделён на `failed=1` (живой, `attach_timeout`, внутренний `alexkekiy@`) и `retired=24` (надгробия удалённых аппов). Все 24 несут `status_reason='app_deleted'`, из них 11 — внешние юзеры (`a.markov-buturski` ×3, `artempro2021` ×2, `bruzas.85` ×2, `good.win2283`, `sergeykozlov2006`, `top.decker`, `artemmendeleev`), и все 11 — хвосты УДАЛЁННЫХ аппов, а не сломанные домены живых. Живая проверка трёх из них: DNS резолвится на LB `155.212.223.198`, Ingress в кластере ноль, снаружи 404 — то есть это не «мы не выпустили сертификат» и не «юзер не долил запись». Прошлая оценка «~9 наших осиротевших» была занижена вдвое с половиной. Поправка к прошлой формулировке: колонка причины В ЕСТЬ (`status_reason varchar(40)`), утверждение «причина нигде не хранится» было ошибкой чтения схемы. Не переоткрывать. Остаточный хвост (при желании, отдельным мелким пунктом): 11 внешних надгробий не видны поимённо нигде, только числом `retired=24`. 🔴 11 ДОМЕНОВ ЖИВЫХ ВНЕШНИХ ЮЗЕРОВ В `failed`, И НИ ОДИН НЕ ВИДЕН ОТДЕЛЬНОЙ ПОЗИЦИЕЙ ПАНЕЛИ (sess-0814j, 2026-08-14, [live: пульс `/api/v1/admin/overview` + прод-psql], hypothesis: H02, origin/main@b84fa872) — панель отдаёт агрегат `domains.failed=24` одним числом, внутри него 11 доменов НАСТОЯЩИХ внешних юзеров: `good.win2283`, `a.markov-buturski` ×2, `top.decker`, `bruzas.85` ×2, `artemmendeleev`, `sergeykozlov`, `artempro2021` ×2. Класс тот же, что уже ловили дважды: агрегат читается как «фон», разбор по именам делает только агент вручную, продукт юзеру ничего не говорит. Нужно: (1) `domain_issues` в overview разделить на «наши инфра-домены» и «домены юзеров» с именами, как сделано для `not_ready`; (2) на странице домена показать причину отказа юзеру. Пруф работы = позиция с ненулевым счётчиком юзерских доменов в панели + падение этого счётчика после разбора.

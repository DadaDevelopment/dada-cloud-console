---
id: 0449
status: closed
prio: P1
stream: 2
title: Порт аппа неизменяем после создания: ошибка автодетекта фреймворка = вечный 502 без рычага
created: 2026-08-21
sess: sess-0821j
closed_at: 2026-08-21
closed_commit: 23aeb021
closed_note: Рычаг смены порта отгружен и ДОСТАВЛЕН: прод на 659e9f53, 23aeb021 (бэкенд, PATCH .../apps/:appName/port + ре-деплой) и 3b74496b (UI + i18n) — предки этого тега. Живая приёмка двухполюсная: PATCH .../apps/x/port без токена = 401 "missing or malformed Authorization header" (роут есть), PATCH .../apps/x/nosuchlever = 404 (проба умеет отдавать труп). Тест мутацией доказан RED: apps.go:1681 cur["port"]=9999 -> "snapshot port = 9999, want 3000". Замер радиуса ДО постройки (0437): расхождение targetPort в проде ровно одно и оно наше собственное, юзерских ноль.
---
Заземление sess-0821j [code, агент-explore]:
- `backend/internal/api/apps.go:812` — `if req.Port == 0 && !req.Worker { req.Port = defaultPortForFramework(req.Framework) }`.
- Таблица `backend/internal/frameworkports/ports.go:19-47` + `config/platform/framework-ports.json`:
  `vite -> 4173`, `nextjs -> 3000`, fallback `8080`.
- Порт дальше зашивается в `TargetPort` сервиса (`backend/internal/box/clusterexpose.go:121`,
  `clustercrystallize.go:1198`).
- **Рычага изменить порт у уже созданного аппа НЕТ**: ни API-роута, ни UI. Проверено поиском
  по эндпоинтам обновления аппа.

Следствие: если детект источника (`build-agent` sourcedetect) ошибся классом фреймворка
(живой пример — наш `affiliate-site`: порт 4173 от vite при процессе на 3000), апп навсегда
отдаёт 502, и юзер НИЧЕГО не может с этим сделать внутри продукта. Пересоздание аппа —
единственный путь, и его никто не подсказывает.

Это ровно класс «диагноз без рычага»: мы умеем показать поломку и не умеем дать её починить.

Что сделать: дать изменение порта существующему аппу (API + UI), с ре-рендером сервиса.
Заземлить числом ДО постройки: сколько аппов в проде имеют targetPort, расходящийся с
реально слушающим сокетом, и сколько из них юзерских. По замеру sess-0821g таких был ОДИН
и он наш собственный — значит приоритет определяется не текущим радиусом, а тем, что
цена ошибки детекта для юзера = безвыходный 502.

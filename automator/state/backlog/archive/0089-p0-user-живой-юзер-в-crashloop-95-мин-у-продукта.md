---
id: 0089
status: closed
prio: P0
hypothesis: H08
title: P0-USER: ЖИВОЙ ЮЗЕР В CRASHLOOP 95+ МИН, У ПРОДУКТА НЕТ РЫЧАГА ПОЧИНИТЬ УЖЕ СОХРАНЁННЫЙ ПЛОХОЙ DSN
created: 2026-08-14
sess: sess-0814d
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-14
closed_commit: 538dade1
---
- [x] ОТГРУЖЕНО `538dade1` (в проде НЕ подтверждено, E138) · 🔴 P0-USER: ЖИВОЙ ЮЗЕР В CRASHLOOP 95+ МИН, У ПРОДУКТА НЕТ РЫЧАГА ПОЧИНИТЬ УЖЕ СОХРАНЁННЫЙ ПЛОХОЙ DSN (sess-0814d, 2026-08-14, [live kubectl + psql], hypothesis: H08, origin/main@26858ea0) — `megafactory` / `artempro2022@yandex.ru`, ns `artempro2022-yandex-ru-prod`, 23 рестарта с 08-13 23:43Z. `DATABASE_URL` = голый хост `pg-router.databases.svc.cluster.local`, скопированный С НАШЕЙ ЖЕ страницы базы. ТРЕТИЙ случай класса. Три отгруженных рычага мимо: `1623ab16` (страница отдаёт DSN) — юзер копировал ДО выката; `5cf7fcbc` (предупреждение в env-форме) авторизован 00:34:44Z, через 52 мин ПОСЛЕ того как юзер сохранил значение в 23:42:13Z; `26858ea0` (авто-сев `DATABASE_URL`) не сработал by design — `seedDatabaseDSNIfAbsent` не перезаписывает непустое значение. ДЫРА КЛАССА: все три рычага действуют в момент ввода/создания, и ни одного — для аппа, УЖЕ заклиненного на плохой строке. Нужен вердикт `cause_kind` на сигнатуру `ENOTFOUND` со сверкой с реальным значением env (лог врёт фантомом `base`, см. пункт ниже) + рычаг починки.
  ⚠️ ЗАЗЕМЛЕНИЕ [live 2026-08-14, sess-0814d] ОПРОВЕРГЛО ТРЕТИЙ ПУНКТ ВЫШЕ. `seedDatabaseDSNIfAbsent` НЕ отказался из-за непустого значения — он НЕ ВЫЗЫВАЛСЯ ВООБЩЕ. Обе точки входа гейтятся на `appRef`, а он пустой: снапшот консоли несёт `spec.appRef: ""` (`databases.go:410` — `req.AppRef != ""`, горутина не запущена; `databases.go:758` — `boundAppRef != ""`, где `boundAppRef = serviceDatabaseAppRef(summaryRaw)`, `databases.go:632-643`). Живой CR несёт `appRef: megafactory`, но это ДЕФОЛТ РЕНДЕРЕРА (`gitops-agent/internal/renderer/renderer.go:53` — `{{ if .AppRef }}{{ .AppRef }}{{ else }}{{ .Name }}{{ end }}`), совпавший с именем аппа по случайности имён. Гард на непустое значение реально существует (`db_dsn_delivery.go:158`), но он ТРЕТИЙ в очереди и до него не доходит.
  🔴 ПРУФ, ЧТО ОТГРУЖЕННЫЙ РЫЧАГ НИ РАЗУ НЕ СРАБОТАЛ: `SeedDatabaseDSN` в `audit_events` = 0 строк ПО ВСЕМУ КЛАСТЕРУ ЗА ВСЮ ИСТОРИЮ. Юзер раскрывал креды 4 раза (23:05:54, 23:19:00, 23:25:04 успешно) — ни один reveal не дошёл до сеятеля. Отгружено ≠ работает; замерять по аудиту, а не по факту коммита.
  Пруфы починки: БД `megafactory` жива (7595 kB, owner `svc-megafactory`, `rolcanlogin=t`), маршрут в `pg-router` есть, `pgbouncer.get_auth` отдаёт SCRAM той же формы, что у здорового соседа `svc-fanbot`. `pg_stat_activity` по `svc-mega%` = 0 строк: апп НИ РАЗУ не дошёл до своей базы, то есть ломать починкой нечего. Радиус: во всём кластере 4 секрета с `DATABASE_URL`, битый ровно один — этот.
  Побочно, всплывёт сразу после починки DSN: `ADMIN_KEY` = 4 символа с `tes`, `VK_APP_ID` начинается кириллицей `тво` (вставил «твой…» вместо ID). Плюс `app.diagnose` в 23:06:52 вернул `can_autofix: false` (`repo_without_installation`) — авто-фикс ему тоже был недоступен.

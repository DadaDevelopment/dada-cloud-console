---
id: 0099
status: closed
prio: P0
title: 2026-08-13 sess-0813i **ОТГРУЖЕНО 5432a7e6 origin/main, live-M2 в долге** · 🔴 СВЕЖИЙ АПП КЛЕЙМИТСЯ «УДАЛЁН» ЧЕРЕЗ 20 СЕКУНД ПОСЛЕ
created: 2026-08-13
sess: sess-0813i
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-13
closed_commit: 5432a7e6
---
- [x] 2026-08-13 sess-0813i **ОТГРУЖЕНО `5432a7e6` origin/main, live-M2 в долге** · 🔴 СВЕЖИЙ АПП КЛЕЙМИТСЯ «УДАЛЁН» ЧЕРЕЗ 20 СЕКУНД ПОСЛЕ СОЗДАНИЯ, ЗАМОК НА 6 ЧАСОВ (sess-0813i, 2026-08-13, [code+live], origin/main@2a42767a) — `ReapOrphanedAppHostnames` (`backend/internal/api/domains.go:1970-2017`) демоутит `domain_hostnames` в `status='failed', status_reason='app_deleted'`, когда у аппа нет свежего снапшота И сработала одна из двух улик. Вторая улика (`operations.DeleteApp` committed) — доказательство намерения, честная. Первая (строки ~1988-1993) — эвристика «реконсайлер жив», EXISTS по ЛЮБОМУ свежему App-снапшоту в environment БЕЗ фильтра по имени. Возрастного гейта у прохода НЕТ ВООБЩЕ, хотя у соседних backfill-проходов того же файла он есть (`defaultDomainBackfillGrace` 5 мин по `resource_snapshots.first_seen_at`, `domains.go:1611-1626,1710,1835`). Следствие: только что созданный апп, чей снапшот ещё не написан, для эвристики неотличим от удалённого — достаточно, чтобы в том же environment был свеж ЛЮБОЙ другой апп. [live] два свежих тестовых аппа в `agent-sandbox/prod` получили `app_deleted` через ~20с после создания при НУЛЕ строк `DeleteApp` в `operations` и `audit_events`. Усугубление: `hostnameReattachCooldown = 6h` (`domains.go:1062`) — строка не самолечится 6 часов, всё это время карточка показывает `Ready 2/2` и одновременно «Приложение, на которое указывал этот адрес, удалено» (`frontend/lib/i18n/console/messages/apps.ts:99-102`). Бьёт ровно по первому деплою нового юзера. ДЕЙСТВИЕ: возрастной гейт на эвристическую ветку (ветку DeleteApp не трогать — она защищает от осиротевших доменов, память `project_deleteapp_orphans_domain_row_under_live_app`), red-then-green на реальной БД, плюс отдельным шагом — не держать 6-часовой замок на строке, демоутнутой без парного `DeleteApp`.

  **РЕЗУЛЬТАТ sess-0813i (2026-08-13), `5432a7e6` в `origin/main`.** Диагноз подтверждён кодом: возрастного гейта у прохода не было вообще, эвристика срабатывала на типичном (не краевом) случае — новый деплой в environment, где свеж ЛЮБОЙ другой апп. Правки (`backend/internal/api/domains.go`): (1) константа `hostnameReapCreationGrace = defaultDomainBackfillGrace` (`:1950`); (2) эвристическая ветка (`:2042`) требует `dh2.created_at < now() - grace`; ветка с committed `DeleteApp` НЕ тронута — там прямая улика намерения, реальное удаление демоутит домен немедленно; (3) `ReattachOrphanedHostnames` (`:1852`) обходит 6-часовой `hostnameReattachCooldown` для строк со `status_reason='app_deleted'` — такая строка джойнится на ЖИВОЙ App-снапшот, то есть заведомо ошибочна и не должна ждать полдня. Red-then-green дословно: `domain_hostname_reap_test.go:152: status = "failed", want unchanged pending (fresh row is mid-flight, not orphaned)` → PASS после фикса. Новые тесты: `TestReapOrphanedAppHostnamesSkipsFreshlyCreatedRowInBusyEnvironment`, `TestReapOrphanedAppHostnamesDemotesOldRowInBusyEnvironmentAfterGrace` (доказывает, что это ОКНО, а не постоянное исключение), `TestReattachOrphanedHostnamesSkipsCooldownForAppDeletedReason`. Гейты: `gofmt -l` пусто, `go build`/`go vet` чисто, `go test ./...` весь backend-модуль зелёный на реальной БД. **Долг:** live-M2 — прод на `352ad04d` (build #1103), фикс едет билдом #1104; после выкатки убедиться, что свежий апп в `agent-sandbox` больше не получает `app_deleted`, а две уже помеченные строки самоизлечились без 6-часового ожидания.

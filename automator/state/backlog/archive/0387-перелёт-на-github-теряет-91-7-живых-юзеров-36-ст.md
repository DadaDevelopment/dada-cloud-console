---
id: 0387
status: closed
prio: P0
stream: 1
hypothesis: H02
title: Перелёт на GitHub теряет 91.7% живых юзеров: 36 стартов, 3 финиша, 7 человек
created: 2026-08-20
sess: sess-0820c
closed_at: 2026-08-20
closed_commit: 424e6ff4
closed_note: Число «перелёт на GitHub теряет 91.7%» СНЯТО: StartGitAppInstall писался из mount-эффекта префетча (page.tsx:304-316 до правки), то есть счётчик считал показы страницы, а не клики. Префетч удалён, каждый путь навигации берёт СВЕЖИЙ url прямо перед переходом через lib/git-install-connect.ts (иначе устаревший nonce ломает корреляцию FinishGitAppInstall), две ссылки-якоря стали кнопками с тем же текстом и стилем. Ревью нашло второй дефект в самой правке: тест-страж рендерил СВОЮ кнопку, а не страницу, и вернувшийся префетч оставил бы его зелёным — добавлен исходный страж на файл страницы, показан красным (not ok 4) при возврате эффекта и зелёным после. 320 pass / 0 fail прогнано мной, tsc чисто. Честный предел: правка чинит ПРИБОР, а не дверь — реальная конверсия перелёта неизвестна до пересчёта 2026-08-23 (E172).
---
Самая крупная ИЗМЕРЕННАЯ утечка воронки рега→живой-апп на сегодня. Числа получены в этом цикле при закрытии E96 (см. `## ЗАМЕР E96 (sess-0820c, 2026-08-20)` в `experiments.md`), инструментирование — `4cab562b`, долг замера был заведён как `0110`.

Улики [live psql, `state/git-oauth-flight.sql`, служебный актор песочницы `2dd1effb-...` исключён по правилу в шапке самого SQL]:
- `app_install`: **started=36, finished=3, lost=33 (91.7%)**, 7 различных живых акторов.
- `user_authorize`: started=1, finished=1, lost=0 — n=1, вывода не несёт.

Знаменатель настоящий: 30 регистраций за 30 суток, из них 18 — фермерская волна 08-08 (окно 08-08 16:00Z..08-09 06:00Z), исключены. Соседние два источника из E96 утечки НЕ показывают: deploy-CTA `users_with_deploy_intent=5 → reached_create_or_connect=5, clicked_but_never_landed=0`; атрибуция реги `total=4, attributed=4, blind=0`. То есть дыра не размазана по воронке — она сосредоточена ровно на перелёте в GitHub.

Почему P0: это стрим 1 ставки (загрузка без git — обход этой самой двери) и одновременно приоритет №4 иерархии («крупнейшая измеренная утечка»). Семь живых человек начали подключать репозиторий и трое доехали. Всё остальное, что мы чиним в надёжности, стоит ниже двери, в которую не входят.

Чего замер НЕ говорит (не выдумывать):
- где именно теряется — на экране выбора аккаунта GitHub, на самом install-экране, на возврате коллбэком или на нашей странице после возврата. `StartGitAppInstall`/`FinishGitAppInstall` — это две точки, между ними чёрный ящик.
- сколько из 33 — повторные попытки одного человека (7 акторов на 36 стартов = в среднем 5 стартов на человека; это может быть и «упорно долбится», и «мы пишем Start на каждый рендер кнопки»). ПЕРВОЕ, что надо проверить, — не завышен ли знаменатель нашим же логированием: старт обязан писаться на действие человека, а не на показ.

Порядок работы:
1. Проверить семантику `StartGitAppInstall` в коде — на клик или на рендер. Если на рендер, число 91.7% недостоверно и весь пункт пересчитать (класс `project_signup_funnel_denominator_counts_everyone_numerator_one_tab.md`).
2. Разложить 33 потери по акторам и времени: сколько людей не доехали НИ РАЗУ (это и есть настоящая смертность двери), сколько доехали с N-й попытки.
3. Только после этого — гипотеза о причине и правка. Известный сосед: `0270` (лендинг обещает GitHub, дверь даёт только Яндекс-вход) и `project_gitlab_connect_was_unreachable_for_public_repos.md`.

Приёмка (M2): доля живых акторов, доехавших до `FinishGitAppInstall` хотя бы раз, считается тем же запросом до и после правки; здоровое значение — не «100%», а «названо, где именно теряются, и потеря на этом шаге упала».

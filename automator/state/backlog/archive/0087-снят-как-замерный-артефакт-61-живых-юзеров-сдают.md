---
id: 0087
status: closed
prio: P0
hypothesis: H02
title: СНЯТ КАК ЗАМЕРНЫЙ АРТЕФАКТ — 61% ЖИВЫХ ЮЗЕРОВ СДАЮТСЯ, НЕ СДЕЛАВ НИ ОДНОГО ПРОДУКТОВОГО ДЕЙСТВИЯ — граф путей
created: 2026-08-14
sess: sess-0814c
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-14
---
- [x] ❌ СНЯТ КАК ЗАМЕРНЫЙ АРТЕФАКТ — 61% ЖИВЫХ ЮЗЕРОВ СДАЮТСЯ, НЕ СДЕЛАВ НИ ОДНОГО ПРОДУКТОВОГО ДЕЙСТВИЯ (sess-0814c, 2026-08-14, [live psql `audit_events` 30д, n=42], hypothesis: H02, origin/main@5cf7fcbc) — граф путей: ПЕРВОЕ действие = `SessionStart` у 19/33, ТЕРМИНАЛЬНОЕ = `SessionStart` у 20/33 (61%); 9/42 (21%) мёртвые сигнапы (ноль в `audit_events`, `builds`, `git_repos`, `agent_chat_messages`, `feedback`). Второй новичок суток `michaelharlam@yandex.ru` дошёл до `CreateProject`→`ViewApps` пустого списка ДВАЖДЫ и не создал апп. Утечка переехала ВЫШЕ билда/деплоя (их E87 закрыл) — в промежуток «залогинился → пустая консоль → ушёл». Кандидат на цикл: первый экран после регистрации не предлагает создать апп/залить папку, пустой `ViewApps` — тупик. Смотреть `frontend/app/(console)/projects/[projectId]/apps/page.tsx` (пустое состояние) и что видит юзер сразу после `CreateProject`.

  **ЗАЗЕМЛЕНИЕ sess-0814d (2026-08-14, [code], origin/main@26858ea0) — ФОРМУЛИРОВКА ПУНКТА ОПРОВЕРГНУТА НАПОЛОВИНУ, ЧИСЛО НЕ ГОДИТСЯ ДЛЯ ПОСТРОЙКИ.**
  (1) «Пустой экран не предлагает создать апп» — НЕВЕРНО. Пустой проект рисует `EmptyProjectOnramp` (`frontend/components/console/empty-project-onramp.tsx:24-64`): хиро «Разверните приложение из GitHub» + CTA `Подключить репозиторий`, карточка «Загрузить архив» (`frontend/lib/i18n/console/messages/apps.ts:38-39`), чипы шаблонов (`overview.ts:34-40`). Пустой `/apps` рисует hero-грид шаблонов + upload + `EmptyState` с кнопкой «Деплой из Git» (`apps/page.tsx:556-574`). Рычаги ЕСТЬ, и они первого клика.
  (2) Само число `ТЕРМИНАЛЬНОЕ = SessionStart` заземлено и оказалось СОМНИТЕЛЬНЫМ как метрика брошенности: `SessionStart` пишет `auditSessionMiddleware` (`backend/internal/api/audit.go:651-661`) на ПЕРВОМ же авторизованном запросе (окно простоя 30 мин), а страница проекта на успешной загрузке пишет `ViewProject` (`projects.go:598-602`, дедуп 10 мин) и `ViewApps` (`apps.go:497-503`). То есть при нормальной загрузке овервью `SessionStart` терминальным быть НЕ МОЖЕТ — его тут же перекрывает `ViewProject`. Терминальный `SessionStart` означает не «посмотрел онрамп и ушёл», а «овервью не загрузилось вообще».
  (3) Отсюда реальный кандидат в баги: фронт глушит отказы фетча (`projects/[projectId]/page.tsx:68-72` — `.catch(() => [])`), юзер видит пустую страницу БЕЗ ошибки, аудит не пишет ничего. Плюс список проектов `GET /api/v1/projects` не аудируется вовсе, то есть застревание на нём невидимо. Плюс автопровижн дефолтного проекта (`frontend/lib/project-context.tsx:114-124`) при отказе уводит в `setProjects([])`.
  СЛЕДУЮЩИЙ ШАГ (не строить онбординг, пока не разделено): когорты юзеров с терминальным `SessionStart` — (a) вообще нет `CreateProject`, (b) `CreateProject` есть, но `ViewProject`/`ViewApps` нет НИКОГДА (= консоль молча падала у живого человека, это P0-класс), (c) `ViewProject`/`ViewApps` есть в более ранней сессии (= артефакт замера, 61% завышены). Запрос отправлен в audit-агента этого цикла.
  ПОБОЧНО В КОД (дёшево, отдельным пунктом): CTA git-импорта в онрампе не несёт `data-ux` — единственный из трёх рычагов, чей клик не виден ни в `audit_events`, ни в `ux_events` (`empty-project-onramp.tsx:40-45`); шаблоны и upload размечены. Чек-лист проекта (`projects/[projectId]/page.tsx:115-121`) рендерится только при `isEmpty === false` (`:143`) — то есть новичок, которому он и нужен, его не видит НИКОГДА. `OnboardingProvider` (`frontend/components/onboarding/onboarding-provider.tsx`) в шелле есть, но ничем не запускается.
  🔴 ВЕРДИКТ sess-0814d [live psql, аудит-агент цикла]: ПУНКТ ЗАКРЫТ, СТРОИТЬ ПО НЕМУ НЕЛЬЗЯ. Когортный сплит (a)/(b)/(c) показал: 17 из 20 юзеров с терминальным `SessionStart` — это волна ботов 2026-08-08 из telegram-регистрации. Вычесть ботов и внутренние аккаунты — органическая доля терминального `SessionStart` в этом окне ≈ 0. Утечки «залогинился → ушёл» НЕ СУЩЕСТВУЕТ, было раздувание знаменателя. Урок общего действия: любую воронку по `audit_events` за 08-08 и позже считать ТОЛЬКО после вычитания фермерской волны (см. `project_signup_farm_wave_pollutes_funnel`).
  ⚠️ Контр-улика против «продавить шаблоны»: комментарий в `empty-project-onramp.tsx:13-23` фиксирует прод-данные — 10 аппов из GitHub, 2 из архива, 2 из шаблона, и НОЛЬ деплоев шаблонов с момента выката hero-карточки 2026-07-22. Плюс `hypotheses.md`: все вернувшиеся деплоили СВОЙ код, все template-юзеры ушли за день.

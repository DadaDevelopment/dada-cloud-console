---
id: 0246
status: closed
prio: P0
title: P1-LOUD-CONSOLE-NOBODY-OPENS · **ЧАСТЬ «а» ЗАКРЫТА 2026-08-02 sess-0802c (8ae3acc, pushed, ждёт выката):** исход отправки пишется
created: 2026-08-02
sess: sess-0802c
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-02
closed_commit: 8ae3acc
---
- [ ] 🔴 P1-LOUD-CONSOLE-NOBODY-OPENS · **ЧАСТЬ «а» ЗАКРЫТА 2026-08-02 sess-0802c (`8ae3acc`, pushed, ждёт выката):** исход отправки пишется в `app_health_alerts` (`last_send_attempt_at`/`last_send_ok`/`last_send_error`/`last_recipient`/`send_failures`, миграция 082), вопрос «дошло ли письмо» отвечается одним SELECT, а не грепом живых логов. Провал отправки больше НЕ сжигает сутки тишины: откат кулдауна на 15 мин (`appHealthAlertRetryBackoff`) — не мгновенный ретрай и не потерянные сутки. Текст ошибки режется по рунам (байтовый срез кириллицы отбивал бы Postgres невалидным UTF-8 — тот самый UPDATE, ради которого всё пишется). M2: реальная БД с 82 миграциями, оба новых теста PASS (не SKIP), vet+gofmt чисто. **ЧАСТЬ «б» СНЯТА БЕЗ ЭСКАЛАЦИИ:** ответ уже дан policy owner 07-30 в owner-actions — письма как канал РУТИНЫ запрещены, но встроенный алерт про упавший апп это ПРОДУКТ, а не переписка; его не отменяют, его делают доказуемым (что и сделано). Owner-запрос не заводился намеренно. **ОСТАТОК (не блокер части «а»):** первопричина шире почты — владелец `6758ab0b` не заходил в консоль с 2026-07-24 и не имеет ни одной строки `SessionStart`; громкая консоль, которую не открывают, = молчащая. Мерить после выката: `last_send_ok` по его аппам — если true, а юзер всё равно молчит, значит проблема не в доставке, и следующий шаг лежит в возврате юзера, а не в алертах. · **Настоящая причина, найденная вместо предполагаемой [live]:** апп мёртв 8.5 суток, консоль по нему красная, а владелец (`6758ab0b`, орга bruzas.85) не совершал НИ ОДНОГО действия с `2026-07-24 21:19 UTC` и не имеет ни одной строки `SessionStart` за весь инструментированный период. Громкая консоль, которую никто не открывает, функционально = молчащая. Единственный внепродуктовый канал — письмо раз в 24ч на апп, и его доставка НЕ доказуема: `claimAppHealthAlertSlot` штампует `last_sent_at=now()` ДО `Send(...)`, а провал `Send` только логируется без отката (`app_health_watcher.go:486-503`), т.е. в БД «попытались» неотличимо от «доставили». Логи за момент отправки (08-01 16:40 и 20:50 UTC) уже потеряны — поды бэкенда прожили 27 минут после свежего роллаута. Два шага, оба дешёвые: (а) писать ИСХОД отправки в `app_health_alerts` (`sent_ok`/`last_error`), чтобы вопрос «дошло ли» отвечался запросом, а не грепом живых логов; (б) решить с owner канал — почта про упавший апп это продукт или тоже «долбёж»? см. owner-actions.

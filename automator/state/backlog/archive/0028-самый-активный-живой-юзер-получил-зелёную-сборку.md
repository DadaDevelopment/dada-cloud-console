---
id: 0028
status: closed
prio: P0
stream: 2
hypothesis: H02
title: САМЫЙ АКТИВНЫЙ ЖИВОЙ ЮЗЕР ПОЛУЧИЛ ЗЕЛЁНУЮ СБОРКУ И МЁРТВЫЙ АПП, И ПРОДУКТ НЕ СКАЗАЛ ЕМУ НИ СЛОВА
created: 2026-08-16
sess: sess-0816b
section: Backlog (execution-bet)
closed_at: 2026-08-16
closed_commit: 406edfef
---
- [x] ЗАКРЫТ sess-0816b 2026-08-16 · 🔴 САМЫЙ АКТИВНЫЙ ЖИВОЙ ЮЗЕР ПОЛУЧИЛ ЗЕЛЁНУЮ СБОРКУ И МЁРТВЫЙ АПП, И ПРОДУКТ НЕ СКАЗАЛ ЕМУ НИ СЛОВА (sess-0816a, 2026-08-16, [live psql `audit_events`/`env_vars` + live kubectl], hypothesis: H02, поток 2, origin/main@8d1d3f4c) — `bruzas.85@mail.ru`, апп `sevarateambot`, ns `tvkassistantbot-prod`, env `233f9018-8aab-470b-95b8-751efcd707a0`. Полная лента его вечера 08-15: `20:51 ViewApps` → `21:00:46 ConnectGitRepo` + `TriggerBuild` → `21:01:06 ViewBuildLogs` → `21:02:11 BuildFinished success` + `CreateApp success` + `SendBuildNotification success` → `22:01:21 ViewBuildLogs` (второй заход, через час) → `22:01:26 ViewApps` → **тишина навсегда**. Под всё это время в `CrashLoopBackOff`, 53+ рестарта, к моменту замера ~7 часов; в логах — не найден `TELEGRAM_API_TOKEN`.
  **Это НЕ стёртая нами переменная — проверено:** в `env_vars` по этому environment_id **ноль строк вообще** (не только по этому ключу), секрета `sevarateambot-env` в неймспейсе НЕТ (есть только tls/registry/nexus/identity-credentials), у `sevarateambot-deploy` пусты И `env`, И `envFrom`. Юзер переменную никогда не задавал.
  **Наш дефект в другом, и он ровно из потока 2:** сборка зелёная, `SendBuildNotification success`, `CreateApp success` — все наши сигналы говорят «получилось», а приложение не живёт ни секунды. Терминальное действие юзера — `ViewBuildLogs`, то есть он ПОШЁЛ ИСКАТЬ причину, и логи сборки (успешной!) ему её показать не могли: причина в рантайме, а не в билде. Он сходил туда дважды с интервалом в час и ушёл. Это форма [[project_diagnosis_without_a_lever_leaves_user_down]], но злее: тут нет даже диагноза.
  **Что чинить (не «алерт вообще», а конкретный стык):** (1) крэшлуп сразу после ПЕРВОГО деплоя обязан рождать видимое в консоли состояние с причиной из логов пода, а не оставлять апп зелёным по последнему билду; (2) `ViewBuildLogs` на успешной сборке при мёртвом поде обязан вести к логам РАНТАЙМА, а не только билда — юзер кликнул ровно туда, где ответа нет; (3) отсутствующая обязательная env-переменная — самый частый класс, его видно из логов пода регэкспом, и он же кандидат №1 для auto-fix (поток 3): «твоему боту нужен `TELEGRAM_API_TOKEN`, вот поле».
  ⛔ Проект НЕ трогать (bruzas — участник churn-интервью, [[project_bruzas_churn_interview]]); письма юзеру запрещены owner'ом 07-30 — чинить в UI, а не перепиской.
  **ЗАКРЫТО sess-0816b (2026-08-16), все три подпункта:** (1) и (3) отгружены параллельной сессией в `406edfef` — классификатор `missing_env_var` + баннер с именем переменной и ссылкой на редактор env. (2) отгружен этим циклом `7e9f1cdb` — страница успешной сборки читает фазу `CrashLoop`/crash-алерт и показывает янтарную панель с причиной и ссылкой на рантайм-логи вместо «вот-вот поднимется». **M2, живой пруф, не билд-грин:** прод на `406edfef` (все 6 деплойментов), и в проде `app_health_alerts` по `sevarateambot` несёт `cause_kind='missing_env_var'`, `cause_line='TELEGRAM_API_TOKEN'`, `last_seen_at=2026-08-16 02:19:29Z` — платформа НАЗВАЛА переменную по живому аппу живого юзера. Единственная строка с непустым `cause_kind` за 12ч, то есть классификатор не шумит.
  Честный предел: панель сборки (`7e9f1cdb`) в прод ещё не доехала и вживую не наблюдалась — доказано только то, что причина теперь есть в данных и рендерится баннером аппа. Юзер в консоль после 08-15 22:01 не возвращался, так что «увидел и починил» не измерено ничем; это ведёт эксперимент E152.

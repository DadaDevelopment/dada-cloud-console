---
id: 0014
status: closed
prio: P1
hypothesis: H02
title: 75a6e261 · 🟠 ДЕПЛОЙ НЕ ОСТАВЛЯЛ СЛЕДА УСПЕХА НА СТРАНИЦЕ АППА
created: 2026-08-16
sess: sess-0816i
section: Backlog (execution-bet)
closed_at: 2026-08-16
closed_commit: 75a6e261
---
- [x] ЗАКРЫТ sess-0816i 2026-08-16 · `75a6e261` · 🟠 ДЕПЛОЙ НЕ ОСТАВЛЯЛ СЛЕДА УСПЕХА НА СТРАНИЦЕ АППА (sess-0816h, ПЕРЕЗАЗЕМЛЕНО sess-0816i 2026-08-16, [live psql `audit_events`+`operations`], hypothesis: H02, origin/main@a2bcfa38).
  🔴 ИСХОДНАЯ ФОРМУЛИРОВКА БЫЛА НЕВЕРНА. «Отправил один и тот же деплой ТРИ раза» — артефакт счёта строк аудита. Правда по operation_id: 16:44:04 операция `a8843ac9` (payload `{port,image,app_name,framework}`) — это АВТОМАТИЧЕСКИЙ деплой платформы после `BuildFinished(success)`; 16:45:29 операция `86bcd28c` (payload `{image,app_name}`, другой код-путь) — единственный РУЧНОЙ деплой юзера; 16:46:22 — терминальный вердикт ТОЙ ЖЕ `86bcd28c`, а не третья попытка. Аудит пишет намерение и вердикт двумя строками с одним operation_id (см. память `project_audit_intent_verdict_contract`), поэтому считать нажатия по строкам нельзя — только по distinct operation_id.
  ✅ ЧТО ОСТАЁТСЯ ПРАВДОЙ И ЗАЧЕМ ЧИНИТЬ: `artempro2022@yandex.ru` через 85 секунд ВРУЧНУЮ раскатал РОВНО ТОТ ЖЕ digest `sha256:ae4ace15…`, который платформа раскатала минутой раньше. Значит платформа не показала ему, что деплой уже произошёл. Механизм: `frontend/app/(console)/projects/[projectId]/apps/[appName]/page.tsx:177-192` (`handleImageUpdate`) при успехе уносит на `/operations?highlight=opId`, а состояние деплоя на самой странице аппа не отрисовано (`deploymentsApi.list` грузится в `setDeployments` на строках 165-169, но текущий образ и время раскатки юзеру не показываются).
  **СДЕЛАНО sess-0816i, `75a6e261`.** Первопричина оказалась НЕ в кнопке страницы аппа, а в отставании `is_current`: `ListDeployments` выводит его из реально бегущего образа (`resource_snapshots`, `backend/internal/api/deployments.go:188`), поэтому принятый деплой минутами не помечен ничем. Доказано таблицей `deployments`: 16:45:29 `trigger=rollback build_id=2d5dac8f` против 16:44:04 `trigger=manual build_id=2d5dac8f` — тот же билд, тот же digest. Юзер действовал СО СТРАНИЦЫ ДЕПЛОЕВ, «откатываясь» на самую свежую строку, которую в этот момент раскатывали, потому что у неё стояла кнопка «Откатить к этой».
  Три поверхности: (1) `frontend/components/deploy/app-current-deploy-card.tsx` — страница аппа показывает текущий/раскатывающийся образ и его возраст, а действия вместо молчаливого редиректа на `/operations` оставляют зелёную плашку со ссылкой на операцию; (2) фид деплоев вешает бейдж «Раскатывается» на свежую непомеченную строку; (3) у неё же подпись кнопки становится «Раскатать заново» — серверный вызов не тронут, врало только слово. Отсутствие данных нигде не рендерится как вердикт.
  Бейдж ограничен окном 15 минут (`isPendingDeployment`): без окна незалетевший деплой крутил бы спиннер «Раскатывается» вечно. Пруф двухполюсный: RED — снял окно, 2 из 9 тестов упали; GREEN — 298/298 фронт-тестов, `tsc --noEmit` чисто, eslint без новых замечаний (одно pre-existing `exhaustive-deps` сверено с `git show HEAD:`).
  🔴 НАЙДЕНО ПОПУТНО, НЕ ЧИНИЛОСЬ (правило одного яка, заведено ниже): поле `trigger` врёт — автодеплой платформы пишется как `manual`, повтор самого свежего образа — как `rollback`.

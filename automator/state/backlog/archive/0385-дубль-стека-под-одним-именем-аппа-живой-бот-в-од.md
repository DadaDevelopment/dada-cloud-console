---
id: 0385
status: closed
prio: P0
stream: 2
hypothesis: H02
title: Дубль стека под одним именем аппа: живой бот в одном проекте, вечный крашлуп в другом
created: 2026-08-20
sess: sess-0820c
closed_at: 2026-08-20
closed_commit: 7fff770d
closed_note: ConnectGitRepo отказывает на репо+app_name уже занятых в другом проекте орги (409 repo_linked_to_other_project); письмо алерта несёт проект. Заземление пункта частично ОПРОВЕРГНУТО: admin_overview.go и фронт админки УЖЕ несли project_name на всех списках, ключ app_health_alerts УЖЕ (namespace, app_name) [mig 047]; алерт по зомби был ВЕРНЫМ, а не ложным — врало только письмо, называвшее имя аппа без проекта. Живые дубли не тронуты.
---
Заземление [live 2026-08-20, kubectl + psql]:
- `sevarabot-prod/sevarateambot-deploy` — **1/1 Running**, под живёт 3ч41м без рестартов, в контейнере `env=TELEGRAM_API_TOKEN,PYTHONUNBUFFERED,PYTHONPATH,UVICORN_LOG_CONFIG,SERVICE_NAME`. Это РАБОЧИЙ бот юзера bruzas.85@mail.ru.
- `tvkassistantbot-prod/sevarateambot-deploy` — **0/1 CrashLoopBackOff**, 19 рестартов за 75 минут, в контейнере НИ ОДНОГО `env` и НИ ОДНОГО `envFrom`. Это дубль того же имени аппа в другом проекте.
- `env_vars`: ровно одна строка `TELEGRAM_API_TOKEN`, `scope=runtime`, `environment_id=7a84b652-2c06-40fb-96b1-a14305b6b024` → namespace `sevarabot-prod`, проект `sevarabot`. То есть переменная лежит там, где живой апп; во втором проекте её нет и быть не должно.
- `deployments` по `app_name='sevarateambot'` чередует ДВА разных репозитория образов: `nexus.dada-tuda.ru/sevarabot/sevarateambot@…` и `nexus.dada-tuda.ru/tvkassistantbot/sevarateambot@…` — по строке на каждый проект, вперемешку по времени (5 строк за 08-19).

Почему это стоит P0, хотя юзер НЕ лежит:
1. **Наши приборы врут.** `/api/v1/admin/overview` показывает `sevarateambot` в `not_ready` как поломку живого юзера, а `app_health_alerts` завела строку `cause_kind='missing_env_var'`, `cause_line='TELEGRAM_API_TOKEN'`, `last_send_ok=t` — то есть юзеру УШЛО письмо про поломку приложения, которое у него работает. Ложная тревога дороже молчания: она учит игнорировать наши письма.
2. **Панель не различает два стека одного имени.** Ключ `(namespace, app_name)` в кластере разный, но человеку и сборщику состояния оба показываются как «апп sevarateambot юзера bruzas».
3. Зомби жжёт ресурсы и место в очереди сборок бесконечно.

Работа:
- Сторож здоровья и панель обязаны показывать namespace/проект рядом с именем аппа, иначе дубли неотличимы (`backend/internal/api/admin_overview.go`, письмо в `internal/notify/notify.go`).
- Понять, КАК родился второй стек: проверить, не создаёт ли `DeployStack`/`CreateApp` стек в чужом проекте при совпадении `app_name` (см. память `project_deploystack_app_name_mismatch_creates_duplicate_stack`), и закрыть саму возможность.
- Дать в продукте рычаг «это дубль, снести» — сейчас юзер не может отличить свой живой апп от зомби.

M2: панель показывает проект/namespace для обоих `sevarateambot`; письмо о поломке не уходит на апп, у которого в другом проекте живой одноимённый; зомби-стек снят и не воскресает.

Честный предел этого заземления: НЕ выяснено, кто и когда создал второй стек — в `audit_events` по `resource_name='sevarateambot'` строки за 08-19 есть (билды/деплои), но `CreateApp` второго стека в окне не найден; нужен отдельный запрос по обоим `environment_id`.

## Подтверждение независимой трассировкой (sess-0820c)

Трассировщик пришёл к тому же выводу отдельным путём и добил конкурирующие гипотезы:

- `git_repos` содержит ДВЕ строки `app_name='sevarateambot', repo_full_name='alexas85/SevaraTeamBot'` — под `tvkassistantbot` (2026-08-15 21:00:46) и под `sevarabot` (2026-08-16 11:50:58). Один и тот же репозиторий подключён дважды в разные проекты одного владельца, без единого предупреждения о коллизии.
- В `argo-infra` (ветка `console-migration`) одновременно живут два дерева: `clusters/beget-prod/projects/sevarabot/environments/prod/apps/sevarateambot/` и `.../projects/tvkassistantbot/environments/prod/apps/sevarateambot/`.
- Доставка env НЕ сломана: `env_vars.created_at 12:00:09.809` ↔ operation `932932ba` (`Committed`, git `7fbec57c`); `updated_at 2026-08-17 23:23:47.838` ↔ operation `7d638c27` (`Committed`, git `eb0bcf4a`).
- Опровергнуты: merge-allowlist (`extraEnv` есть в `ownedCommonKeys`, `gitops-agent/internal/renderer/values_merge.go:20-33`) и фильтр области видимости (`scope IN ('runtime','both')`, `gitops-agent/internal/worker/envvars.go:39`).

Сканирование всей базы на этот класс: `select app_name, count(distinct environment_id) from deployments join environments ... having count(distinct environment_id) > 1` даёт ровно два имени — `sevarateambot` (2 окружения, юзерский) и `dada-development-site` (3 окружения, наш внутренний). То есть класс редкий, но живой и уже стоил одному юзеру недели неработающего бота плюс ложного письма от нас.

Открытый вопрос перед сносом близнеца: куда смотрит вебхук GitHub App у `alexas85/SevaraTeamBot`. Два живых поллера на одном токене Telegram дадут 409 на `getUpdates`, так что «настроить оба» — не решение. Владельцу проекта решать, какой апп жив; наша часть — не врать панелью и предупреждать на `ConnectGitRepo`/`CreateApp`.

---
id: 0375
status: closed
prio: P0
stream: 3
hypothesis: H08
title: Доставка 83af9559 (ENOSPC-вердикт) не подтверждена — снять пруф образа и cause_kind у fonbet-value
created: 2026-08-19
sess: sess-automator
closed_at: 2026-08-19
closed_commit: 83af9559
closed_note: ЗАМЕРЕНО [live]: образ прода f44dd84d несёт 83af9559; app_health_alerts fonbet-value cause_kind=platform_storage, cause_line="OSError: [Errno 28] No space left on device", last_send_ok=t 2026-08-19 20:59Z — вердикт доехал и письмо ушло. E165 measured.
---
Коммит на origin/main (проверено: `git merge-base --is-ancestor 83af9559 origin/main` = YES).
Пруф ДОСТАВКИ не снят.

Что не сработало в цикле 08-19 (никаких вердиктов из этого не выводить — отсутствие улики не улика):
- Jenkins MCP отвалился посреди цикла (server disconnected), сборку посмотреть нечем.
- Сабагент проверки доставки умер: `403 Request not allowed`.
- kubectl висит: kubeconfig несёт `proxy-url: http://127.0.0.1:8899`, прокси ПОДНЯТ (nc ok,
  вторая копия дала Errno 48), но `get ns` не отдал ни строки за 60с+; прямой TCP на
  83.222.27.62:26443 закрыт (`nc` DEAD). См. memory
  project_kubeconfig_proxy_url_makes_dead_proxy_look_like_dead_cluster и
  project_vpn_tunnel_eats_tcp443_while_icmp_and_dns_live.

Снять два факта:
1. Образ backend в проде несёт 83af9559 (или более поздний коммит).
2. live psql `app_health_alerts`, namespace `artemmendeleev%`, апп `fonbet-value`:
   ожидается `cause_kind=platform_storage` + `cause` со строкой `No space left on device`;
   до доставки там NULL пятые сутки.

Пока не снято — E165 в experiments.md висит без замера после доставки.

## Уточнение по сети (08-19, конец цикла)

Не «прод лежит» — прод ЖИВ. Разошлись два полюса:
- ВНЕШНЕ [live, check-host.net 6 узлов]: `console.dada-tuda.ru` ALIVE 6/6 (307),
  `dada-tuda.ru` ALIVE 6/6 (301), `id.dada-tuda.ru` 5/6 (302).
- С МАШИНЫ РУТИНЫ [live]: `curl` по трём эндпоинтам console = `http=000`;
  `kubectl` = `dial tcp 83.222.27.62:26443: i/o timeout`.

То есть сломан локальный egress (TCP:443 и :26443), а не платформа. Подпись совпадает с
memory project_vpn_tunnel_eats_tcp443_while_icmp_and_dns_live. Важно: `git push` по 443
в НАЧАЛЕ этого же цикла прошёл — значит туннель отвалился в течение цикла, и «сеть работала
20 минут назад» ничего не гарантирует (та же ловушка, что с `git fetch`).

Порядок в следующем цикле: СНАЧАЛА починить/поднять путь (`ensure-proxy.sh`, проверить `nc -z`
по трём хостам), только ПОТОМ снимать пруф доставки. Не начинать с kubectl — он в этой ситуации
выглядит как мёртвый кластер.

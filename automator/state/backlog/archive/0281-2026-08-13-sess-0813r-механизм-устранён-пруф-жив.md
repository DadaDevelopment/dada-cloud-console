---
id: 0281
status: closed
prio: P0
title: 2026-08-13 sess-0813r **МЕХАНИЗМ УСТРАНЁН, ПРУФ ЖИВОЙ** · 🔴 ПОДЫ argocd-prod СИДЯТ НА ПРИОРИТЕТЕ 0
created: 2026-08-13
sess: sess-0813r
section: Открытые долги (не терять)
closed_at: 2026-08-13
closed_commit: edcea950
---
- [x] 2026-08-13 sess-0813r **МЕХАНИЗМ УСТРАНЁН, ПРУФ ЖИВОЙ** · 🔴 ПОДЫ `argocd-prod` СИДЯТ НА ПРИОРИТЕТЕ 0 — НАШ ЖЕ CI МОЖЕТ ВЫТОЛКНУТЬ ПЛАТФОРМУ С НОДЫ (sess-0813q, 2026-08-13, [live kubectl]) — за десять минут агент Jenkins (класс `critical`) дважды вытеснил `pg-shard-0-postgresql-0` (приоритет 0). Заткнуто точечно: `postgres-shard-0` получил `primary.priorityClassName: postgresql` (`edcea950`) — доставка подтверждена в 13:45:08Z. Дыра осталась общая: у остальных подов `argocd-prod` приоритет 0, то есть любой всплеск сборок = кандидаты на вытеснение, включая консоль. Правильный рычаг — приоритетный класс платформы по умолчанию для namespace + podAntiAffinity CI-агентов к нодам с платформенными stateful-подами (память `project_ci_agent_shared_node_disk_with_platform_postgres`).

  **РЕЗУЛЬТАТ sess-0813r (2026-08-13 14:20-15:00Z), E94.** Чинили не «кому добавить класс», а само право вытеснения. Новые классы в `dada-argo@2da5dc18` (`infrastructure/priority-classes`): `ci-agent` и `platform`, оба 100000, оба `preemptionPolicy: Never` — очередь сохраняется, право убивать соседей исчезает. `jenkins-pipelines@6e85d2f` переводит ОБА шаблона агента (`vars/kubePodTemplate.groovy:112,253`) с `critical` на `ci-agent`; порядок соблюдён — класс раскатан и проверен живьём ДО коммита шаблона, иначе под агента отвергается на admission и CI встаёт целиком. `dada-cloud-console@1429b0b0`+`1162eb46` даёт чарту `global.priorityClassName: platform` во всех деплойментах, включая redis и migration-job.
  **M2 [live]:** `kubectl get priorityclass ci-agent platform` -> обе `100000 Never`. Через минуту после пуша Argo раскатал чарт, и новые поды поехали с классом: `backend-645db74d95`, `build-agent-57d5f79789`, `frontend-7949bddf78`, `gateway-645c445d55`, `gitops-agent-578f887dd8`, `redis-7cd98f46c7` — все `platform` (старые реплики `<none>` доживают роллаут). Пруф на агенте — первый под, поднявшийся после `6e85d2f`.
  **ПОГОНЯ ЗА ПРУФОМ ВСКРЫЛА ПРОМАХ:** под агента сборки #1119 поднялся с `priorityClassName: critical`, то есть правка `jenkins-pipelines@6e85d2f` его не касалась вовсе. Под `dada-cloud-console-agent-*` объявлен ИНЛАЙНОМ в `dada-cloud/Jenkinsfile:138`, а не в общей библиотеке — библиотека рулит агентами остальных репозиториев. Закрыто `0650dd30` (`critical` -> `ci-agent` в самом Jenkinsfile). **ПРУФ ЖИВОЙ:** под сборки #1120 `dada-cloud-console-agent-2bpkz-2hlcg` (создан 14:58:39Z, `runUrl` = job/.../main/1120/) поднялся с `priorityClassName: ci-agent`, `.spec.priority` = 100000 — очередь та же, право вытеснения отобрано. Урок: правка общей библиотеки не доказана, пока не увиден живой под ИМЕННО того пайплайна.
  ОСТАТОК: `portainer-agent` класса не получил — прод ставит его отдельным чартом argo-infra, эта установка его не рендерит.

---
id: 0443
status: closed
prio: P1
stream: 5
title: Строка pending без yk_payment_id снова появилась 08-19 -- свипер 3d6379f9 её не закрыл
created: 2026-08-21
sess: sess-0821d
closed_at: 2026-08-21
closed_note: ЛОЖНАЯ ТРЕВОГА, премиса моя и она неверна. Строка 25f07e96 (2026-08-19 21:01:07) -- НЕ мёртвая кнопка чекаута, а счёт юрлица: payer_inn=7807402712, invoice_number=INV-2026-00002 [live psql cloud-console@pg-shard-0]. У счёта юрлица yk_payment_id пустой ПО ДИЗАЙНУ -- оплата идёт переводом, платёж в ЮKassa не создаётся, свипер такую строку добивать не должен и правильно не трогает. Ровно этот класс ложняка уже закрыт коммитом 7d16147a («Счёт юрлица не должен поднимать алерт про мёртвую кнопку оплаты»). Карточных строк, застрявших в pending, в проде НЕТ ни одной за 30 дней. Урок: has_yk=f сам по себе не сигнатура -- сигнатуру даёт has_yk=f И payer_inn IS NULL.
---
Замечено при разборе денег sess-0821d [live psql `payments`, окно 30 дней].

Полная выборка платежей за 30д показывает строку:
`dada | business | pending | has_yk=f | 2026-08-19 21:01:07`.

Это ровно та сигнатура, которую закрывал `3d6379f9` (fail-tx + часовой свипер):
строка `pending` живёт без `yk_payment_id`, то есть платёж в ЮKassa так и не создан,
а у нас висит незакрытое намерение. Строке двое суток -- часовой свипер обязан был
её добить и не добил.

Заземлить прежде чем чинить:
1. Свипер вообще запускается в проде? (флаг/лог/аудит его собственного действия --
   мерить по счётчику своего действия, а не по «код доехал»: память
   `project_shipped_lever_can_be_structurally_unreachable`).
2. Если запускается -- почему пропустил именно эту строку: окно возраста, фильтр по
   org_id, или условие на `yk_payment_id IS NULL` не то.

Смягчающее: org_id=`dada`, то есть пострадал внутренний аккаунт, не чужой человек.
Поэтому P1, а не P0. Но сигнатура та же, что съела первого чужого покупателя, и
знать, что свипер её не берёт, надо ДО следующего чужого платежа.

Соседний факт из того же замера (не дефект, а улика в плюс): внутренние попытки
08-18 13:24 имеют `has_yk=t` -- значит после фиксов чекаут платежи в ЮKassa
действительно создаёт.

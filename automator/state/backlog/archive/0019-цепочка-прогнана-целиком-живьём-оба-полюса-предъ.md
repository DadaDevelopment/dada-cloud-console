---
id: 0019
status: closed
prio: P0
hypothesis: H08
title: **ЦЕПОЧКА ПРОГНАНА ЦЕЛИКОМ ЖИВЬЁМ, ОБА ПОЛЮСА ПРЕДЪЯВЛЕНЫ**
created: 2026-08-16
sess: sess-0816c
section: Backlog (execution-bet)
closed_at: 2026-08-16
closed_commit: 406edfef
---
- [x] ЗАКРЫТ sess-0816c 2026-08-16 · **ЦЕПОЧКА ПРОГНАНА ЦЕЛИКОМ ЖИВЬЁМ, ОБА ПОЛЮСА ПРЕДЪЯВЛЕНЫ** [live, `agent-sandbox`/`envprobe0816`, см. E153]: zip → сборка `success` (66с) → под в `CrashLoopBackOff` (3м32с от загрузки) → платформа САМА назвала причину `cause_kind=missing_env_var`, `cause_line=PROBE_API_TOKEN` (≈5 мин) → нажат рычаг `PUT .../env/PROBE_API_TOKEN` (операция `DeployImageVersion`) → **апп жив `1/1 Running` через 2м00с**. Рычаг НЕ декоративный, вопреки опасению из [[project_shipped_lever_can_be_structurally_unreachable]]. Пробник удалён в этом же цикле (A5), остались штатные надгробия (домен `status_reason=app_deleted`, health-строка с эпохой). 🔴 НАЙДЕНА ДЫРА тем же прогоном → отдельный пункт ниже (диагноз не липкий).
  ~~Исходная формулировка:~~ 🔴 ЦЕПОЧКА «КРАШЛУП → ПРИЧИНА → РЫЧАГ → ЖИВОЙ АПП» НИКОГДА НЕ НАБЛЮДАЛАСЬ ЦЕЛИКОМ (sess-0816c, 2026-08-16, hypothesis: H08, потоки 1+3, origin/main@7e9f1cdb) — за два цикла отгружены `406edfef` (классификатор `missing_env_var` + баннер с именем переменной и ссылкой в редактор env) и `7e9f1cdb` (страница успешной сборки показывает крашлуп вместо «вот-вот поднимется»). Доказано ровно одно: причина ЕСТЬ в данных по живому аппу (`app_health_alerts.cause_kind='missing_env_var'` у `sevarateambot`). НЕ доказано ничего из остального: юзер в консоль не вернулся, и «задал переменную → апп поднялся» не наблюдалось ни разу — то есть рычаг может быть декоративным ровно как в [[project_shipped_lever_can_be_structurally_unreachable]]. Заземление [code]: сохранение env-переменной ставит операцию редеплоя (`backend/internal/api/envvars.go:50` `queueEnvApply`), значит механизм на бумаге есть. Задача цикла: прогнать цепочку живьём в `agent-sandbox` на аппе `envprobe0816` (загрузка АРХИВОМ, то есть заодно поток 1) и предъявить оба полюса — до правки апп мёртв и причина названа, после правки апп жив.

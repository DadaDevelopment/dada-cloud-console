---
id: 0097
status: closed
prio: P0
hypothesis: H02
title: SEO: В ПОИСКЕ 11 URL ИЗ ~66, EN-ЗЕРКАЛО ВЫЛЕТАЕТ КАК LOW_QUALITY
created: 2026-08-13
sess: sess-0813m
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-13
closed_commit: 92f8f6ab
---
- [x] 🔴 SEO: В ПОИСКЕ 11 URL ИЗ ~66, EN-ЗЕРКАЛО ВЫЛЕТАЕТ КАК LOW_QUALITY (sess-0813m, 2026-08-13, [live Webmaster API], hypothesis: H02) — `search-urls/in-search/samples` отдаёт ровно 11 адресов при 33 RU-маршрутах в `frontend/app/sitemap.ts` + их EN-зеркале; история `in-search`: пик 16 (07-29/30) -> 11 (08-12). События исключения: `LOW_QUALITY` у `/en/hosting-django|fastapi|flask|streamlit|vk-bot`, `/en/analog-vercel|railway|render|fly-io`, `/en/migrate-vercel`, `/en/deploy-without-git`, `/developer/mcp-ai-agents`; `analog-netlify` = NOTHING_FOUND. Все проверенные URL живые (200), то есть это вердикт качества, а не 404. При этом запросы РЕАЛЬНЫЕ и наши: 13 запросов/7д, «аналог vercel в россии», «аналоги railway в россии», «а есть какой то то российских хост на подобии railway» (15 показов, 3 клика). ДЕЙСТВИЕ: снять причину исключения (тонкое EN-зеркало тянет качество хоста) и вернуть страницы в индекс.

  **РЕЗУЛЬТАТ sess-0813m (2026-08-13 11:00-11:45Z). ЗАЯВЛЕННАЯ ПРИЧИНА ОПРОВЕРГНУТА [live Webmaster API + code].** «Тонкое EN-зеркало» не существует: `frontend/lib/i18n/context.tsx` `localeFromPath` отдаёт `en` для `/en/*`, страницы рендерят настоящий английский на сервере, у каждой свои `metadata` (title/description/canonical/hreflang/robots) — головы корректны на всех проверенных URL. Исключённые страницы несут 767-1068 слов, иногда БОЛЬШЕ индексируемых; пересечение по словам с `/analog-vercel` (40-63%) не разделяет индексируемые и исключённые. Обход здоров: `indexing/samples` — 200-е до 08-11, `recrawl/quota` нетронута 150/150. Вывод: `LOW_QUALITY` — документированная оценка СПРОСА Яндексом (автоперепроверка, ~2 недели), а не технический дефект. Рекомендацию ресёрчера `noindex` на `/en/*` ОТКЛОНИЛ (M0: причина не подтверждена первоисточником).
  **ЧТО НАШЁЛ ВМЕСТО ЭТОГО (реальная измеренная утечка).** `query-analytics/list`: 38 запросов, 58 показов, 8 кликов. Весь платёжный кластер («оплатить vercel» 45, «оплатить railway из россии» 15, «оплатить railway» 10, «оплата рейлвей» 7, «оплата heroku для россиян» 5) стоит на позициях 9-22 с НУЛЁМ кликов, все 8 кликов дают «аналог»-запросы с позиций 1-6. На `/analog-vercel` и `/analog-railway` приходит платёжный интент, а в теле страниц про оплату не было ни слова (у vercel-страницы одна внутренняя ссылка `/migrate-vercel`, у railway-страницы ноль). СДЕЛАНО `92f8f6ab`: блок `PayNote` под героем обеих страниц RU+EN (`frontend/components/marketing/sections.tsx`, `frontend/lib/i18n/dict.ts`) + честный FAQ «Как оплатить Railway из России?» (кормит `FaqJsonLd` → сниппет). Vercel → профильная `/oplatit-vercel-iz-rossii`, Railway → `/pricing`.
  **ПОТОЛОК КАНАЛА.** Весь измеренный спрос по 38 запросам ~120 показов/мес — расширять число pSEO-страниц под это НЕ стоит. Перекраул `/oplatit-vercel-iz-rossii` и `/rabotaet-li-vercel-v-rossii` — только ПОСЛЕ выкатки контента. Замер позиций через `query-analytics` не раньше чем через 2 недели.
  ОСТАТОК В БЕКЛОГ: отдельная страница «оплатить railway из россии» (спрос ~36 показов) — делать, только если PayNote-эксперимент покажет клики.

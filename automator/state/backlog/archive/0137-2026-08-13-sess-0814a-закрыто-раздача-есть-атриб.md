---
id: 0137
status: closed
prio: P0
title: 2026-08-13 sess-0814a **ЗАКРЫТО: РАЗДАЧА ЕСТЬ, АТРИБУЦИЯ ДОКАЗАНА ЖИВЬЁМ** · Пункт (1) закрыт НЕ пробником, а реальными прогонами
created: 2026-08-13
sess: sess-0814a
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-13
closed_commit: dee646ef
---
- [x] 2026-08-13 sess-0814a **ЗАКРЫТО: РАЗДАЧА ЕСТЬ, АТРИБУЦИЯ ДОКАЗАНА ЖИВЬЁМ** · Пункт (1) закрыт НЕ пробником, а реальными прогонами владельца [live psql `audit_events`]: `UploadSourceArchive|Build|genagent` 08-13 21:51/22:08/22:24Z несут `client_claimed=cli` + `agent_session_claimed=CLAUDECODE`, каждая → `success`-билд → `DeployImageVersion`. Пункт (3) закрыт: релиз `cli-v0.1.0` (`dee646ef`) с бинарями darwin arm64/amd64 + linux amd64/arm64 и `checksums.txt`; `cli/install.sh` переписан на скачивание+sha256 с фолбэком на локальную сборку; `cli/release.sh` — кросс-компиляция; `version.Version` теперь var под `-ldflags -X`. Живая проверка на машине владельца 22:43Z: `curl -fsSL .../cli/install.sh | sh` → `Installed: /Users/alex/.local/bin/ddc (prebuilt release)`, `ddc --help` отвечает. Нюанс, решённый агентом: репо шарит релизы с консолью (`v0.3.0` = Latest), поэтому `releases/latest` не годится — install.sh резолвит последний тег `cli-v*` через GitHub API, релиз опубликован с `--latest=false`. Пункт (2) (браузерный `ddc login`) снят как неактуальный: владелец УЖЕ логинился и деплоил — `~/.config/ddc/token.json` живой (refresh есть), `targets.json` несёт три цели (`tree`, `genagent`, sandbox-проба). Отчёт владельцу — в owner-actions. Исходная формулировка: 🔴 OWNER-ЭСКАЛАЦИЯ 2026-08-13 (через ментора): ДОВЕСТИ `ddc deploy` ДО ПРОДА — owner недоволен, что заказ не в его руках («эффекта не видно»). Взять в БЛИЖАЙШИЙ цикл, наравне с живыми инцидентами. Пункт (2) — браузерный логин — НЕ блокер остального: доставь (1) и (3), а прогон логина положи owner'у в owner-actions одной строкой «готово к твоему прогону: curl ... | sh && ddc deploy». M2 цикла = owner физически может выполнить команду на своей машине. Исходный пункт (sess-0813f) — осталось: (1) доставить мидлварь `client_claimed`/`agent_session_claimed` (`backend/internal/api/audit.go`) на прод и убедиться живьём, что строка `UploadSourceArchive` от CLI несёт `client_claimed=cli` — без этого H-CLI (<15% первых деплоев и 0 агентных вызовов за 30д) не измеряется вообще; (2) прогнать браузерный `ddc login` целиком — это делает владелец или человек, агент креды в IdP не вводит; проверить, что второй `ddc deploy` идёт без логина на кэшированном токене и что `offline_access` реально приехал в `scope` выданного токена (device endpoint принимает любой scope-текст, 200 там ничего не доказывает); (3) `install.sh` сейчас собирает локально, публикации бинаря нет — до раздачи юзерам нужен релиз.

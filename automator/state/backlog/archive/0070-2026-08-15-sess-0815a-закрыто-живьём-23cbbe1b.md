---
id: 0070
status: closed
prio: P0
hypothesis: H02
title: 2026-08-15 sess-0815A **ЗАКРЫТО ЖИВЬЁМ 23cbbe1b**
created: 2026-08-15
sess: sess-0815
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-15
closed_commit: 23cbbe1b
---
- [x] 2026-08-15 sess-0815A **ЗАКРЫТО ЖИВЬЁМ `23cbbe1b`** — все 7 компонентов прода на `23cbbe1b`; 4 зависшие операции завершились САМИ на первом клейме нового бинаря (`keycloak` 00:29:04Z, `mlflow-v2` 00:37:58Z, `fonbet-db` 00:41:07Z, `fin-core` 00:41:07Z), ни одной ручной правки в БД. `count(operations WHERE status IN ('Processing','Reconciling') AND updated_at < now()-30min)` = **0** по всей платформе; `SetDatabaseTier` в `Processing`/`Created` = 0 из 126. У всех четырёх audit-строка `outcome=success`+`metadata.noop=true` с точной причиной, `git_commit` NULL, парная `pending` на месте. Юзерская `fonbet-db` висела 10.5 ч. Остаток вынесен в E145 (замороженный снапшот у баз с именем в двух окружениях → почасовой noop-цикл). · 🔴 `SetDatabaseTier` НЕ МОЖЕТ ЗАВЕРШИТЬСЯ, КОГДА МЕНЯТЬ НЕЧЕГО — ОПЕРАЦИЯ ЖИВОГО ЮЗЕРА ВИСИТ `Processing` ЧАСАМИ (sess-0815A, 2026-08-15, [live: пульс `/api/v1/admin/overview` + прод-psql; code], hypothesis: H02) — у `artemmendeleev@gmail.com` (проект `Default`, БД `fonbet-db`, op `40ca155c-02f6-4378-9b35-cd47884888ff`) операция висит 2.6ч; вместе с ней застряли `keycloak`, `mlflow-v2`, `fin-core` — 4 из 4 последних `SetDatabaseTier`, 3 стартовали в одном 5-минутном окне. Механизм [code]: диспетчер `gitops-agent/internal/worker/dbwatcher.go:232-238` помечает терминально ТОЛЬКО ошибку, а завершение пишет `commitFilesAndRecord` → `db.MarkCommitted`; ветка «менять нечего» `db_tier.go:72-74` делает голый `return nil` — строка навсегда остаётся `Processing`, и `ClaimPending` (`internal/db/operations.go:95`) перезахватывает её каждые 30 минут вечно. Тот же голый `return nil` в `db_enforcement.go:71`, `db_shard.go:76` и `db_shard.go:157` — класс, а не единичный баг. Второй виток: снапшот `resource_snapshots.summary_json.tier` обновляется ТОЛЬКО после коммита, поэтому при `changed=false` источник правды реконсайлера никогда не догоняет git, и `db_tier_reconciler.go` ставит задачу снова; его дедуп `enqueueDatabaseTier` (`db_tier_reconciler.go:215`) считает «в полёте» только `('Created','Reconciling')` и не видит `Processing`. Пруф работы = ни одной `SetDatabaseTier` в `Processing` старше 30 минут, у `fonbet-db` терминальный статус, и `summary_json.tier` совпадает с git. **СДЕЛАНО:** `db.MarkNoop` закрывает такую операцию как `Committed` (git держит ровно то, что просили) с `git_commit = NULL`, причину несёт audit-строка `outcome=success` + `metadata.noop=true`, чтобы разбор путей отличал no-op от коммита. Переведены все четыре сайта класса: `db_tier.go`, `db_enforcement.go`, оба пути `db_shard.go`. Дедуп бэкенда (`db_tier_reconciler.go`, `db_quota_watcher.go`, `db_move_placement.go`, `db_archive_worker.go`) теперь считает `Processing` статусом «в полёте». RED дословно: `operations_noop_test.go:55: a no-op operation must end terminally, got status "Processing"` и `db_quota_enqueue_realdb_test.go:198: a second tier flip was queued behind one already being processed`; после правки оба зелёные, `gitops-agent ./...` и `backend/internal/api` зелёные на реальной риге. Побочно: фикстуры orphan-GC теперь убирают за собой — одна оставленная строка `resource_snapshots` с `live_source=k8s` валила `TestOverviewNotReadyFreshnessDetectsBlindness` и `TestPlatformStatusSnapshotReconciler_StaleAppSnapshotIsDegraded` (свежесть реконсайлера читается как MAX по ВСЕМ строкам, рига — одна общая база); проверено, что те же два теста красные и на чистом `origin/main@d89b3f78`. **M2 живьём НЕ закрыт**: образ gitops-agent в проде на момент коммита старый; после раскатки перемерить — ни одной `SetDatabaseTier` в `Processing` старше 30 минут и у `fonbet-db` терминальный статус (4 зависшие строки должны самозалечиться на следующем клейме).

---
id: 0407
status: closed
prio: P0
stream: reliability
hypothesis: H02
title: Провал RevealEnvVar/SetEnvVar без причины на форме: юзер лечит непонятную ошибку кнопкой DeleteApp
created: 2026-08-20
sess: sess-0820e
closed_at: 2026-08-20
closed_commit: af4c27ff
closed_note: Оба конца провода: env-пути отвечают respondErrorCode (16 кодов Set/Bulk/Reveal/Delete + solutions), фронт мапит код -> причина + следующий шаг и прямо говорит, что снос аппа не поможет; сырой err остаётся в логе и аудите (в нём светился ключ 08-18). Гейты RED/GREEN с обеих сторон, backend real-DB + frontend 343 pass. Живого прогона в проде не было.
---
Заземлено [live psql audit_events, разбор пути sess-0820e].

Улика: `kkartov@yandex.ru` (проект `073e255d-8e88-4aff-9621-47d46411a32c`) сделал **7 × `DeleteApp`
подряд**, и каждый раз этому предшествовал `FAILURE RevealEnvVar`. Человек не чинил env — он сносил
приложение, потому что снос был единственной понятной ему кнопкой. Это и есть терминальная точка
пути: юзер сдаётся не на крэше и не на редиректе, а на отказе интерфейса объяснить, ЧТО пошло не так.

Первопричина конкретного эпизода (хвостовой `\n` в ключе) закрыта `17db736d`. Но закрыт частный
случай, а не класс: `err` по-прежнему уезжает в лог и в аудит, а до формы env-переменных
структурированная причина не долетает — на следующем сбое расшифровки история повторится дословно.

Места, где причина теряется: `backend/internal/api/envvars.go:326`, `:487`, `:718`,
`backend/internal/api/solutions.go:696`.

Смежный, уже заведённый дефект той же подсистемы — 0398 (четыре места выкидывают err молча).
Этот пункт про ДРУГОЙ конец провода: не про то, что бэкенд молчит, а про то, что фронт не умеет
показать причину, даже когда она есть.

M2: искусственно вызванный отказ расшифровки на тестовом аппе в песочнице даёт на форме env
человеческую причину и следующий шаг, а не голый "не удалось"; в аудите видно, что юзер после
отказа остаётся в env-форме, а не уходит в DeleteApp.

---
id: 0101
status: closed
prio: P0
title: 2026-08-13 sess-0813k **ПОЧИНЕНО, ПРИЧИНА — ПОТОЛОК ОБЛАКА** · 🔴 ДОСТАВКА В ПРОД ОСТАНОВЛЕНА НА CI: билд не может получить агента
created: 2026-08-13
sess: sess-0813k
section: 🔴 P0 08-15 (sess-0815b) — «ПОСЛЕДНЯЯ МИЛЯ» ОТДАЁТ ЛОЖНО-ЗЕЛЁНОЕ: 17/17 ok ПРИ ТР
closed_at: 2026-08-13
closed_commit: 73e5c16a
---
- [x] 2026-08-13 sess-0813k **ПОЧИНЕНО, ПРИЧИНА — ПОТОЛОК ОБЛАКА** · 🔴 ДОСТАВКА В ПРОД ОСТАНОВЛЕНА НА CI: билд не может получить агента (sess-0813j, 2026-08-13, [live]). #1104

  **РЕЗУЛЬТАТ sess-0813k (2026-08-13 10:15-10:35Z).** Гипотеза sess-0813j подтверждена [live]: у облака `self-managed` в `/var/jenkins_home/config.xml:213` стоял `<containerCap>3</containerCap>` — потолок на ВСЁ облако. За 55 минут ожидания #1104 облако подняло 20+ чужих агентов (`build-maven`, `build-python`, `agent-sync-hub` — шторм бранч-индексации), наш лейбл не получил ни одного слота, пода не создавалось ВООБЩЕ (ни `FailedScheduling`, ни `FailedCreate`), в логе Jenkins — ни одной ошибки про наш лейбл (упор в cap плагин пишет на FINE). Альтернатива «нездоровый podTemplate» отпала: сообщений о попытке создания пода нет в принципе. JCasC в этом Jenkins НЕТ (`casc_configs` отсутствует, env пуст) → config.xml на PVC = источник правды. ДЕЙСТВИЯ: (1) бэкап `config.xml.bak-0813-containercap`; (2) `containerCap` 3 → 5 (ноды выдерживают: CPU 11-38%, DiskPressure=False на всех 4); (3) `kubectl rollout restart deploy/jenkins -n devops-tools`. ВТОРОЙ КАПКАН: переживший рестарт #1104 (`Resuming build after Jenkins restart`) агента заново НЕ запросил и блокировал очередь («Build #1,104 is already in progress») — снят через `POST .../1104/stop` кредом Jenkins API из `~/.claude.json` (в наборе тулов Jenkins MCP `/stop` нет). ПРУФ M2: 10:32:53Z под `dada-cloud-console-agent-4kz05-twdhs` создан, #1105 (`73e5c16a`) поехал. Память: `project_jenkins_cloud_container_cap_starves_our_builds`. Цена ошибки: 4 коммита (включая фикс «свежий апп = удалён» `5432a7e6` и upload-без-git `69e0ff60`) стояли недоставленными почти сутки.

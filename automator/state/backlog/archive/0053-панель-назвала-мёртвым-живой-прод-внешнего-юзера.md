---
id: 0053
status: closed
prio: P3
hypothesis: platform-truth
title: ПАНЕЛЬ НАЗВАЛА МЁРТВЫМ ЖИВОЙ ПРОД ВНЕШНЕГО ЮЗЕРА, ПОТОМУ ЧТО ОДИН ТИК НЕ ПРИНЁС ТЕЛА
created: 2026-08-15
sess: sess-0815k
section: Backlog (execution-bet)
closed_at: 2026-08-15
closed_commit: 5a418024
---
- [x] ✅ ПАНЕЛЬ НАЗВАЛА МЁРТВЫМ ЖИВОЙ ПРОД ВНЕШНЕГО ЮЗЕРА, ПОТОМУ ЧТО ОДИН ТИК НЕ ПРИНЁС ТЕЛА (sess-0815k, 2026-08-15, [live psql `resource_snapshots` + kubectl + 80 ручных проб изнутри кластера], hypothesis: platform-truth, origin/main@`5a418024`) — `fonbet-value` (`artemmendeleev@gmail.com`) записан `http_reason: status_503` (=мёртв) в 09:11:28Z и `app_status_503` (=жив) в 09:16:28Z. Разобрано поимённо: под `1/1 Running`, стартовал 04:29:08Z и с тех пор не трогался; эндпоинт `10.244.2.238:8000` на месте; deployment READY 1/1; `ServiceIncident` по этому сервису погас в 04:58. Тело ответа байт-стабильно: 40 проб по фактическому пути пробы hop-1 (`http://ingress-nginx-pub-controller.network.svc.cluster.local` + Host) и 40 по hop-2 (тот же хост по https, как его строит `nextRedirectTarget` — редирект НЕ уходит на публичное имя) дали 80/80 `503` с авторским JSON 3305-3307 байт, ни одной пустой. Воспроизвести красный тик не удалось — и это и есть диагноз: вердикт «мёртв» строился не на признаке смерти, а на его ОТСУТСТВИИ. `body, _ := io.ReadAll(...)` выбрасывал ошибку чтения, а `isAppAuthoredBody` по документированному правилу считает пустое тело не-авторским → `status_503`. Пустое тело от ingress-nginx невозможно (свою страницу ошибки он отдаёт всегда), значит «нечего читать» = обрыв, а не улика. Фикс `5a418024`: ошибка чтения поднята в классификатор, ingress-код без авторского тела И без прочитанных байт помечается `unknownAuthor` и перепроверяется РОВНО один раз; страница nginx и авторское тело верятся сразу, второй попытки у них нет; пусто дважды — остаётся красным. Тест `TestProbe_RetriesOnlyWhenTheAuthorIsUnknown` держит все четыре полюса, RED показан выводом (без фикса: `reason = "status_503", want "app_status_503"`). **M2 не закрыт: образ поедет через CI.** Снять после раскатки: `fonbet-value` зелёный И `n8n` красный одновременно — однополюсная проверка тут уже трижды пропускала неверный вердикт.

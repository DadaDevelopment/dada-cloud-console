---
id: 0314
status: open
prio: P2
stream: 6
title: P2-BOX-GTM-5 sess-0801i ОТГРУЖЕНО В КОД, M2 НЕ ЗАКРЫТ · Коммит 2de1667 в origin/main
sess: sess-0801i
section: 📣 ПОТОК 6 — ПРОДАТЬ МЕХАНИКУ /box (owner-directive 2026-08-01 интерактив)
closed_at: unknown
closed_commit: 2de1667
---
- [~] 🟡 P2-BOX-GTM-5 sess-0801i ОТГРУЖЕНО В КОД, M2 НЕ ЗАКРЫТ · Коммит `2de1667` в origin/main [origin]: новая секция «Подключить за 60 секунд» на `/box` сразу после «Как это работает» (`frontend/components/marketing/box-connect.tsx`, 132 строки) — таб Claude Code (`/plugin marketplace add DadaDevelopment/dada-cloud-console` + `/plugin install dada-cloud@dada-cloud`, обе команды проверены живьём: репо публичный 200, marketplace `dada-cloud`, плагин `dada-cloud`) и таб «любой другой агент» (JSON `mcp-remote` с ОБЯЗАТЕЛЬНЫМ `--static-oauth-client-info`, см. P1-BOX-DCR-BLOCKED). Badge `Приватный превью`→`Доступно сейчас`, hero CTA `Получить доступ`→`Подключить агента` на `#connect`, форма заявки осталась вторичным путём. Честность подтянута к доказанному: `works` = самообслуживание/тёплый пул 350-450мс/публичный TLS/поминутная тарификация; кристаллизация сквозняком осталась в `notYet` (живьём не доказана). Метаданные `/box` и `/en/box` больше не врут «приватный превью». Гейты: `tsc --noEmit` чисто, lint 0 ошибок, `npm run build` ок. **Доехало в прод и проверено на прод-сигнале, не по отчёту агента** [live]: deploy `dada-cloud-console-frontend` пиннится на `28ffbac7` (наш коммит внутри), старый ReplicaSet `d87cdd5d9` дренирован, 8/8 запросов к `https://cloud.dada-tuda.ru/box` отдают секцию «Подключить за 60 секунд», «Приватный превью» = 0 (было 13), обе plugin-команды в HTML, 4 маркера `box_connect:`, EN-версия отдаёт `Connect in 60 seconds` + `Available now`. Конфиг второго таба клиентский (рендерится по клику, не в первичном HTML) — проверен в бандле `/_next/static/chunks/1w02fh5_jfcvn.js`: `"mcp-remote","https://console.dada-tuda.ru/mcp","--static-oauth-client-info","{\"client_id\":\"dada-mcp\"}"` в обеих локалях. Побочно: Jenkins #757 упал `ClosedByInterruptException` — сборку `b72033a` параллельной сессии убил наш собственный пуш (supersede), не код. **M2 остаётся невыполненным**: «человек со стороны по инструкции создал box» не проверено и агентом проверено быть не может — нужен либо живой заход постороннего, либо один ручной прогон владельца по опубликованной инструкции на чистой машине. Бейзлайн для сверки после деплоя: до коммита прод `/box` отдавал 13× «Приватный превью» и 0 упоминаний коннекта [live curl].

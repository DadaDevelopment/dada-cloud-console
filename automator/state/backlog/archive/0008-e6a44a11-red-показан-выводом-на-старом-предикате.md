---
id: 0008
status: closed
prio: P0
hypothesis: H08
title: e6a44a11 (RED показан выводом на старом предикате
created: 2026-08-19
sess: sess-0819c
section: Backlog (execution-bet)
closed_at: 2026-08-19
closed_commit: e6a44a11
---
- [x] ОТГРУЖЕНО sess-0819c 2026-08-19 · `e6a44a11` (RED показан выводом на старом предикате: `TestCauseRefreshOutcomePreservesCauseOnUnmatchedLog` падал «got refreshed=true»; после правки пакет зелёный. Корень оказался НЕ в `maybeCauseRefresh`, а в `causeRefreshOutcome` `app_health_watcher.go:783`: непустой лог без совпадения считался отрицательной уликой, хотя `tailLog` читает только последние `appHealthLogTailLines` строк ОДНОГО инстанса контейнера — диагностическая строка гуляет в окно и из окна между рестартами при неизменном краше. Теперь перезаписать названную причину может ТОЛЬКО непустой новый вердикт `causeKind != ""`; kube-авторитетные причины (OOMKilled/ImagePullBackOff) приходят непустыми и перезаписывают как раньше. M2-ДОЛГ: подтвердить живьём на `gulyaev-ai-core`, что `cause_kind` перестал обнуляться, ПОСЛЕ доставки образа `:e6a44a11` в `argocd-prod`.) · 🔴 СТИРАНИЕ ПРИЧИНЫ КРАША НАБЛЮДЕНО НА ВТОРОМ ЖИВОМ АППЕ — КЛАСС `envprobe0816` НЕ ЗАКРЫТ (sess-0819b, 2026-08-19, [live kubectl+psql `app_health_alerts`], hypothesis: H08). `gulyaev-ai-core` (`lifecoachrussia@yandex.ru`, ns `lifecoachrussia-yandex-ru-prod`, регистрация в тот же день 09:37Z) крашлупится с 11:31Z, 13 рестартов, причина в логе НЕ менялась ни разу: `ModuleNotFoundError: No module named 'app'` — то есть код юзера, и платформа его верно классифицировала: `last_sent_cause_kind='app_code'`, письмо ушло 11:34:24Z (`SendNotification` success). НО живая колонка `app_health_alerts.cause_kind` СЕЙЧАС ПУСТА при неизменившемся падении — тик снова прочитал лог перезапускающегося контейнера. Следствие для юзера, который прямо сейчас смотрит консоль: `frontend/lib/app-alerts.ts` `crashCauseKey()` на пустом kind возвращает `null` (осознанно — не гадать «твой код»), поэтому `frontend/components/deploy/app-alerts-banner.tsx:568-579` НЕ рисует строку вердикта, и баннер немой: «апп падает», без причины и без рычага. Гвард `e0714e0d` этот класс не закрыл — чинить `causeRefreshOutcome` (`backend/internal/api/app_health_watcher.go` ~780) так, чтобы уже названная причина не сбрасывалась НИКОГДА без непустой отрицательной улики.

---
id: 0018
status: closed
prio: P0
title: ПЛАТФОРМА НАХОДИЛА ПРИЧИНУ КРАША И САМА ЖЕ ЕЁ СТИРАЛА (найдено прогоном E153, origin/main@e0714e0d)
created: 2026-08-16
sess: sess-0816c
section: Backlog (execution-bet)
closed_at: 2026-08-16
closed_commit: e0714e0d
---
- [x] ЗАКРЫТ sess-0816c 2026-08-16 · 🔴 ПЛАТФОРМА НАХОДИЛА ПРИЧИНУ КРАША И САМА ЖЕ ЕЁ СТИРАЛА (найдено прогоном E153, origin/main@e0714e0d) — у флапающего пода `reason` скачет `CrashLoopBackOff` ⇄ `Error` на ОДНОМ падении, дешёвый пропуск в `maybeCauseRefresh` не срабатывает, тик читает лог только что перезапущенного контейнера, получает пусто и пишет NULL поверх верного диагноза (`backend/internal/api/app_health_watcher.go:669`, `:592-600`). Наблюдено: `envprobe0816` 10:46:42Z `missing_env_var/PROBE_API_TOKEN` → 10:52:50Z `NULL` при том же падении. Починено `e0714e0d`: пустой лог БЕЗ классификации → `refreshed=false`, причина переживает тик; настоящая смена причины перезаписывать умеет (непустой лог без совпадений = реальная отрицательная улика; `OOMKilled`/`ImagePull*` идут через непустой `causeKind`). RED показан снятием гварда, GREEN восстановлен. Не покрыт DB-слой — real-DB тесты скипаются без рига.
  ⚠️ ОСТАЁТСЯ ОТКРЫТЫМ соседний вопрос: `reason=Error` не письмописуем (`emailableReason`), то есть апп, застрявший в этой ветке, не даёт юзеру письма вообще. Не чинил (правило одного яка) — мерить надо сперва, сколько живых аппов реально сидят в `Error`, а не в `CrashLoopBackOff`.

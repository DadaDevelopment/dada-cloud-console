---
id: 0277
status: closed
prio: P0
stream: 2
title: 2026-08-15 sess-0815g **ОТГРУЖЕНО 2c0a8729 в origin/main; M2 ОТКРЫТ (живой пруф
created: 2026-08-15
sess: sess-0815g
section: 🎯 ПОТОК 2 — Deploy speed & reliability как продукт
closed_at: 2026-08-15
closed_commit: 2c0a8729
---
- [x] 2026-08-15 sess-0815g **ОТГРУЖЕНО `2c0a8729` в origin/main; M2 ОТКРЫТ (живой пруф — следующий цикл)** · 🔴 P2-ROLLOUT-STRANDS-INFLIGHT-OPS · sess-0814L, найдено на своей же уборке · **Раскатка gitops-agent замораживает все операции, которые он держал в руке, ровно на 30 минут.** Механика [code]: воркер помечает пачку `status='Processing'` (`gitops-agent/internal/db/operations.go:90-105`), под умирает при роллауте — и пере-захват возможен только после `staleProcessingTimeout = 30 * time.Minute` (`gitops-agent/internal/db/operations.go:53`). Реклейм-путь есть, но его гранулярность = полчаса, и она платится ЦЕЛИКОМ при каждом штатном деплое самого агента. Пруф [live psql, окно 7 суток]: 356 терминальных операций, 11 медленнее 5 минут, из них **7 — в полосе ~31 минуты**, т.е. подпись таймаута, а не работы:
  `DeployImageVersion megafactory 08-13 23:10 → 31.2 мин` (живой юзер, тот самый, что потом трое суток лежал в CrashLoop),
  `DeployImageVersion backend 08-13 02:36 → 30.6`, `DeployImageVersion fonbet-value 08-09 23:26 → 31.3`, `CreateS3Bucket dada-archive 08-14 14:40 → 31.4`.
  Сегодня же поймал это на себе: `DeleteApp tlsprobe-0814l` и `DeleteServiceDatabase tlsprobe-db-0814l` были захвачены в 20:37:46Z, под ушёл на раскатке `25188653` (пин образа `27219cbb`, build #1152) в 20:38 — освободятся не раньше 21:07:46Z. Частота удара не редкая: в ns `argocd-prod` за 7 часов **11 ReplicaSet'ов** gitops-agent, т.е. 11 окон, в каждое из которых любая летящая операция юзера рискует встать на полчаса.
  Чинить не таймаутом (он страхует настоящую смерть пода), а корректным завершением: ловить SIGTERM и снимать свои `Processing`-захваты обратно в `Created` перед выходом, плюс `terminationGracePeriodSeconds`, которого хватит на текущую операцию. Пруф работы = после раскатки агента ноль операций в полосе 25-35 минут за неделю.
  **СДЕЛАНО `2c0a8729` (sess-0815g, 2026-08-15).** Процесс держит реестр своих захватов в памяти (`gitops-agent/internal/db/operations.go`, `holdClaim` в `ClaimPending`, `releaseClaim` в `MarkCommitted`/`MarkNoop`/`MarkFailed`) и на SIGTERM возвращает их в очередь — `ReleaseHeldClaims` делает `UPDATE ... SET status='Created' WHERE id = ANY($1) AND status='Processing'`, поэтому операция, успевшая закончиться в окне между снапшотом и UPDATE, свой вердикт сохраняет. Вызов из `gitops-agent/cmd/gitops-agent/main.go` (`releaseClaimedOperations`, свой контекст на 10s, т.к. сигнальный уже отменён) + `terminationGracePeriodSeconds: 60` в чарте (`helm/dada-cloud-console/templates/gitops-agent-deployment.yaml:24`, значение в `values.yaml`) — без него релиз убивают на полпути дефолтные 30s. RED воспроизведён ТРИЖДЫ на реальной базе, не по отчёту: без `holdClaim` в `ClaimPending` → `operations_release_test.go:39: shutdown released no operations while holding a claim`; без `AND status='Processing'` → `:69: shutdown moved 1 finished operations, want 0`; без `releaseClaim` в `MarkFailed` → `:98: a finished operation is still registered as held`. После: `go test ./internal/db/ -count=1 ok 3.754s`, `go build ./...`, `gofmt -l`, `go vet` чисты, `helm template` рендерит грейс 60. **M2 НЕ ЗАКРЫТ:** пруф берётся следующим циклом — (а) в логе пода gitops-agent после раскатки строка `released claimed operations back to the queue`, (б) ноль операций в полосе 25-35 минут среди захваченных перед роллаутом. Локальный гейт `probe-main-build.sh` НЕ отработал (<5 GiB свободно, GOCACHE 4.8G, чистить нельзя — рядом жил `go test` параллельной сессии), но вместо него отработал настоящий CI: **Jenkins `#1167` SUCCESS ровно на `2c0a8729`** [live], `go test ./...` по gitops-agent прошёл (`internal/db ok`, `internal/worker ok`), образ собран и запушен. **ЧЕСТНАЯ ОГОВОРКА:** `internal/db` в CI отработал за 0.004s — значит `TEST_DATABASE_URL` там не задан и МОИ три теста в CI были ПРОПУЩЕНЫ; их RED и GREEN сняты локально на реальной базе, CI подтверждает только компиляцию и отсутствие регрессий в остальных тестах. **ДОСТАВКА ПОДТВЕРЖДЕНА ЖИВЬЁМ** [live kubectl]: `deploy/dada-cloud-console-gitops-agent` в `argocd-prod` несёт образ `...gitops-agent:2c0a8729` И `terminationGracePeriodSeconds: 60` (единственный деплой в неймспейсе с 60, у остальных дефолтные 30), под `d64d5d4fb-r9qd5` Running 1/1. **Замер сразу после этой раскатки** [live psql]: `Processing` = 0, терминальных операций медленнее 5 минут за 6 часов = 0 — то есть эта раскатка не заморозила ничего, но и пруфа не дала: очередь была пуста, а убитый под крутил ещё СТАРЫЙ бинарь. Строка `released claimed operations back to the queue` физически может появиться только на СЛЕДУЮЩЕЙ раскатке.
Зачем: «деплой сложный и долгий» + 2 наших инцидента уронили прод artem (OOM 07-20, EIO 07-21) молча.

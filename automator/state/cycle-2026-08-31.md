# 2026-08-31 цикл (sess-0831a) — пульс: 2 CrashLoop разобраны по корню

**Время цикла:** ~40 мин, read-only диагностика + 1 артефакт.
**Гейт доступа:** probe-prod-access.sh ЗЕЛЁНЫЙ (apiserver/psql/console).
**Доставка:** прод-образ 8cf16820 == origin/main 8cf16820, ДОСТАВЛЕНО ПОЛНОСТЬЮ.
Панель живая (freshness blind=false, observed 16:08Z). Main не красный.

## Панель поломок /admin/overview — разобрано поимённо
- not_ready (2):
  1) `gulyaev-ai-core` (lifecoachrussia@yandex.ru, ЖИВОЙ юзер, audit сегодня) —
     CrashLoop 54 рестарта/4ч20м. КОРЕНЬ В КОДЕ ЮЗЕРА [live logs]: env-ключ
     мастер-ключа knowledge vault декодируется не в 32 байта ->
     `VaultError: Knowledge master key must decode to exactly 32 bytes` ->
     RuntimeError на импорте app. URL 503. Это юзерский баг, наша платформа
     ни при чём; юзер получает честный CrashLoop-диагноз в панели (уже есть).
  2) `gateway` (internal-prod, НАШ) — CrashLoop 709 рестартов/3д5ч. КОРЕНЬ [live]:
     PublicApi list GET вырос до 276 443 байт (замер live) > 256KiB дефолта
     Spring WebFlux -> DataBufferLimitException на старте; в обработчике ошибки
     ещё и `.sort()` на immutable `List.of` -> UnsupportedOperationException ->
     `Application run failed`. Оба бага в коде gateway-service (репо не в этой
     песочнице). Стопгап без правки кода: `javaToolOptionsAppend:
     -Dspring.codec.max-in-memory-size=2MB` в values gateway (чарт умеет,
     helm/common/templates/deployment.yaml:214). Разбор: automator/tasks/
     2026-08-31-gateway-crashloop.md.ВАЖНО: это упало 2026-08-18 (последний
     деплой), но алерты молчали до 08-31 15:57 — first_detected_at совпал с
     сегодняшним циклом, 13 дней слепоты (watchdog/class-fix не ловил internal-prod).
- not_ready_other (4): api-zerkalo-ru (3545д? нет, 41д, unmaintained), 
  ai-gateway-b72e67 (8.8д, unmaintained), dada-vpn-sub + payments (age 21с —
  свежие CR, ещё синхронизируются, НЕ поломка).
- domain_issues (2): m2-delwedge (наша scratch, тест агента), a2a-hub.pro (брошен
  владельцем) — обе старые известные, реаппер продолжает, не трогаем.
- failed_builds (1): dadadev-brains no_dockerfile (наша цель, известный).
- stuck_operations: 0. freshness: blind=false, stale=2 (старые known).

## Инциденты юзеров
- ЖИВЫЕ за 48ч: artempro2021 (72 события), lifecoachrussia (24), m206rv159,
  artemmendeleev. Ни один не заблокирован ПЛАТФОРМОЙ (fanvk Ready, URL 200;
  gulyaev = его код; fonbet-стек: 0 билдов за 5д, не красный).
- nepsetelegram (messiajit4): Ready, URL 502 — но это worker=false класс
  (уже 0476): бот без HTTP-листенера, платформа дала домен+ingress.
  Тот же класс, не новый инцидент.

## Замер воронки (psql)
- Юзеры 60 всего, +7д = 4 (messiajit4, kof97zip, m206rv159, zqleaders —
  08-08 ботoферма волна вымерла: 17 сигнапов того дня, 0 активны).
- Активных 48ч = 3-4 (арте-класс). Конверсия рег->проект: 31/60 имеют проект.
- Первый шаг юзеров 7д: SessionStart -> ViewApps -> CreateProject у всех 4,
  дальше расходятся (CreateApp / git-connect / БД). CreateApp 21 за 7д,
  DeleteApp 0. Builds 7д: 24/24 success (это очередь, не воронка).
- Платежи: 1 succeeded ЗА ВСЁ ВРЕМЯ = 37a8d276 2026-07-25 990₽, org=dada
  (это ПОКУПКА ОВНЕРА, не чужого). По метрике гейта #3 «первый succeeded
  от ЧУЖОГО» = по-прежнему 0. При этом 4 canceled арте (990+2900x2=6790₽
  недополученных) + pending 2900₽. Арте ХОЧЕТ платить, но отвал — не наш
  UI: canceled приходит вебхуком от ЮKassa.
- STUDSTARTUP-промо: 0 redemptions (кампания запущена, никто не ввёл).
- cryocrm (startup до 05-09 через promo reactivation): последний action 08-11.

## Беклог (CLI отсутствует в песочнице — использовал файлы)
bl/CLI нет в этой песочнице; очередь читал из backlog.md-индекса. Новых
пунктов по итогам цикла заводить не стал: gateway-фикс = код в ЧУЖОМ репо
(gateway-service, не в песочнице) — остановлен границей доступа, не як;
юзерские кейсы уже покрыты существующими пунктами (0476 worker=false).

## Что НЕ сделано и почему (честно)
- gateway stopgag (javaToolOptionsAppend) требует push в argo-infra
  clusters/beget-prod/.../gateway/values.yaml — там чужие незакоммиченные
  изменения (AGENTS.md, lessons.md, runbook) в дереве; push = деплой прод.
  Решение: отложено в артефакт с готовым диффом, применять владельцем/след.
  циклом после git status-чистки. Это единственное изменение реального
  состояния, которое цикл не довёл до конца, и причина — не як, а гейт M3.
